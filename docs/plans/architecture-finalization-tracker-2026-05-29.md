---
plan_role: input
status: active
source_of_truth: false
owner: platform
last_reviewed: 2026-06-16
adopted_as_canonical: 2026-05-31
current_program_path: docs/plans/architecture-finalization-program-2026-05-29.md
current_tracker_bridge: docs/plans/current-tracker.md
---

# Architecture Finalization — Execution Tracker

> Status: Canonical architecture-finalization queue input adopted by `docs/plans/current-program.md` and `docs/plans/current-tracker.md` on 2026-05-31. The `current-*` documents remain the formal source of truth under planning governance.

**Date:** 2026-05-29 · **Owner:** _unassigned_ · **Companion:** `architecture-finalization-program-2026-05-29.md`

**Rev 6 (2026-06-01):** targeted governance/legal-entity refinement only; no re-architecture and no Phase-C runtime implementation. Added `recovery_legal_tenant_id` as the recovery/representation entity distinct from membership `legal_tenant_id` (T-208/T-209/T-408 + ADR-13/15); made cross-jurisdiction handoff grant **case-scoped access** instead of a tenant switch (T-209/T-302b); strengthened ADR-09/T-301 with separation-of-duties and audited break-glass expectations for technical super-admin/governance/legal-entity administration; added open decision #14 to keep detailed operational RACI/pricing roles as business governance, not pilot code scope. **Rev 7 (2026-06-03):** pre-M1 hardening from external Opus review completed in PR `#922` / squash merge `0bb05318fdc3d2bc4899ba3507b6f3238fa4c1be`: the domain authority owns the current claim-status graph and raw SQL writer detection covers the physical `claim` table before `ARCH-M1-01`; it did not start `T-104`, add events, or reopen routing/auth/tenancy/schema scope. **Rev 8 (2026-06-03):** `ARCH-M1-01` / `T-104` completed in PR `#924` / squash merge `b742476ed6906749e1cdc61086588eea8f11d2e5`: the additive `domain_events` transactional outbox table and typed `appendEvent(tx, ...)` helper now exist. **Rev 9 (2026-06-04):** `ARCH-M1-02` completed in PR `#926` / squash merge `ddbc207ca51b36a09ba1c2358f831c7e24504beb`: `transitionClaimStatus()` now appends one conservative non-PII `claim.status_changed` event inside the status/lifecycle/history transaction, rollback proof covers status/history/event together, same-status no-op paths emit no event, and `domain_events` has tenant-isolated RLS. `T-002` and `T-002c` are closed; `T-105` is WIP only for the claim status-change subset. **Rev 10 (2026-06-04):** `ARCH-M1-03` / `T-104c` completed in PR `#928` / squash merge `cc9c87554a41c431ad933b2f3396635565f5a398`: `appendEvent()` now fails closed through an explicit payload allowlist, starts with `claim.status_changed@1`, rejects unsupported names/versions and missing/extra/invalid fields, and inserts a sanitized plain payload object. **Rev 11 (2026-06-04):** `ARCH-M1-04` / `T-104b` completed in PR `#930` / squash merge `92cb4f049df56a9ffcdc22973967ba0e9c90bd2f`: `domain_event_deliveries` now records tenant-isolated per-consumer deliveries with stable idempotency keys, relay selection is tenant-scoped, undelivered mode filters through delivery records with transaction-safe locking, replay mode supports arbitrary offsets without row locking, and delivery recording is idempotent. **Rev 12 (2026-06-04):** `ARCH-M1-05` / `T-105b` audit projection foundation completed in PR `#932` / squash merge `2cf984c6b66eae1a05e5ff8e3c5a4072ff07ee05`: `claim.status_changed@1` now has a bounded `audit_projection` consumer foundation with deterministic audit row IDs, idempotent replay behavior, projection failure retry proof, and event-name/version relay filtering. `T-105b` remains WIP because existing domain-claims best-effort `claim.status_changed` audit calls still need to be retired after the projection path is activated. The next bounded slice is `ARCH-M1-06`, limited to that activation and duplicate-audit retirement before timeline reads, billing consumers, PII reference tables, or broader event families. **Rev 13 (2026-06-04):** `ARCH-M1-06` / `T-105b` completed in PR `#934` / squash merge `462e5c3baf5131ade6615bf1c935a795304bc85e`; the next bounded slice is `ARCH-M1-07`, limited to `T-103` claim case/recovery lifecycle-state foundation while `status` remains authoritative through M2. **Rev 14 (2026-06-04):** `ARCH-M1-07` / `T-103` completed in PR `#936` / squash merge `55179bdb3a1f9eae40bc62831e61de2f0166360b`; the next bounded slice is `ARCH-M1-08`, limited to `T-101` incident-country live writer foundation. **Rev 15 (2026-06-05):** `ARCH-M1-08` / `T-101` completed in PR `#938` / squash merge `dc5b5ef68281dceaa94294169343ee5e96c7bb5e`; the next bounded slice is `ARCH-M1-09`, limited to `T-102` incident-country backfill and coverage reporting before `T-111`, ida-host/session work, event retention, timeline reads, or broader event-family expansion. **Rev 16 (2026-06-08):** `ARCH-M1-09` / `T-102` completed via direct commit `624c6c5af2184dfbc359f3b7a24ef2c93415c1f8` (idempotent `buildIncidentCountryBackfillPlan`, dry-run operator script `scripts/backfill-incident-country.ts`, coverage reporter `scripts/incident-country-backfill-report.ts`; durable backfill source is `claim_stage_history.note` diaspora-origin notes as claim-pack JSON is not persisted per-claim); the next bounded slice is `ARCH-M1-10`, limited to `T-111` governing-law and terms-version foundation. **Rev 17 (2026-06-08):** `ARCH-M1-10` / `T-111` completed in PR `#995` / squash merge `fa0e930f3011533952029c8789f82964cc4dc037`: nullable `governing_law` (ISO 2-letter check constraint in both Drizzle schema and migration SQL, backfilled via `UPPER(country_code)`) and `terms_version` added to `tenants` via additive migration `0076`; the next bounded slice is `ARCH-M1-11`, limited to `T-112` entity-of-record foundation on `subscriptions` (nullable `legal_tenant_id` + `billing_entity` + `governing_law_snapshot` + `terms_version_accepted`; backfilled from current tenant; no live-writer wiring and no billing-path changes in this foundation slice). **Rev 18 (2026-06-08):** wove UI/UX & dashboard modernization into existing M1/M3/M4/M5 nodes (no separate UI/UX workstream; critical path unchanged). Extended T-108/T-114 (neutral `ida.*` shell + canonical UI E2E lane), T-206 (timeline streaming), T-502 (active `legacy/` deletion + client-layout dedup). New tasks T-115 (front-door shell + skeleton), T-310 (dynamic theme tokens via CSS custom properties injected by Server Components + CDN-served tenant assets, never host, no cross-`access_tenant_id` brand leak — ADR-20), T-410 (lint-enforced `useOptimistic` boundary: reversible actions only, status-spine mutations stay pessimistic), T-411 (shared Smart Next Step library backing SVC-DASH + FLIGHT-09). Modularity reuses the existing `T-007b` 150-line gate. Resolved open decision **#17** with concrete budget (Lighthouse mobile > 90, entry bundle < 120 KB gzipped via size-limit CI, public-page TTFB < 100ms from Edge CDN); added open decisions #15 (design-token strategy) and #16 (skeleton/streaming library). Proposal: `2026-06-07-uiux-modernization-integration-amendments.md`. **Rev 19 (2026-06-08):** `ARCH-M1-11` / `T-112` completed in PR `#997` / squash merge `0bd2eace04eb86a2b123b3348163a72f15acf3cb`: nullable `legal_tenant_id` (FK to tenants), `billing_entity`, `governing_law_snapshot` (ISO 2-letter check constraint), and `terms_version_accepted` added to `subscriptions` via additive migration `0077`; `subscriptions` and `membershipPlans` extracted to own schema files; `memberships.ts` re-exports both for backward compatibility; `legal_tenant_id` backfilled from `tenant_id`, `governing_law_snapshot` backfilled from `tenants.governing_law`; 3 focused unit tests cover schema export, column ordering, and migration content; live-writer wiring at acceptance deferred to a later slice. The next bounded slice is `ARCH-M1-12` / `T-106` (finish deferred backfills: `claims.claim_number` + `tenants.code`; enable unique indexes; size M, no dependencies). **Rev 20 (2026-06-08):** `ARCH-M1-12` / `T-106` deferred backfills in progress: additive migration `0078` populates `tenants.code` via `UPPER(SPLIT_PART(id,'_',2))` for NULL rows (unique constraint already exists from migration `0024`); operator script `scripts/backfill-claim-number.ts` generates `claims.claim_number` values via `generateClaimNumber()` with dry-run-first, `--apply` + `--tenant`/`--limit` guards, per-claim transaction, and skip on null `createdAt`; schema comment cleanup removes stale "Nullable for migration" annotations from `claims.claim_number` and `tenants.code`; 9 focused unit tests pass; event retention, crypto-shredding, ida-host work, timeline reads, billing consumers, and broader event-family expansion remain out of scope for this slice. **Rev 21 (2026-06-08):** `ARCH-M1-12` / `T-106` completed in PR `#999` / squash merge `b11ae6d3ecc489e3a39b45a76c1e0565cb7c3ea9`: additive migration `0078` populates `tenants.code` via `UPPER(SPLIT_PART(id,'_',2))` for NULL rows; `scripts/backfill-claim-number.ts` generates `claims.claim_number` via `generateClaimNumber()` with dry-run-first guards; schema comment cleanup removes stale "Nullable for migration" annotations; 9 focused unit tests pass. The next bounded slice is `ARCH-M1-13` / `T-104d` (event retention + crypto-shredding: GDPR erasure renders PII unreadable via key destruction while event row / hash chain stays intact; documented in ADR-03; depends on T-104c DONE; Size M). **Rev 22 (2026-06-10):** Phase-4 integration pass — additive planning only; no statuses changed, no re-sequencing, critical path unchanged, `ARCH-M1-13` / `T-104d` remains the next promoted slice. (a) **Unified premium dashboard composition** extending the Rev 18 UI/UX weave: new nodes T-116 (`CaseSummary` discriminated union + compile-time-exhaustive case-kind renderer registry — accidents, Green Card, and VONESA in one interface with no UI silos), T-117 (RSC dashboard shell with `@cases`/`@actions`/`@timeline` parallel-route slots, PPR, `React.cache()` session/context dedupe, ≤2 projection queries per view), T-118 (`ui/crystal` Crystal Home V2 matte/glass primitives per `2026-01-28-matte-layered-dashboard-design.md`), T-210 (timeline event-renderer registry over `domain_events`), T-309 (tenant-keyed cache guard — the cache twin of `assertNoTenantLeak`), FLIGHT-09b (VONESA 5-step Kontrollo→Apliko→Kërkesa→Ndjekja→Pagesa stepper as a pure lifecycle projection). (b) **CQRS read models** for Finance/Reporting as outbox-only consumers (new Workstream H: CQRS-00…04; ADR-21; program §2c) — cross-domain DB joins forbidden; the VONESA 30% success fee tracked via a `fee_receivables` projection; marketing/funnel metrics via reporting projections; projection builds stay gated behind DOM promotion (OD#18). (c) **Type-level guard layer** (ADR-22): T-002d branded `AuthorizedTransition` proof, FLIGHT-05b phantom `Signed<>`/`Accepted<>` witnesses + closed `CompensationEvidence` union (no-win-no-fee unrepresentable), T-403b brand-minted `AICallContext`. (d) **Enterprise-readiness DoD weave** (program §6): `ent-tm`/`ent-perf`/`ent-sca`/`ent-dlv` evidence lanes are program-completion criteria; T-104h is explicitly the ENT-DLV01/02 execution vehicle; added OD#18 (CQRS promotion timing) and OD#19 (fee-receivable Art. 17(3) retention residuals).

**Rev 23 (2026-06-11):** `T-002d` completed through PR `#1006` / squash merge `ca35bbfde6d30db82f4e53e5f5b8e510f795302c`: `transitionClaimStatus()` status persistence now requires branded `AuthorizedTransition` proof minted by `canTransition()`, the brand is module-private, raw-status persistence has compile-fail proof, and the runtime re-check plus writer guard remain. No schema, proxy, routing, auth, tenancy, UI, or i18n scope changed, and no replacement architecture runtime slice was promoted.

**Rev 24 (2026-06-12):** `ARCH-M1-13` / `T-104d` completed through PR `#1001` / squash merge `6e364a58ac4219512f7d8dcc9802eb85b812771a`: `domain_event_keys` now records tenant-scoped subject erasure keys, tenant-isolated RLS is enabled, `markSubjectErased()` / `isSubjectErased()` are exported from `@interdomestik/database`, and focused tests cover migration shape, input normalization, blank identifier rejection, and erased-key detection. The next promoted architecture slice is `T-104f`, limited to the reference-only encrypted PII model for domain events; timeline UI, broad DSR coverage, billing, routing, auth, tenancy, and product surfaces remain out of scope.

**Rev 25 (2026-06-12):** `T-104f` completed through PR `#1015` / squash merge `ec04913b1253db9e7aa4afa94063a8705a670b0f`: `event_pii_references` and `event_pii_keys` now provide tenant-scoped reference-only encrypted PII storage for domain events, separated key material, tenant-isolated RLS, exported create/read helpers, destroyed-key and erased-subject fallback behavior, and focused migration/helper proof. The next promoted architecture slice is `T-104g`, limited to bounded per-isolate key caching and destroyed/missing-key negative-path proof; timeline UI, broad DSR coverage, billing, routing, auth, tenancy, and product surfaces remain out of scope.

**Rev 26 (2026-06-12):** `T-104g` completed through PR `#1016` / squash merge `a900a84974cd133835a03264b850c8496f170b0b`: event PII key material now uses a bounded per-isolate cache that is never a correctness boundary, destroyed/missing/erased reads evict cache state, and focused proof covers stale-cache override, TTL/max-entry bounds, and destroyed/missing-key fallback. The next promoted architecture slice is `T-104h`, limited to GDPR erasure render proof across the listed PII residue stores; runtime DSR workflows, timeline UI, billing, routing, auth, tenancy, and product surfaces remain out of scope.

**Rev 27 (2026-06-12):** `T-104h` completed through PR `#1017` / squash merge `ac4f7cf39a93eb7916b531358bc17d3a340e72d4`: erasure render helpers now preserve the timeline event skeleton when event PII is unavailable, redact only own raw IP/UA audit metadata fields, and define erased-subject render contracts for `claim`, `claim_documents`, and `member_notes` while preserving non-PII row shape. No replacement runtime slice is promoted in this closeout because OBR-eligible `T-113` depends on non-promoted ida-host work and `T-204`/`T-208`/`T-209` depend on later structural work; the next governed action is an `OBR-COMMIT` record/blocker update or explicit design-gate re-authorization decision.

**Rev 28 (2026-06-14, OBR-DG07):** `T-408` is complete through PR `#1051` / squash merge `037fa75099a0f9e33e84605613eb8d65c5f991d8`, and the post-`T-408` gate in `docs/plans/2026-06-14-obr-dg07-t301-role-boundary-promotion.md` explicitly promotes only `T-301` as the next implementation goal. `T-209` remains blocked on `T-302b`; `T-302/T-302b`, full M3, `T-202`, M5, ida-host, AI posture, UI/UX overhaul, WS-F/G/H, OMG, DOM, and structural-only work remain unpromoted unless separately reauthorized.

**Rev 29 (2026-06-15):** `T-301` completed through PR `#1056` / squash merge `6d222fc8b51868e507efee99ec47b222c913dd61`: role/capability boundaries now separate `admin`, `tenant_admin`, technical `super_admin`, `global_support`, and `auditor`; governance approval explicitly excludes technical super admins and self-approval; break-glass context requires reason, future expiry, and `security.break_glass_used` audit metadata; explicit tenant `super_admin` grants remain valid for admin portal semantics; branch-manager access requires tenant and branch context. `T-304` is now the next active governed implementation goal. `T-209` remains blocked on `T-302b`; `T-302/T-302b`, broad M3, `T-202`, M5, ida-host, AI posture, UI/UX overhaul, WS-F/G/H, OMG, DOM, and structural-only work remain unpromoted unless separately reauthorized.

**Rev 30 (2026-06-15):** `T-304` completed through PR `#1060` / squash merge `c53f4d37716755924a4c2a3a954689a468224771`: member-surface sessions now exercise member scope separately from persisted `user.role`; persisted agent role no longer grants the member-dashboard agent-only bypass on `/member`; focused unit and Playwright gate proof covers an ex-agent entering `/member` and receiving member scope only. Remote CI, PR E2E, Pilot Gate, SonarCloud, CodeQL, security checks, Copilot/Codex review-thread remediation, and `pr-finalizer` were green before merge. `T-306` is now the next active governed implementation goal. `T-209` remains blocked on `T-302b`; `T-302/T-302b`, broad M3, `T-202`, M5, ida-host, AI posture, UI/UX overhaul, WS-F/G/H, OMG, DOM, CRM expansion, and structural-only work remain unpromoted unless separately reauthorized.

**Rev 31 (2026-06-15):** `T-306` completed through PR `#1063` / squash merge `5037276aa9edb2c4efd0b2566c507bf0e4262249`: membership attribution is now durable and read-only, read-scope bindings are revoked rather than granted, attribution cannot confer claims/recovery/document access, and commission ownership remains payable from durable subscription ownership instead of active read-scope rows. Remote CI, PR E2E, Pilot Gate, SonarCloud, CodeQL, security checks, Codex reviewer-thread remediation, and `pr-finalizer` were green before merge. `T-308` is now the next active governed implementation goal. `T-209` remains blocked on `T-302b`; `T-302/T-302b`, broad M3, `T-202`, M5, ida-host, AI posture, UI/UX overhaul, WS-F/G/H, OMG, DOM, CRM expansion, and structural-only work remain unpromoted unless separately reauthorized.

**Rev 32 (2026-06-15):** `T-308` completed the ADR-only closeout by authoring `docs/architecture/adr-05-attribution-read-only.md` and finalizing `docs/architecture/adr-09-role-separation-governance-boundaries.md`. ADR-05 records attribution as durable commercial provenance only and never data access, route/session elevation, tenant/RLS authority, or billing authority. ADR-09 records accepted role boundaries for technical `super_admin`, tenant governance roles, read-oriented `global_support`, read-oriented `auditor`, scoped operational roles, separation-of-duties, and reasoned time-limited break-glass metadata. This does not start `T-209`, `T-302`, `T-302b`, broad M3, `T-202`, M5, ida-host, AI posture, UI/UX overhaul, WS-F/G/H, OMG, DOM, CRM expansion, runtime routing, auth, tenancy, billing, schema, RLS, proxy, or product work unless separately reauthorized.

**Rev 33 (2026-06-15, OBR-DG08):** the post-`T-308` gate in `docs/plans/2026-06-15-obr-dg08-t109-country-host-alias-promotion.md` explicitly promotes only `T-109` as the next implementation goal. `ARCH-FINAL` remains the program umbrella, not a standalone implementation slice. `T-209` remains blocked on `T-302b`; `T-302`, `T-302b`, broad M3, `T-202`, M5, ida-host expansion beyond `T-109`, AI posture, UI/UX overhaul, WS-F/G/H, OMG, DOM, CRM expansion, and product work remain unpromoted unless separately reauthorized.

**Rev 34 (2026-06-15):** `T-109` completed in PR `#1069` / squash merge `b8e29233b9ec2d8c63d3844ef30de49b89381f4f`: country hosts `ks`, `mk`, `al`, and `pilot` now resolve through explicit compatibility-alias metadata with default booking hints; `ida.*` remains neutral public/no-tenant; hostile lookalike hosts fail closed; and an architecture guard prevents new host-as-tenant branching outside the allowed compatibility boundary. `T-114` is now the next active governed implementation goal for ida-localhost/session-context setup, subject to fresh current-authority resolution before implementation.

**Rev 35 (2026-06-16):** `T-114` completed in PR `#1072` / squash merge `104899bd46c1c06cf5a8fbbe32061002ad781f31`: explicit `ida.localhost` Playwright/session setup now exists with scope-specific auth state and dashboard smoke proof for member/admin/agent/staff without host-as-tenant shortcuts; country-host lanes remain alias/regression coverage. Copilot/Codex review comments were fixed in `efa1e77c`; SonarCloud quality gate passed with `0` new issues and `0` security hotspots; remote CI, PR E2E, Pilot Gate, CodeQL, security checks, Vercel ignored-build, and `pr-finalizer` were green before merge. `T-302` is now the next active governed implementation goal. `ARCH-FINAL` remains the program umbrella, not an implementation slice; `T-209` remains blocked on `T-302b`, and `T-302b`, broad M3, `T-202`, M5, AI posture, UI/UX overhaul, WS-F/G/H, OMG, DOM, CRM expansion, and product redesign remain unpromoted unless separately authorized.

**Rev 36 (2026-06-16):** `T-302` completed in PR `#1075` / squash merge `927349df686e5497d7a8bad5aacf10ad0669f66a`: `resolveTenantContext(request, session)` now provides the focused four-context resolver foundation for `host_id`, `booking_tenant_id`, `access_tenant_id`, and `legal_tenant_id` while preserving existing request-source precedence and `hasHostSessionTenantMismatch` behavior. Focused proof covered host/session parity, trusted forwarded hosts, neutral/default host behavior, stale-cookie conflict, missing session tenant handling, and legal-tenant derivation. Copilot's environment-reset finding was fixed; the Codex session-wins comment was resolved as `T-303` out-of-scope; SonarCloud quality gate passed with `0` new issues and `0` security hotspots; remote CI, PR E2E, Pilot Gate, CodeQL, security checks, Vercel ignored-build, and `pr-finalizer` were green before merge. `T-303` is now the next active governed implementation goal, subject to fresh current-authority resolution before implementation. `T-209` remains blocked on `T-302b`, and `T-302b`, broad M3, `T-202`, M5, AI posture, UI/UX overhaul, WS-F/G/H, OMG, DOM, CRM expansion, and product redesign remain unpromoted unless separately authorized.

**Rev 37 (2026-06-16):** `T-305` completed in PR `#1080` / squash merge `b3fc99e055c47313478e99be8e875b4c413aeabb`: tenant context resolution now returns distinct `host_id`, `booking_tenant_id`, `access_tenant_id`, and `legal_tenant_id` concepts; session concepts are normalized behind a focused helper with legacy `tenantId` compatibility; access/session mismatch protection remains tied to `access_tenant_id`; booking/legal-only session data does not grant access tenancy. Focused proof, required Phase C local gates, remote CI, PR E2E, Pilot Gate, SonarCloud, CodeQL, security checks, Copilot no-comment review, Vercel ignored-build, and `pr-finalizer` were green before merge. At the time of Rev 37, the fresh authority candidate was `T-302b` access-tenant isolation/RLS re-pointing; Rev 39 later completed it, so this historical note is no longer current promotion authority. `T-209` remained blocked on `T-302b`, and broad M3, `T-202`, M5, AI posture, UI/UX overhaul, WS-F/G/H, OMG, DOM, CRM expansion, and product redesign remained unpromoted unless separately authorized.

**Rev 38 (2026-06-16):** tracker-only closeout evidence for `T-305` was recorded in PR `#1081` / merge `9be386a3e5ae61c067d2669dfa77fe411ce52aab`. This governance correction also removes the stale `T-209` prerequisite from `T-302b`, because the current authority records `T-209` as blocked on `T-302b`; `T-302b` is the enabling isolation/RLS step, not dependent on the later handoff implementation. No product/runtime/auth/schema/business logic, proxy, routes, billing, README, AGENTS, or architecture-document behavior changed.

**Rev 39 (2026-06-17):** `T-302b` completed in PR `#1085` / squash merge `b9c8eba408b741fc089935915958ca90866f5c4d`: access-sensitive application paths now resolve isolation through explicit `access_tenant_id` semantics, case-scoped document grants exist in shared-auth, and focused proof covers access-tenant fallback, portal/admin/upload/document access, and tightened staff/branch visibility. Remote checks were green before merge, including CI, PR E2E, Pilot Gate, security, CodeQL, SonarCloud, commitlint, and `pr-finalizer`; Copilot findings were remediated and Codex GitHub review was present. Broad RLS policy migration remains unstarted; the next canonical candidate is `T-302c`, subject to fresh current-authority resolution.

**Rev 5 (2026-05-30):** incorporated the adversarial audit review (`adversarial-audit-review-2026-05-30.md`) and user-driven modularity standards. Concurrency + atomicity + access-tenant hardening — all Phase-C-safe (additive/transitional, no `proxy.ts`, Paddle kept). New: `T-001b` (lifecycle*version), `T-007b` (150-line modularity rule for new/modified files), `T-104f/g/h` (reference-only PII + key cache + erasure render), `T-105b` (audit derived from events), `T-203b` (lock recovery prereqs), `T-302c/d` (extend `withTenantContext` + migrate RLS to `app.current_access_tenant_id`), `T-305b` (access_tenant_id on divergent tables), `T-205b` (law-pack perf guard), `SVC-CORE-b` (law-pack registry). Strengthened: **T-002** → single transactional `transitionClaimStatus()` command (+`T-002c`); **T-104b** → per-consumer `domain_event_deliveries` + `FOR UPDATE SKIP LOCKED`; **T-208** → `T-208b` no-fallback test. Resolved open decision **#8** → reference-only PII + crypto-shred keys. Note: real Postgres RLS already exists (so T-302d is a policy \_migration*, not new RLS); inbound webhook idempotency already exists (reuse `commercial_action_idempotency` pattern).

**Rev 4 (2026-05-30):** integrated **VONESA (Passenger Rights, WS-F: FLIGHT-00…11)** and the **10 renewed services (WS-G: SVC-00…10)** as feature-flagged verticals on the existing spine — they gate **nothing** on the critical path, Design Gate, or Go-Live. Confirmed the canonical **6 domains** (expertise = `domain-recovery/expert-network`, no 7th domain). T-007 brand-lint now also **asserts the mandatory protective message on key pages**; T-002b adds the flight (`NO ASSIGNMENT/POA = NO SUBMISSION`) and vehicle-damage (`no negotiation without valuation-delta + consent`) transition invariants. New ADR-13/14/15. Analysis: `vonesa-architecture-integration-2026-05-30.md`.

**Rev 3 (2026-05-29):** four-context model from the aspirational diagram — `resolveTenantContext()` now returns `host_id` / `booking_tenant_id` / `access_tenant_id` / `legal_tenant_id`, with **`access_tenant_id` (isolation/RLS) split from `legal_tenant_id` (billing/law)** (T-302 four-context union, T-302b re-point isolation, T-305 adds `access_tenant_id`); `recovery_law` named as an explicit case-level value derived from `incident_country_code` (T-208).

**Rev 2 (2026-05-29):** incorporated the code-grounded critique (`CRITIQUE-architecture-finalization-2026-05-29.md` + 8 implementation-gap findings). New/changed: T-000 writer inventory + all-writers routing (admin path bypass confirmed); T-006b role-array lint + `global_support`/`auditor`; T-104→outbox+relay+PII allowlist+crypto-shredding (T-104b/c/d); T-108 real no-tenant context type; T-101/102 live incident-country mapping; T-112 targets `subscriptions`; T-113 `user.residence_country`; T-208 out-of-network fallback; T-408 per-subscription billing snapshot + webhook reconciliation; T-506/507 migration active-case guard + residence/DSR flow.

**Rev 3 (2026-05-29):** added the **front-door/session-context stabilization prerequisite for dashboard gate design**. New/changed: T-011 inventories Playwright host-as-tenant lanes; T-108/T-109/T-114 require `ida.localhost` canonical dashboard/auth setup with explicit session tenant context; T-302/T-303 clarify that proxy cannot be the only authority because edge middleware may not know `session.user.tenantId`; M0/M1 gates now block new dashboard/auth tests that rely on `ks/mk` hostnames as tenant identity.

**How to use:** Each task has an ID, milestone, workstream, acceptance criterion (the test/check that proves it done), dependencies, and the scored dimension it moves. Work top-to-bottom within a milestone; tasks at the same milestone with no shared dependency can run in parallel. Update **Status** as you go: `TODO` → `WIP` → `REVIEW` → `DONE`.

**Legend** — WS: A=Tenant decomposition, B=Case/Recovery, C=Observability, D=Authz/Routing, E=Product-model, F=Passenger Rights/VONESA, G=Assistance Services Catalog, H=CQRS read models (Finance/Reporting — outbox-only consumers, DOM overlay). Dimension codes: ARCH, PROD, DEBUG, BND (boundaries), SOC (separation), SM (state machines), TEST, OBS, ISO (isolation), LP (least privilege), ROUTE, COUP (coupling), UX (frontend/UX quality), PERF (performance budget). UI/UX work has no separate workstream — it is woven into WS D (routing/layout) and E (product-model).

---

## Milestone M0 — Guardrails (no schema change; Phase-C safe)

| ID     | Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | WS  | Acceptance criterion                                                                                                                                                                                                                                                                                                                                                                                                    | Deps          | Dim         | Est | Owner | Status |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ----------- | --- | ----- | ------ |
| T-000  | **Writer inventory:** enumerate every `claims.status` writer in the repo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | B   | Inventory artifact: `docs/plans/2026-05-31-arch-m0-01-writer-inventory.md`; guard: `scripts/check-claim-status-writers.mjs` via `pnpm security:guard`. Confirmed runtime writers include `claims/status.ts`, `staff-claims/update-status.ts`, `admin-claims/update-status.ts` (bypass), `agent-claims/update-status.ts`, draft cancel, legacy staff helper, admin ops action, and creation/submit initialization paths. | —             | SM          | S   |       | DONE   |
| T-001  | Write `canTransition(from,to,actor,context)` pure fn over current 8-status array; harden it as the sole owner of the current allowed transition graph before M1.                                                                                                                                                                                                                                                                                                                                                                                                                         | B   | Unit tests: valid graph transitions are allowed; illegal jumps such as `draft -> resolved` and `resolved -> draft` are rejected before update/history; `→negotiation`/`→court` remain rejected unless `paymentAuthorizationState==='authorized'` or staff recovery prerequisites are satisfied; app admin UI derives its transition options from the domain graph.                                                      | T-000         | SM          | M   |       | DONE   |
| T-002  | Replace **all writers from T-000** with one **transactional command** `transitionClaimStatus()` (the sole writer); `canTransition` is called inside it. Staff, package admin, package agent, package generic `claims/status.ts`, package legacy helper, member draft cancellation, and app-local admin ops adoptions have landed; claim creation/submit initialization is explicitly classified as initialization-only. PR `#926` completed the remaining event append by writing one conservative non-PII `claim.status_changed` event only for successful status-changing transitions. | B   | One command does, in one tx: load aggregate (+`lifecycle_version`) → `canTransition` → optimistic update → `claimStageHistory` insert → `appendEvent(tx, ...)`; admin & agent direct writes removed; CI grep bans `db.update(claims).set({status})` outside the command; rejection tests on each old path; same-status no-op/history paths intentionally emit no `claim.status_changed` event.                          | T-001, T-001b | SM, ARCH    | L   |       | DONE   |
| T-002c | Atomic-command guarantee: status + history + event commit together or not at all                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | B   | PR `#926` fault-injection proof shows status, lifecycle-version, history, and event commit together and roll back together; no partial commit remains on the aggregate path.                                                                                                                                                                                                                                            | T-002         | SM, OBS     | M   |       | DONE   |
| T-002b | Extend `canTransition` with the service/flight invariants (engine grows as SVC/FLIGHT land)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | B   | (1) `→submitted_to_airline` rejected w/o signed assignment\|POA + accepted fee + consent; (2) vehicle-damage `→negotiation` rejected w/o valuation-delta + signed service consent; (3) sensitive services need medical consent (+ human review for invalidity). Each tested on every writer                                                                                                                             | T-001         | SM          | M   |       | TODO   |
| T-002d | **Branded transition proof (Rev 22, ADR-22):** the persistence step inside `transitionClaimStatus()` accepts only an `AuthorizedTransition` value minted by `canTransition()` via an **unexported `unique symbol` brand**. Completed in PR `#1006` / squash merge `ca35bbfde6d30db82f4e53e5f5b8e510f795302c`.                                                                                                                                                                                                                                                                            | B   | The brand symbol is module-private to the transition package, so no other module can construct the proof; the sole status-write site requires `AuthorizedTransition` in its signature (no raw-status overload); a `@ts-expect-error` compile-fail fixture proves raw structural construction and raw-status persistence do not type-check; the existing CI grep ban and runtime re-check remain as defense-in-depth.    | T-002         | SM, TEST    | S   |       | DONE   |
| T-003  | Route `staff-claims/update-status.ts` through `canTransition` (replace ad-hoc checks)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | B   | All write paths share one transition authority; existing staff tests still green                                                                                                                                                                                                                                                                                                                                        | T-001         | SM          | S   |       | DONE   |
| T-004  | Extract `assertNoTenantLeak(rows, tenantId)` from `server/domains/claims/index.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | C   | PR `#885` extracted the reusable wrapper, wired the original claim list path through it, and proved same-tenant rows pass while cross-tenant rows throw.                                                                                                                                                                                                                                                                | —             | ISO, DEBUG  | S   |       | DONE   |
| T-005  | Apply `assertNoTenantLeak` to every list query in `server/domains/*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | C   | PR `#887` completed the current applicable server-domain list-query adoption by routing the claims list through a tenant-safe wrapper, proving direct raw-query usage stays behind that wrapper, and adding overlapping-id two-tenant guard proof.                                                                                                                                                                      | T-004         | ISO         | M   |       | DONE   |
| T-006  | Role-exhaustiveness + distinctness test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | D   | PR `#889` exports `ROLE_PERMISSIONS` and proves every `ROLES.*` value is covered, only declared permissions are used, exported role arrays are immutable, and `admin`/`tenant_admin` are not permission-identical to `super_admin`.                                                                                                                                                                                     | —             | LP, TEST    | S   |       | DONE   |
| T-006b | Lint: forbid raw role arrays outside shared authz helpers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | D   | PR `#892` added a repo-owned guard that fails on inline role lists (e.g. `['admin','staff']`) outside the approved shared-auth/static proof boundary, with narrow allowlist custody, seeded allow/reject proof, and `security:guard` wiring.                                                                                                                                                                            | T-006         | LP, TEST    | M   |       | DONE   |
| T-007  | CI brand-discipline lint over `messages/**` + checkout/registration + email templates: (a) block banned framings across **all** services/locales, (b) **assert the mandatory protective message is present on every key page**                                                                                                                                                                                                                                                                                                                                                           | E   | PR `#894` added the brand discipline guard with explicit checked surfaces, banned-framing policy, mandatory protective-message proof, seeded violations, and required guard-lane wiring.                                                                                                                                                                                                                                | —             | PROD, TEST  | M   |       | DONE   |
| T-007b | **Modularity guard (150 lines):** Enforce the 150-line modularity rule for all new/modified files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A   | PR `#896` added the modularity guard with explicit generated/vendor/baseline exception policy, seeded oversized-new-file detection, seeded oversized-legacy-file-growth detection, and required guard-lane wiring.                                                                                                                                                                                                      | —             | ARCH, TEST  | S   |       | DONE   |
| T-008  | `compensation guaranteed` lint across all locales + email templates                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | E   | PR `#897` added `compensation guaranteed` to the compensation-promise framing policy and proved seeded detection in locale/message copy plus the explicit thank-you email template surface.                                                                                                                                                                                                                             | T-007         | PROD, TEST  | S   |       | DONE   |
| T-009  | Convert `packages/domain-claims/src/claims/status.ts` stringly returns to `ActionResult<T>`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | B   | PR `#899` converted the claim-status path to the package `ActionResult` discriminated contract, preserved existing error strings and success behavior, updated focused tests, and proved no legacy `return { error:` remains in the converted path.                                                                                                                                                                     | —             | DEBUG       | S   |       | DONE   |
| T-010  | Author ADR-04 (`canTransition` sole writer) and ADR-09 stub (role separation)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | B,D | `docs/architecture/adr-04-claim-status-transition-sole-writer.md` records the sole-writer architecture; ADR-09 was created as a stub in `docs/architecture/adr-09-role-separation-governance-boundaries.md` and later finalized by `T-308`, without runtime role, proxy, auth, tenancy, schema, or billing changes.                                                                                                     | T-001         | ARCH        | S   |       | DONE   |
| T-011  | Inventory and guard Playwright tenant lanes that use host as tenant identity                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A,D | `docs/plans/2026-06-02-arch-m0-11-tenant-host-lane-inventory.md` documents every inventoried project/fixture/server setup using `ks.localhost`/`mk.localhost`/country hosts as tenant handles; `scripts/check-e2e-tenant-host-lanes.mjs` blocks new dashboard/auth specs from using host as tenant identity; existing country-host lanes are tagged alias/regression-only.                                              | —             | TEST, ROUTE | M   |       | DONE   |

**M0 gate:** PR that advances to `negotiation` without authorization, adds a banned string, **removes the protective disclaimer from a key page**, makes `tenant_admin` permission-identical to `super_admin`, adds raw role arrays outside authz helpers, adds new dashboard/auth E2E coverage that relies on country host as tenant identity, or **adds/modifies any file that violates the 150-line modularity guard or Boy Scout rule (T-007b)** → CI red. No migration shipped.

---

## Milestone M1 — Foundation: ida-first host model + additive schema (dual-write; nothing dropped)

| ID     | Task                                                                                                                                                                                             | WS  | Acceptance criterion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Deps                          | Dim                  | Est | Owner | Status |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | -------------------- | --- | ----- | ------ |
| T-108  | Register `ida.interdomestik.com` (`IDA_HOST` + `ida.localhost`) as canonical neutral host with a **real no-tenant public context**                                                               | A   | Change `TenantResolutionResult` to discriminated union `{kind:'tenant',tenantId} \| {kind:'public'}` (today it always requires `tenantId`; `default_public`→`tenant_ks`); `ida.*` resolves to `{kind:'public'}`; **test asserts `ida.*` sets NO tenant cookie** (today `proxy-logic.ts` sets it whenever a tenant resolves). **UI corollary:** `{kind:'public'}` renders a neutral, no-tenant-branding public shell (skeleton fallback); no tenant theme/logo until a session resolves; test asserts zero tenant-branded tokens on `ida.*`                                                                                                                                                                                                                                                               | M0,T-011                      | ARCH, ISO, UX        | L   |       | TODO   |
| T-109  | Reclassify `ks/mk/al/pilot` as aliases: keep resolving for now, set `default_booking_tenant_id` hint, stop new per-country branching                                                             | A   | PR `#1069` / squash merge `b8e29233b9ec2d8c63d3844ef30de49b89381f4f` keeps country hosts working through explicit compatibility-alias metadata and default booking hints, preserves `ida.*` as neutral public/no-tenant, fails closed for unknown and hostile lookalike hosts, and adds `scripts/check-country-host-alias-guard.mjs` to block new host-as-tenant branching outside the compatibility boundary. Local proof passed focused alias/security/front-door/proxy unit coverage, architecture/modularity/e2e-contract guards, `slice:verify`, `pr:verify`, `security:guard`, and `e2e:gate`; remote CI, PR E2E, Pilot Gate, SonarCloud, CodeQL, security checks, Copilot thread resolution, and `pr-finalizer` were green before merge.                                                          | T-108                         | ARCH, ROUTE          | M   |       | DONE   |
| T-110  | Add `host_id` (entry host) as telemetry/data distinct from resolved tenant                                                                                                                       | A   | `host_id` recorded on requests/events; not load-bearing for tenant resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | T-108                         | ARCH, OBS            | S   |       | TODO   |
| T-114  | Add explicit `ida.localhost` Playwright/local setup path for tenant/session context                                                                                                              | A,D | Completed through PR `#1072` / squash merge `104899bd46c1c06cf5a8fbbe32061002ad781f31`: canonical dashboard/auth setup now has an explicit `ida.localhost` Playwright/session lane with scope-specific auth state; `ks.localhost`/`mk.localhost`/country-host lanes remain alias/regression coverage; member/admin/agent/staff smoke proof passes without host-as-tenant shortcuts; reviewer comments were fixed, SonarCloud passed with `0` new issues / `0` security hotspots, and remote CI/PR E2E/Pilot Gate/CodeQL/security/pr-finalizer were green before merge.                                                                                                                                                                                                                                   | T-011,T-108,T-109             | TEST, ROUTE, ISO, UX | M   | codex | DONE   |
| T-101  | Add `claims.incident_country_code` (ISO-3166, nullable) + `incident_jurisdiction`; **wire live writers**                                                                                         | A   | PR `#938` / squash merge `dc5b5ef68281dceaa94294169343ee5e96c7bb5e` added the nullable incident-country fields, database checks, tenant-leading reporting index, schema/migration proof, and live create/submit/draft writer mapping from explicit user/diaspora/domain-assistance inputs without tenant, host, legal-entity, or route inference. Historical backfill remains `T-102`.                                                                                                                                                                                                                                                                                                                                                                                                                   | M0                            | PROD, ARCH           | M   |       | DONE   |
| T-102  | Backfill incident-country from `claim-pack` JSON / diaspora-origin where no live source exists                                                                                                   | A   | Commit `624c6c5af2184dfbc359f3b7a24ef2c93415c1f8` added `packages/domain-claims/src/claims/incident-country-backfill.ts` (idempotent `buildIncidentCountryBackfillPlan`), focused unit tests, export from `domain-claims`, `scripts/backfill-incident-country.ts` dry-run-first operator script with `--apply`, `--tenant`, `--limit` guards, and `scripts/incident-country-backfill-report.ts` JSON coverage reporter. Claim-pack JSON is not persisted per-claim so the durable source is `claim_stage_history.note` diaspora-origin notes; the coverage report reflects this. Next promoted slice: `ARCH-M1-10`, limited to `T-111` governing-law and terms-version foundation.                                                                                                                       | T-101                         | PROD                 | M   |       | DONE   |
| T-103  | Add `claims.case_lifecycle_state` + `recovery_lifecycle_state` (nullable), computed from `status` via `canTransition`                                                                            | B   | PR `#936` added nullable lifecycle-state columns with database checks, deterministic `ClaimStatus` to case/recovery mapping, initialization-path writes, and transition dual-writes while keeping `status` authoritative through M2. New/updated rows carry both; parity tests cover status↔states.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | T-001                         | SM, ARCH             | L   |       | DONE   |
| T-001b | Add `claims.lifecycle_version` (monotonic) + optimistic-lock conflict tests                                                                                                                      | B   | Additive column default 0; `transitionClaimStatus` updates conditional on observed version; concurrent-transition test → exactly one winner, loser gets typed `ClaimTransitionConflictError`; version feeds `domain_events.aggregate_version`. (Note: `update-claim-status.ts` already has a status-CAS — this generalizes it + adds monotonic ordering)                                                                                                                                                                                                                                                                                                                                                                                                                                                 | M0                            | SM, ARCH             | M   |       | DONE   |
| T-104  | Create `domain_events` table as a **transactional outbox** (tenant/actor/role/entity/event_name/version/correlation_id/payload)                                                                  | C   | PR `#924` added the table/index/check migration, Drizzle schema export, migration snapshot, and typed `appendEvent(tx,...)` helper that writes **inside the caller's Postgres transaction**; focused tests prove helper shape validation, caller-owned transaction insert, and database-default `created_at` behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | M0                            | OBS                  | M   |       | DONE   |
| T-104b | Background relay/CDC delivers outbox events to consumers — idempotent + replayable; **per-consumer delivery table**                                                                              | C   | PR `#930` added `domain_event_deliveries` with tenant-isolated RLS, per-event/consumer uniqueness, stable delivery idempotency keys, composite tenant/event FK back to `domain_events`, tenant-scoped relay selection, transaction-safe undelivered locking, replay mode from arbitrary offsets without row locking, idempotent delivery recording, explicit relay counters, and blank-ID rejection before insert/query construction.                                                                                                                                                                                                                                                                                                                                                                    | T-104,T-104c                  | OBS                  | M   |       | DONE   |
| T-104c | Payload **PII allowlist + redaction**; store references not inline PII                                                                                                                           | C   | PR `#928` enforces the current write-family payload guard inside `appendEvent()`: `claim.status_changed@1` is allowlisted with only valid `fromStatus` and `toStatus`, unsupported event names/versions and missing/extra/invalid fields are rejected, and a fresh sanitized plain payload object is inserted so caller prototypes/`toJSON` hooks cannot leak unclassified data.                                                                                                                                                                                                                                                                                                                                                                                                                         | T-104                         | OBS, PROD            | M   |       | DONE   |
| T-104d | **Retention + crypto-shredding** so GDPR erasure works without breaking immutability                                                                                                             | C   | PR `#1001` / squash merge `6e364a58ac4219512f7d8dcc9802eb85b812771a`: `domain_event_keys` tenant-scoped erasure-key foundation, tenant-isolated RLS, Drizzle schema export, `markSubjectErased()` / `isSubjectErased()` helpers, and focused migration/erasure tests. Event rows remain immutable while erased subject keys make PII unavailable to later reference readers. Next promoted slice: `T-104f`, limited to reference-only encrypted PII references for domain events.                                                                                                                                                                                                                                                                                                                        | T-104c                        | PROD, OBS            | M   |       | DONE   |
| T-105  | Emit `case.*` + `recovery.*` + `membership.*` events **in the same txn as the state change**                                                                                                     | C   | Completed through PR `#1044` / squash merge `17dc56e8`: the remaining bounded event-family coverage needed before `T-204` now emits `case.created`, `recovery.success_fee_collected`, and membership domain events through the transactional outbox with focused allowlist and regression proof. Earlier PR `#926` completed the initial `claim.status_changed` subset inside `transitionClaimStatus()`, and same-status no-op paths still write none.                                                                                                                                                                                                                                                                                                                                                   | T-104, T-001                  | OBS, DEBUG           | L   |       | DONE   |
| T-105b | Derive `audit_log` from `domain_events`; **no best-effort audit on aggregate state changes**                                                                                                     | C   | PR `#932` added the first bounded `claim.status_changed@1` audit projection consumer foundation. PR `#934` activated that projection path from claim status transitions, retired duplicate best-effort `claim.status_changed` audit writes from aggregate status paths, preserved sanitized event payload semantics, and proved projection retry/delivery behavior plus claim-stage history metadata preservation.                                                                                                                                                                                                                                                                                                                                                                                       | T-104, T-002c                 | OBS, DEBUG           | M   |       | DONE   |
| T-104f | Reference-only encrypted PII model; **never inline timeline payloads** (resolves open decision #8)                                                                                               | C   | PR `#1015` / squash merge `ec04913b1253db9e7aa4afa94063a8705a670b0f`: `event_pii_references` + `event_pii_keys` tables, separated key material, tenant-scoped FKs/RLS, exported create/read helpers, and focused migration/helper proof. Event PII is reference-only; destroyed keys and erased subjects return `erased_or_unavailable`. Next promoted slice: `T-104g`, limited to bounded per-isolate key caching and destroyed/missing-key negative-path proof.                                                                                                                                                                                                                                                                                                                                        | T-104c                        | PROD, OBS            | M   |       | DONE   |
| T-104g | Bounded per-isolate key cache + destroyed-key negative-path tests                                                                                                                                | C   | PR `#1016` / squash merge `a900a84974cd133835a03264b850c8496f170b0b`: TTL-bounded per-isolate cache; current DB row remains authoritative; destroyed/missing/erased reads evict cache state; focused proof covers stale-cache override, TTL/max-entry bounds, and destroyed/missing-key `erased_or_unavailable` fallback. Next promoted slice: `T-104h`, limited to GDPR erasure render proof across the listed PII residue stores.                                                                                                                                                                                                                                                                                                                                                                      | T-104f                        | OBS                  | S   | codex | DONE   |
| T-104h | GDPR erasure render test — **across all PII stores, not only events**                                                                                                                            | C   | PR `#1017` / squash merge `ac4f7cf39a93eb7916b531358bc17d3a340e72d4`: event rows remain and timeline skeletons render when event PII is unavailable; own raw IP/UA fields in `audit_log.metadata` redact without dropping audit context; erased-subject render contracts are defined for `claim`, `claim_documents`, and `member_notes` while preserving non-PII row shape; ties to T-507 (DSR).                                                                                                                                                                                                                                                                                                                                                                                                         | T-104f                        | PROD, OBS            | M   | codex | DONE   |
| T-106  | Finish deferred backfills: populate `claims.claim_number`, `tenants.code`; enable unique indexes                                                                                                 | A   | PR `#999` / squash merge `b11ae6d3ecc489e3a39b45a76c1e0565cb7c3ea9`: migration `0078` backfills `tenants.code`; `scripts/backfill-claim-number.ts` handles `claims.claim_number` via `generateClaimNumber()`; schema comment cleanup complete; 9 unit tests pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —                             | ARCH                 | M   |       | DONE   |
| T-111  | Add `governing_law` + `terms_version` to `tenants`/future `legal_entities` (each registered company carries its law + contract version)                                                          | A   | PR `#995` / squash merge `fa0e930f3011533952029c8789f82964cc4dc037`: nullable `governing_law` (text, ISO 2-letter, check constraint `^[A-Z]{2}$` in both Drizzle schema and migration, backfilled via `UPPER(country_code)`) and `terms_version` (text, nullable) added to `tenants` via additive Drizzle migration `0076`; check constraint declared in `pgTable` definition matching the pattern in `claims.ts`; 3 focused unit tests cover schema export, column ordering, and migration content; ADR-10                                                                                                                                                                                                                                                                                              | T-101                         | ARCH, PROD           | S   |       | DONE   |
| T-112  | Capture entity-of-record on the **`subscriptions`** row (no `membership` table exists): `legal_tenant_id` + `billing_entity` + `governing_law_snapshot` + `terms_version_accepted` at acceptance | A   | Foundation in `ARCH-M1-11`: nullable `legal_tenant_id` (FK to tenants), `billing_entity`, `governing_law_snapshot` (ISO 2-letter check constraint), and `terms_version_accepted` added to `subscriptions` via additive Drizzle migration `0077`; `subscriptions` schema extracted to `packages/database/src/schema/subscriptions.ts`; `membershipPlans` extracted to `membership-plans.ts`; `memberships.ts` re-exports both for backward compatibility; `legal_tenant_id` backfilled from `tenant_id`, `governing_law_snapshot` backfilled from `tenants.governing_law`; 3 focused unit tests cover schema export, column ordering, and migration content; `subscriptions` confirmed as M1 system-of-record (no new memberships aggregate in this milestone); live-writer wiring at acceptance deferred | T-111                         | ARCH, PROD           | M   |       | DONE   |
| T-113  | Add `user.residence_country` distinct from tenant/host (today `user` has `tenantId`, no residence) — selects entity-of-record; pulls GDPR/EU consumer law into scope                             | A   | Field exists on `user`; not load-bearing for host; documented as the entity-of-record selector input                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | T-108                         | ARCH, PROD           | S   |       | TODO   |
| T-107  | Author ADR-01 (tenant decomposition) + ADR-03 (event stream) + ADR-06 model half (`ida.*` neutral host) + ADR-10 (entity-of-record)                                                              | A,C | ADRs merged; ADR-06 explicitly says front-door/session-context stabilization precedes dashboard gate design, while full legal/billing/recovery architecture remains phased                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | T-101,T-104,T-108,T-111,T-114 | ARCH                 | S   |       | TODO   |

**M1 gate:** `ida.interdomestik.com` resolves with **no implied tenant** and is the recognized canonical host; canonical dashboard/auth E2E setup can run through `ida.localhost` with explicit tenant/session context; country hosts are flagged aliases with no new per-country logic; new cases carry incident-country + both lifecycle states; **new memberships capture `legal_tenant_id` + governing-law/terms snapshot**; **every status change goes through the single `transitionClaimStatus()` command writing status + `lifecycle_version` + history + one `domain_events` row atomically** (no best-effort audit); event payloads are reference-only/redacted; `claim_number`/`tenant.code` uniqueness enforced; all readers still work off `status`; **the unified-dashboard composition model (T-116/117/118) exists with the accident kind registered, ≤2 projection queries per dashboard view, and zero country-host identity in dashboard IA**; **the live login is unchanged.**

---

## Milestone M2 — Case / Recovery authoritative

| ID     | Task                                                                                                                                                                                                                                    | WS  | Acceptance criterion                                                                                                                                                                                                                                                                                                                                        | Deps                | Dim                  | Est | Owner | Status |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | -------------------- | --- | ----- | ------ |
| T-201  | Re-cut `domain-claims` → `domain-case` + `domain-recovery` (package boundary; keep `claims` table)                                                                                                                                      | B   | Completed through PR `#1047` / merge commit `ccd80a4dcaf77b6bf08b90cb6b0d8760dd276034`: `domain-case` and `domain-recovery` expose the remaining public contracts needed before `T-204`, `domain-claims` keeps compatibility facades, the physical `claims` table remains in place, and the case/recovery boundary guard proves no forbidden cross-imports. | T-103               | BND, ARCH            | L   |       | DONE   |
| T-202  | Make `case_lifecycle_state`/`recovery_lifecycle_state` the authoritative read fields                                                                                                                                                    | B   | UI + queries read new fields; `status` becomes derived/compat; parity test                                                                                                                                                                                                                                                                                  | T-103, T-201        | SM, ARCH             | L   |       | TODO   |
| T-203  | Enforce recovery invariants structurally on all write paths                                                                                                                                                                             | B   | Tests: no `authorized` without signed agreement@authorized; `in_court` needs `legal_action_cap_percentage`; `resolved` needs success-fee row or documented `no_fee`                                                                                                                                                                                         | T-201               | SM                   | L   |       | TODO   |
| T-203b | Lock or version mutable recovery **prerequisite** rows inside the transition tx                                                                                                                                                         | B   | When a recovery invariant depends on mutable agreement/payment/assignment state, the prereq rows are `SELECT … FOR UPDATE` (or version-checked) **within** `transitionClaimStatus`'s tx; concurrent test: two transitions reading the same prereq cannot both proceed (today staff path reads prereqs unlocked)                                             | T-203, T-002        | SM                   | M   |       | TODO   |
| T-204  | Bridge billing↔claims success-fee via `recovery.success_fee_collected@1` event                                                                                                                                                          | B,C | Completed through PR `#1049` / squash merge `4d8bed7fe9600829f63a0f1b29b1cd835485aa84`: billing consumes `recovery.success_fee_collected@1` through a replay-safe consumer, collects through Paddle, records delivery only after successful billing action, and avoids direct claims-to-billing coupling.                                                   | T-105, T-201        | COUP, OBS            | M   | codex | DONE   |
| T-205  | State-machine transition test suites (case + recovery), one file per machine                                                                                                                                                            | B   | Every edge/invariant in audit §9 covered; rejection reasons asserted                                                                                                                                                                                                                                                                                        | T-203               | TEST, SM             | M   |       | TODO   |
| T-205b | Law-pack loader performance guard (low priority for pilot)                                                                                                                                                                              | B   | Test asserts one pack load per country per warm isolate; no all-country JSON parse on a transition/assistance check                                                                                                                                                                                                                                         | SVC-CORE-b          | TEST                 | S   |       | TODO   |
| T-206  | Member-facing case timeline reads from `domain_events` (one query)                                                                                                                                                                      | C   | "Everything that happened to claim X" is a single query; snapshot test. **Presentation:** progressive/streamed render (RSC streaming or incremental hydration) with skeleton fallback; no client per-event fetch waterfall; meets perf budget (OD#17)                                                                                                       | T-105               | OBS, DEBUG, UX, PERF | M   |       | TODO   |
| T-207  | Author ADR-02 (case vs recovery split)                                                                                                                                                                                                  | B   | ADR merged; documents `claims`-row migration path                                                                                                                                                                                                                                                                                                           | T-201               | ARCH                 | S   |       | TODO   |
| T-208  | Derive explicit `recovery_law` from `incident_country_code` (Rome II) and introduce `recovery_legal_tenant_id`, distinct from membership `legal_tenant_id`, `governing_law`, and `access_tenant_id`; **define out-of-network fallback** | B   | Completed through PR `#1031` / squash merge `a866b1c3`: recovery-law routing, recovery legal-tenant selection, typed unsupported-jurisdiction outcomes, and existing-incident-country backfill coverage are in place without membership-law, booking-tenant, host, access-tenant, or ambient fallback.                                                      | T-101, T-201        | ARCH, PROD           | M   |       | DONE   |
| T-208b | Named test: unsupported incident country **cannot** fall back to membership governing law                                                                                                                                               | B   | Completed through PR `#1035` / squash merge `247420eb9fb460391aff9ad2e4f1bfe601359d5c`: unsupported `incident_country_code` returns typed `no_network_or_unsupported_jurisdiction` and never resolves recovery law/entity from membership governing law, membership legal tenant, booking tenant, host tenant, access tenant, or ambient fallback context.  | T-208, SVC-CORE-b   | TEST                 | S   |       | DONE   |
| T-209  | Home-entity → incident-location handoff as a first-class audited flow (`recovery.handed_off_to_jurisdiction`) that creates case-scoped access, not a tenant switch                                                                      | B,C | Triage-to-recovery in a different country emits an audited event, sets/records `recovery_legal_tenant_id`, and issues a case-scoped access grant for the local recovery/legal actor; the grant exposes only that case and approved documents, never the full member profile or membership entity-of-record; test                                            | T-105, T-201,T-302b | ARCH, OBS, LP        | M   |       | TODO   |

**M2 gate:** every recovery transition rejected unless its invariant holds, on all paths; recovery resolves applicable law from `incident_country` and recovery entity from `recovery_legal_tenant_id` (both distinct from membership `governing_law`/`legal_tenant_id`); home→incident handoff is audited, grants only case-scoped access, and leaves the membership entity-of-record intact; success-fee triggered by event; timeline is one query.

---

## Milestone M3 — Single tenant context; host ≠ tenant; attribution read-only

| ID     | Task                                                                                                                                                                                                         | WS  | Acceptance criterion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Deps          | Dim        | Est | Owner | Status |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ---------- | --- | ----- | ------ |
| T-301  | De-collapse roles and governance boundaries: distinct `admin` / `tenant_admin` / `super_admin`, **+ `global_support`/`auditor`**, with SoD and audited break-glass expectations                              | D   | Completed through PR `#1056` / squash merge `6d222fc8b51868e507efee99ec47b222c913dd61`: role/capability boundaries separate `admin`, `tenant_admin`, technical `super_admin`, `global_support`, and `auditor`; governance approval excludes technical super admins and self-approval; break-glass requires reason, future expiry, and `security.break_glass_used` audit metadata; explicit tenant `super_admin` grants remain valid for admin portal semantics; branch-manager access requires tenant and branch context.                                                                                                                      | T-006, T-006b | LP         | M   | codex | DONE   |
| T-302  | Implement `resolveTenantContext(request, session)` returning the **four-context** discriminated union (`host_id`, `booking_tenant_id`, `access_tenant_id`, `legal_tenant_id`); sole producer of tenant truth | A,D | Completed through PR `#1075` / squash merge `927349df686e5497d7a8bad5aacf10ad0669f66a`: app/server context now has a focused typed resolver foundation for `host_id`, `booking_tenant_id`, `access_tenant_id`, and `legal_tenant_id`; the implementation reuses existing request-source precedence and `hasHostSessionTenantMismatch`; proof covers host/session parity, trusted forwarded hosts, neutral/default host behavior, stale-cookie conflict, missing session tenant handling, and legal-tenant derivation.                                                                                                                          | M1,T-114      | ISO, SOC   | L   | codex | DONE   |
| T-302b | Re-point isolation/RLS to `access_tenant_id` (not a generic `tenantId`); keep `legal_tenant_id` and `recovery_legal_tenant_id` separate; add case-scoped grant semantics                                     | A,D | Completed through PR `#1085` / squash merge `b9c8eba408b741fc089935915958ca90866f5c4d`: shared-auth exposes explicit access-tenant resolution and case-scoped document grants; portal/admin/upload/document access paths use access-tenant semantics; staff/branch visibility is tightened; focused proof covers fallback, isolation, grant, and visibility behavior. Broad RLS policy migration remains with `T-302c`/`T-302d`; `apps/web/src/proxy.ts`, canonical routes, product UI, billing, README, AGENTS, and broad architecture docs were preserved.                                                                                   | T-302, T-301  | ISO, LP    | L   | codex | DONE   |
| T-302c | **Extend** `withTenantContext` (do not fork) to set `app.current_access_tenant_id`; add DB import-boundary lint; deprecate column-agnostic `withTenant`                                                      | A,D | Keep existing `assertRlsConnectionRoleReady` + `set local role` + `row_security=on` steps; add `withAccessTenant(ctx, table)` that accepts **only** an `accessTenantId` column (kills the wrong-column footgun); enforce via the **existing `db-access-guard` annotation** + ban raw `dbAdmin`/`dbRls` outside approved adapters; `db.update(claims)` forbidden outside the transition command                                                                                                                                                                                                                                                 | T-302b        | ISO, SOC   | L   |       | TODO   |
| T-302d | **Migrate existing RLS policies** to `app.current_access_tenant_id` (transitional `coalesce(access_tenant_id, tenant_id)`); add RLS to `domain_events`                                                       | A,D | `DROP`/replace policies from `0031`/`0035`; during dual-write the wrapper sets **both** `app.current_tenant_id` and `app.current_access_tenant_id` so un-migrated policies don't fail closed; new `domain_events` table gets an access-tenant RLS policy; extend `rls-engaged.test.ts` to assert isolation holds under the access setting. **Highest blast-radius M3 change — gate behind that test**                                                                                                                                                                                                                                          | T-302c        | ISO        | L   |       | TODO   |
| T-303  | Host=default-only; session-tenant wins over cookie/host/query                                                                                                                                                | A,D | Completed through PR `#1078` / squash merge `972e40649f7aee8622d3e28639fd458e81a1222b`: neutral/default hosts now resolve valid authenticated session tenant context as authoritative (`source: session`, session `booking_tenant_id`/`access_tenant_id`) over stale cookie/host/query hints, while explicit host/session mismatch protection still returns `tenant_mismatch`; focused proof covers default/neutral host behavior, session precedence, mismatch handling, invalid session tenant edge cases, and existing canonical route gates.                                                                                               | T-302         | ISO        | M   | codex | DONE   |
| T-304  | Introduce `actor_role_on_session` distinct from `user.role`                                                                                                                                                  | D   | Completed through PR `#1060` / squash merge `c53f4d37716755924a4c2a3a954689a468224771`: scope is conferred by the exercised role on the member session, `/member` uses member scope for agent/member/user sessions, persisted `user.role` does not grant agent-only member-dashboard bypasses there, and an ex-agent entering `/member` receives member navigation scope only.                                                                                                                                                                                                                                                                 | T-301         | LP, ROUTE  | M   | codex | DONE   |
| T-305  | Add `host_id` + `booking_tenant_id` + `access_tenant_id` as concepts distinct from `legal_tenant_id`                                                                                                         | A   | Completed through PR `#1080` / squash merge `b3fc99e055c47313478e99be8e875b4c413aeabb`: `resolveTenantContext` now returns distinct `host_id`, `booking_tenant_id`, `access_tenant_id`, and `legal_tenant_id` concepts; session-derived access/booking/legal concepts are normalized behind a focused helper with legacy `tenantId` compatibility; access/session mismatch protection remains tied to `access_tenant_id`, and booking/legal-only session data does not grant access tenancy. Focused proof covered separated concepts, neutral/default host behavior, legacy fallback, no-access edge cases, and existing resolver boundaries. | T-302         | ARCH       | M   | codex | DONE   |
| T-305b | Add/backfill `access_tenant_id` on **every tenant-scoped table that can diverge** from legal entity                                                                                                          | A   | Enumerate divergent tables (`claim`, `claim_documents`, recovery/agreement tables, `domain_events`, flight tables) vs never-divergent; backfill `access_tenant_id = tenant_id`; each gets the access-tenant RLS policy (T-302d)                                                                                                                                                                                                                                                                                                                                                                                                                | T-305, T-302d | ARCH, ISO  | M   |       | TODO   |
| T-306  | Consolidate `domain-referrals` + `ownership-attribution` into read-only attribution surface                                                                                                                  | A,D | Completed through PR `#1063` / squash merge `5037276aa9edb2c4efd0b2566c507bf0e4262249`: attribution is durable and read-only, read-scope bindings are revoked rather than granted, attribution cannot confer claims/recovery/document access, and commission ownership remains payable from durable subscription ownership.                                                                                                                                                                                                                                                                                                                    | T-304         | COUP, LP   | M   | codex | DONE   |
| T-307  | Split `proxy-logic.ts` into resolve / secure(CSP,HSTS) / gate modules (behind M0 baseline)                                                                                                                   | D   | Three files; E2E baseline (`.e2e_baseline_count`) unchanged; gated change approved                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | T-302         | SOC, ROUTE | L   |       | TODO   |
| T-308  | Author ADR-05 (attribution read-only) + finalize ADR-09 (roles)                                                                                                                                              | D   | Completed by `docs/architecture/adr-05-attribution-read-only.md` and `docs/architecture/adr-09-role-separation-governance-boundaries.md`: attribution is read-only commercial provenance; role boundaries distinguish technical, governance, support, audit, and scoped operational authority. No runtime routing, auth, tenancy, billing, schema, RLS, proxy, broad M3, or product work was started.                                                                                                                                                                                                                                          | T-301,T-306   | ARCH       | S   | codex | DONE   |

**M3 gate:** `resolveTenantContext()` returns all four contexts; isolation/RLS keys off `access_tenant_id` (test-proven, never the legal/billing value); stale cookie cannot override session; ex-agent cannot gain agent scope; attribution flows to reports with zero read scope; host-vs-session test green; `ida.localhost` remains the canonical dashboard/auth lane.

---

## Milestone M4 — Product model made structural

| ID     | Task                                                                                                                                                                                                                          | WS  | Acceptance criterion                                                                                                                                                                                                                                                                                                                                                                                                                  | Deps              | Dim        | Est | Owner | Status |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ---------- | --- | ----- | ------ |
| T-401  | Fix `grace_period` card lockout; lift guard to `<MembershipCard subscription={...}/>` with required prop                                                                                                                      | E   | grace_period member sees card; component refuses to render without active\|grace subscription; test both                                                                                                                                                                                                                                                                                                                              | M0                | PROD       | M   | codex | DONE   |
| T-402  | Type the hierarchy: `membership_offer` (price/interval/tier) vs `membership_proof` (card) vs `membership_workspace`                                                                                                           | E   | No code path attaches a price to proof/workspace; type-level guarantee + test                                                                                                                                                                                                                                                                                                                                                         | T-401             | PROD, BND  | L   |       | TODO   |
| T-403  | Introduce `AICallContext` in `domain-privacy/ai.ts` (purpose/retention/posture/consent)                                                                                                                                       | E   | Type defined; covers invalidity posture + general case                                                                                                                                                                                                                                                                                                                                                                                | —                 | PROD       | M   |       | TODO   |
| T-404  | Make `AICallContext` non-optional on every `domain-ai` entry point; `client.ts` rejects calls without it                                                                                                                      | E   | Dropping context = type error + runtime rejection; test proves both                                                                                                                                                                                                                                                                                                                                                                   | T-403             | PROD, TEST | M   |       | TODO   |
| T-403b | **Brand-mint `AICallContext` (Rev 22, ADR-22):** unexported `unique symbol` brand, minted only by `declareAiPosture()` in `domain-privacy` after runtime consent validation                                                   | E   | Forging the context outside `domain-privacy` is a compile error (`@ts-expect-error` fixture); `declareAiPosture()` returns a typed `ConsentMissingError` when consent is absent; medical purposes require `ConsentEvidence<'medical'>` (other consent evidence does not unify); every `domain-ai` call emits `ai.call_executed {purpose, rule_version}` to the outbox, feeding the ENT-TM06 (AI run read/review) threat-model surface | T-403, T-404      | PROD, TEST | S   |       | TODO   |
| T-405  | Codemod existing `domain-ai` call sites to pass context                                                                                                                                                                       | E   | All call sites compile; no implicit default posture                                                                                                                                                                                                                                                                                                                                                                                   | T-404             | PROD       | M   |       | TODO   |
| T-406  | Author ADR-07 (card derived view) + ADR-08 (AI posture required) + ADR-11 (disclosure/tax binding)                                                                                                                            | E   | ADRs merged                                                                                                                                                                                                                                                                                                                                                                                                                           | T-401,T-404,T-407 | ARCH       | S   |       | TODO   |
| T-407  | Entity disclosure: show contracting company + governing law at signup, on membership, on every invoice                                                                                                                        | E   | All three surfaces render from `legal_tenant_id`+`governing_law`; missing disclosure fails a test on those surfaces                                                                                                                                                                                                                                                                                                                   | T-112             | PROD, TEST | M   |       | TODO   |
| T-408  | Bind Paddle membership invoicing/tax/currency to the **subscription's snapshot** (`legal_tenant_id`/`billing_entity`/`terms_version`) and bind recovery success-fee invoicing to `recovery_legal_tenant_id`, not host/session | E   | Completed through PR `#1051` / squash merge `037fa75099a0f9e33e84605613eb8d65c5f991d8`: Paddle membership invoicing and webhook reconciliation bind to stored subscription snapshots, recovery success-fee invoicing binds to `recovery_legal_tenant_id`, cross-entity stored-payment cases avoid charging the wrong Paddle account, and unsupported explicit billing-entity metadata fails fast.                                     | T-112,T-208,T-204 | PROD, COUP | L   | codex | DONE   |

**M4 gate:** grace_period card works; no price reachable on the card; a `domain-ai` call without `AICallContext` won't compile and won't run (and the context is unforgeable outside `domain-privacy` — T-403b); every signup/membership/invoice shows contracting entity + governing law; membership invoices bind to subscription `legal_tenant_id`; recovery success-fee invoices bind to `recovery_legal_tenant_id`; the VONESA 5-step stepper renders as a pure lifecycle projection with Kërkesa locked until the FLIGHT-05/05b witnesses exist (FLIGHT-09b).

---

## Milestone M5 — Live cutover (flip the switch; host model already shipped at M1, gated on M2+M3)

| ID    | Task                                                                                                                    | WS  | Acceptance criterion                                                                                                                                                                                                                                                                                                                                      | Deps                | Dim         | Est | Owner | Status |
| ----- | ----------------------------------------------------------------------------------------------------------------------- | --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ----------- | --- | ----- | ------ |
| T-501 | Flip `ida.*` to the **sole** live login behind a flag; country hosts become 301 redirects to `ida.*`                    | A,D | Flag on → `ida.*` is the only login; `ks/mk/al/pilot` 301 to `ida.*` carrying `default_booking_tenant_id`                                                                                                                                                                                                                                                 | T-108, T-302, T-202 | ARCH, ROUTE | M   |       | TODO   |
| T-502 | Consolidate to 4 layout groups; retire `legacy/`, dedupe 3 `agent` dirs; remove per-country host branching              | D   | Exactly 4 portal layouts; `legacy/` routes + `legacy/agent` + `(dashboard)/agent` **actively deleted** (not just unrouted); duplicate client layout components collapsed to the 4 shells with **zero orphaned client layout** (static check); one protected-route gate; no per-country host branch left; consolidated-layout E2E smoke on `ida.localhost` | T-307, T-109        | ROUTE, UX   | M   |       | TODO   |
| T-503 | Drop legacy `status` column after ≥1 release of dual-write                                                              | B   | Readers use lifecycle states only; migration drops `status`; release-cycle evidence attached                                                                                                                                                                                                                                                              | T-202               | SM, ARCH    | M   |       | TODO   |
| T-504 | Split `tenants` → `legal_entities` / `marketing_hosts` / `default_booking_links`                                        | A   | Mechanical migration; referential integrity preserved                                                                                                                                                                                                                                                                                                     | T-305               | ARCH        | L   |       | TODO   |
| T-505 | Finalize ADR-06 (live cutover + cookie/session precedence)                                                              | A   | ADR completed (model half authored at M1 in T-107)                                                                                                                                                                                                                                                                                                        | T-501               | ARCH        | S   |       | TODO   |
| T-506 | Entity-migration capability with **active-case guard**: move members from home/hub entity to a new local `legal_entity` | A   | Re-issues terms under new `governing_law`, re-captures acceptance, preserves history, emits `membership.entity_migrated`; **blocks while `recovery_lifecycle_state` is non-terminal** (or applies documented legacy-entity run-off); dry-run + rollback tested; ADR-12                                                                                    | T-504, T-112, T-507 | ARCH, PROD  | L   |       | TODO   |
| T-507 | Residence-change flow + DSR: user updates `residence_country` → defined trigger for terms re-acceptance / migration     | A   | ADR-12 documents whether change triggers immediate re-acceptance+migration or defers to renewal; ties to DSR; active-recovery members are deferred/run-off, not force-migrated                                                                                                                                                                            | T-113               | PROD, ARCH  | M   |       | TODO   |

**M5 gate (program done):** diaspora member logs in at `ida.*` (the sole live login), booked under any `legal_tenant_id`, incident in any `incident_country_code`; none of the §2 conflations expressible; entity-migration proven on a dry-run; 8-status enum, `legacy/` routes, and per-country host branching gone; country subdomains 301 to `ida.*`.

---

## Workstream F — Passenger Rights / VONESA (Flight Delay, EC261)

Rides the spine behind a feature flag; **does not gate Design Gate or `ida.*` Go-Live**. Detail: `vonesa-architecture-integration-2026-05-30.md`. Milestone shown per task.

| ID            | M     | Task                                                                                                                       | Acceptance criterion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Deps                           |
| ------------- | ----- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------ |
| FLIGHT-00     | M0    | Program spec + legal-text gate; feed brand-lint catalog                                                                    | Domain/flow/legal spec approved; VONESA banned phrases + protective message added to T-007; **runtime build gated on counsel sign-off** (open decision #6)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —                              |
| FLIGHT-SCHEMA | M1    | Additive tables + first `flight.*` events                                                                                  | `flight_claims, flight_eligibility_checks, flight_documents, claim_assignments, poa_documents, fee_agreements, airline_submissions, compensation_ledger, direct_payment_events, airline_contacts`; each carries `tenant_context_id/legal_tenant_id/access_tenant_id`, with recovery agreement/ledger rows carrying `recovery_legal_tenant_id`; `flight.*` emitted via outbox                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | T-104, FLIGHT-00               |
| FLIGHT-01     | M2    | EC261 rule engine (pure, versioned) → `AssistanceOutcome`                                                                  | delay/cancel/denied-boarding + 250/400/600 tiers unit-tested; `rule_version` stamped; out-of-scope→`not_eligible`; insufficient data→`manual_review_required`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | FLIGHT-SCHEMA                  |
| FLIGHT-02     | M2    | `FlightDataProviderPort` + adapter + manual-review fallback                                                                | Domain depends only on the port; **API down ⇒ `manual_review_required`, case still creatable**; `api_response_redacted` stored                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | FLIGHT-SCHEMA                  |
| FLIGHT-05     | M2    | Assignment/POA engine + airline-submission invariant                                                                       | Generate+sign assignment (default) & POA (fallback); **`canTransition` rejects `→submitted_to_airline` without signed assignment\|POA + accepted fee + consent**, on every writer (T-002b)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | FLIGHT-SCHEMA, T-001, T-201    |
| FLIGHT-05b    | M2    | **Phantom-typed authorization witnesses + no-win-no-fee constructor constraint (Rev 22, ADR-22)**                          | `Signed<Assignment\|PoA>` / `Accepted<FeeAgreement\|PrivacyConsent>` brands minted **only** by the SMS-OTP verification module (recording `ip_hash/device_id/document_hash`); `submitToAirline` signature requires `(Signed<Assignment> \| Signed<PoA>) & Accepted<FeeAgreement> & Accepted<PrivacyConsent>`; `issueSuccessFeeInvoice(evidence: CompensationEvidence, agreement: Accepted<FeeAgreement>)` where `CompensationEvidence` is a **closed union** (`airline_paid_via_ida` \| `direct_payment_declared`) with **no variant for an unresolved claim** — "invoice before win" is unrepresentable; EC261 tier amounts typed `250\|400\|600`; `@ts-expect-error` compile-fail fixtures prove submission-without-witness and invoice-without-evidence both fail to type-check, alongside the T-002b runtime invariant                                                                                   | FLIGHT-05, T-002d              |
| FLIGHT-06     | M2    | Airline submission + `airline_contacts` registry                                                                           | Submission only after invariant; registry staff-maintained/versioned (not hardcoded); submission emits event                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | FLIGHT-05                      |
| FLIGHT-07     | M2    | Recovery workflow: ADR/escalation/legal-partner handoff (reuse `domain-recovery`)                                          | rejected/no-response→escalation queue; legal handoff uses case-specific isolated access                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | FLIGHT-06, SVC-09, SVC-10      |
| FLIGHT-04     | M3    | Documents + OCR + isolation                                                                                                | Flight docs in shared Vault, isolated by `access_tenant_id`; agents/promoters cannot see ID/bank docs; OCR only with `AICallContext`; access logged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | T-302b, T-404                  |
| FLIGHT-03     | M4    | Intake multi-step (free check)                                                                                             | Visitor free eligibility check; member case creation; no-guarantee copy; SQ/MK/EN                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | FLIGHT-01, FLIGHT-02           |
| FLIGHT-09     | M4    | Member dashboard card + Smart Next Step + **SMS-OTP signing**                                                              | "Fluturim i vonuar" card; next-step states; assignment/POA signed via OTP with `ip_hash/device_id/document_hash`; **card/next-step UI comes from the shared T-411 library**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | FLIGHT-03, FLIGHT-05           |
| FLIGHT-09b    | M4    | **VONESA 5-step journey stepper (Rev 22): Kontrollo → Apliko → Kërkesa → Ndjekja → Pagesa as a pure lifecycle projection** | Stepper derives **exclusively** from `case_lifecycle_state` + `recovery_lifecycle_state` + ledger/fee-invoice status — the UI owns zero state and can express zero illegal ones; **Kërkesa renders locked** with explicit unlock conditions until assignment\|POA signed + fee accepted + consent, displaying `canTransition` rejection reasons verbatim (never re-implementing the rule); Ndjekja renders `flight.*` events through the shared T-210 timeline registry; Pagesa renders gross→fee(30%)→net from the pure ledger math + entity disclosure (T-407); the direct-payment branch keeps the case **visually open** while `fee_invoice_status ∈ {pending, overdue}`; built from `ui/crystal` Stepper + the T-411 library; SQ/MK/EN; protective disclaimer present; a standard accident claim renders the same Stepper/Timeline primitives with its own phase mapping (one visual grammar, no silos) | FLIGHT-09, T-118, T-116, T-210 |
| FLIGHT-10     | M4    | Staff/admin queue                                                                                                          | Queues (new/eligibility/manual-review/docs-missing/assignment-pending/ready/overdue/escalation/payment-expected/direct-payment-fee); per-case view                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | FLIGHT-06                      |
| FLIGHT-08     | M5    | Compensation ledger + Direct Payment Fallback + payout + Paddle snapshot                                                   | gross→fee→net math (pure, tested); ledger bridged to billing by `recovery.compensation_received` event; membership invoice bound to subscription `legal_tenant_id`; recovery fee/invoice bound to `recovery_legal_tenant_id`; **direct-payment branch: declare→fee invoice→reminders→no clean close while fee open**; contractual fee-still-due rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | T-408, T-204, FLIGHT-07        |
| FLIGHT-11     | M2→M5 | Tests / compliance / brand-lint (full matrix)                                                                              | unit/integration/e2e/security/i18n per VONESA §23; tenant-isolation tests; no-guarantee lint green                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | spans                          |

---

## Workstream G — Assistance Services Catalog (the 10 renewed services)

Rules-first `domain-assistance` modules → `AssistanceOutcome`, wired to `domain-case` (free/member) and `domain-recovery` (professional). Behind feature flags; **does not gate Design Gate or Go-Live**. Detail in `services_technical_alignment_pergjigje.md`.

| ID         | M               | Task                                                                                                        | Acceptance criterion                                                                                                                                                                                                                                                                             | Deps                   |
| ---------- | --------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------- | ---------- | --- | --- | ---- |
| SVC-00     | M0              | ASSIST-00 spec + disclaimer/consent model; feed brand-lint catalog                                          | 6-domain contracts defined; versioned disclaimer model; **green light = Spec/Design Gate only** until consent/PII/authorization models close                                                                                                                                                     | —                      |
| SVC-CORE   | M1→M2           | Versioned rule-pack registry + `AssistanceOutcome` contract + `consent_events` + `document_classification`  | Packs carry source/date/owner + version; outcome typed (reasons/evidence/disclaimers/`human_review_required`); consent model usable by all services                                                                                                                                              | SVC-00, T-104          |
| SVC-CORE-b | M1→M2           | Law-pack registry: per-isolate promise cache + Zod schema validation; load **only** the needed country/pack | Static loader map per supported country; `loadLawPack(country)` cached per warm isolate + schema-validated; unsupported country throws typed error; **no loader defaults to `legal_tenant_id`/`booking_tenant_id`/host** (rules stay pure-over-injected — this is the loader layer feeding them) | SVC-CORE               | ARCH, PROD | M   |     | TODO |
| SVC-06     | M2 (Priority-1) | **Help Now** incident-guide (EAS vs police), **no login / no PII**                                          | Country incident rules pack MK/KS/AL; hard-stops (injury/immovable/wheel/steering/brake/disagreement/foreign-plate) → `POLICE_REQUIRED`/`HUMAN_REVIEW`; **never suggests EAS when police required**; `rule_source`+`last_reviewed` required                                                      | SVC-CORE               |
| SVC-01     | M2              | Legal-basis pre-check                                                                                       | Rule Matrix per country/insurance-class/damage-type; orientation-only outputs; hard-stops; never a final opinion                                                                                                                                                                                 | SVC-CORE               |
| SVC-02     | M2              | Procedure guide                                                                                             | Procedure JSON packs MK/KS/AL (steps/docs/deadlines/escalation triggers); no insurer contact without authorization                                                                                                                                                                               | SVC-CORE               |
| SVC-03     | M2→M4           | Injury categorization (medical consent)                                                                     | **Explicit medical-data consent**; AI extraction only with consent; hard-stops (death/permanent disability/minor/conflicting records)→human review; orientation only                                                                                                                             | SVC-CORE, T-404        |
| SVC-04     | M2              | Vehicle-damage alternative check + **valuation-delta** + transition invariant                               | 3 phases; compare fields (parts/labor/paint/VAT/depreciation/hidden); human review for total-loss/airbag/structural; **`→negotiation` blocked without valuation-delta + signed service consent** (T-002b)                                                                                        | SVC-CORE, T-001, T-201 |
| SVC-05     | M2              | Invalidity coefficient (member-only, human-reviewed)                                                        | Invalidity tables per insurer/version; AI outputs candidate items + missing docs only; **`final_coefficient_by_ai = false`**; hard-stops (no policy/no table/treatment incomplete)                                                                                                               | SVC-CORE, SVC-07       |
| SVC-07     | M2              | Expert network (`domain-recovery/expert-network`)                                                           | Expert registry (license/country/DPA); **cost-approval + member-approval required**; case-linked; activation audited                                                                                                                                                                             | T-201                  |
| SVC-09     | M2              | Court escalation (`domain-recovery/legal-escalation`)                                                       | Court invariants (official rejection / no-response after deadline / low offer with evidence) + cost approval + member approval; full audit event                                                                                                                                                 | T-201, T-203           |
| SVC-10     | M2              | Legal representation handoff (`domain-recovery/legal-handoff`)                                              | Partner-lawyer agreement; **case-specific isolated document access**; confidentiality; member approval + POA                                                                                                                                                                                     | T-201, T-302b          |
| SVC-08     | M5              | Member discounts (Discount Rules Matrix)                                                                    | Standard vs member success-fee transparent **before** signing; applied post-agreement; membership discount eligibility reads subscription `legal_tenant_id`, while success-fee billing binds to `recovery_legal_tenant_id`                                                                       | T-408                  |
| SVC-DASH   | M4              | Dashboard catalog cards + Smart Next Step + OTP-signed consents                                             | All 10 service cards + VONESA card; next-step messaging; OTP-signed assignment/POA/**medical consent**; SQ/MK/EN; protective disclaimer present; **presentation comes from the shared T-411 library**                                                                                            | T-401, SVC-CORE, T-411 |

---

## Workstream H — CQRS Read Models (Finance / Reporting; Rev 22, DOM overlay)

Outbox-only consumers of `domain_events` — **cross-domain database joins are forbidden**; `domain-finance`/`domain-reporting` never read `claims`, `recovery`, `subscriptions`, or any core table (program §2c, ADR-21). Behind feature flags; consumer skeletons may land with M1 plumbing, projection builds stay gated behind DOM promotion per `current-program.md` (OD#18). **Gates nothing** on the critical path, Design Gate, or Go-Live. Milestone shown per task.

| ID      | M   | Task                                                                        | Acceptance criterion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Deps                               |
| ------- | --- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| CQRS-00 | M1  | Register `finance` + `reporting` as named relay consumers (no-op skeletons) | Per-consumer rows in `domain_event_deliveries` with independent cursors (one slow consumer never blocks the other); delivery idempotency keys reuse the existing pattern; replay-from-offset-zero proven against empty projections; consumers **pin `event_name@event_version`** and park unknown versions for review (typed `unknown_event_version`, never guess); poison events park with a delivery error + alert, never block the queue                                                                                                                                                                                                                                                     | T-104b                             |
| CQRS-01 | M3  | DB import-boundary enforcement for the two read-model packages              | The T-302c import-boundary lint extended: `domain-finance`/`domain-reporting` cannot import core Drizzle schemas (`claims`, `recovery`/agreements, `subscriptions`) or raw `dbAdmin`/`dbRls`; their DB roles carry **zero grants** on core tables (RLS-engaged test proof); packages depend only on the event-relay API + their own projection tables                                                                                                                                                                                                                                                                                                                                           | T-302c, CQRS-00                    |
| CQRS-02 | M5  | `fee_receivables` projection — VONESA 30% success-fee tracking              | Projects `recovery.compensation_received` → receivable (fee from the **pure passenger-rights ledger math**: 30% over the typed `250\|400\|600` EC261 tiers, unit-tested), `recovery.fee_calculated` → fee/legal-fee/bank-cost split, `flight.direct_payment_declared` → direct-payment receivable + dunning schedule, `billing.fee_invoice_settled\|disputed\|written_off` → closure; upserts keyed `(entity_id, aggregate_version)` so redelivery is a no-op; **rebuild-from-zero diff against the live projection is empty**; reconciliation against Paddle webhook-reconciled state alerts on mismatch (never silent drift); event payloads reference-only (no passenger PII enters Finance) | CQRS-00, CQRS-01, FLIGHT-08, T-204 |
| CQRS-03 | M5  | Reporting funnel/marketing projections                                      | Consumes `assistance.eligibility_checked` (`rule_version`, outcome class), `case.opened`, `flight.*` lifecycle, and envelope attribution facts (`booking_tenant_id`/`host_id`); materialized views for Kontrollo→Apliko conversion, cases/month vs the 81/month break-even, cost-per-won-case vs launch budget; **zero PII by construction** (inputs allowlisted/redacted at `appendEvent`); attribution flows to reports without conferring access (ADR-05 test); crypto-shred erasure never needs to reach reporting aggregates (proven in the T-104h matrix)                                                                                                                                 | CQRS-00, CQRS-01, FLIGHT-03        |
| CQRS-04 | M5  | Finance issues gated commands to `domain-billing`; **no second ledger**     | Any money movement Finance triggers (commission settlement, fee dunning escalation) is an event-bridged command to `domain-billing`, bound to the `legal_tenant_id`/`recovery_legal_tenant_id` snapshots; test proves `domain-finance` contains no Paddle client and no ledger-write path; ADR-21 authored                                                                                                                                                                                                                                                                                                                                                                                      | CQRS-02, T-408                     |

---

## UI/UX nodes (Rev 18 — woven into M1/M3/M4/M5; no separate workstream; critical path unchanged)

These ride existing milestone gates; none extends the longest chain `T-001 → T-103 → T-201 → T-202 → T-503`. Modularity reuses the existing `T-007b` 150-line gate. Milestone shown per node.

| ID    | M   | Task                                                                     | WS  | Acceptance criterion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Deps                       | Dim             |
| ----- | --- | ------------------------------------------------------------------------ | --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | --------------- |
| T-115 | M1  | Front-door public shell + skeleton baseline                              | D   | `ida.*` `{kind:'public'}` serves a neutral public shell (login/landing) with a shared skeleton fallback; no tenant branding until session resolves; components ≤150 lines (T-007b); E2E smoke on `ida.localhost`; meets public-shell perf budget (OD#17)                                                                                                                                                                                                                                                                                             | T-108, T-114               | ROUTE, UX, PERF |
| T-310 | M3  | Dynamic theme/branding token resolution from session context             | D   | Branding resolved **exclusively via CSS custom properties** (`var(--theme-primary)`) injected by Next.js **Server Components at layout level**; tenant assets (logos) served from a **dynamic CDN URL stored on the tenant row** — **no static per-tenant CSS/asset imports** (bundle size constant as tenants grow); tokens derive from `resolveTenantContext()`, **never host**; neutral on `{kind:'public'}`; switching session tenant re-themes with no host change; **no branding leaks across the `access_tenant_id` boundary** (test); ADR-20 | T-302, T-302b              | ROUTE, ISO, UX  |
| T-410 | M4  | Optimistic UI hook for reversible member actions (`useOptimistic`)       | E   | `useOptimistic` reconciled to the typed `ActionResult`, **rolls back on error**; **restricted to actions that do NOT change the claim-status spine** (governed by `canTransition`); **every status-changing mutation MUST use a pessimistic spinner** — enforced by a lint/convention (`useOptimistic` forbidden in modules importing the transition command / status mutations); money/legal-gated actions (recovery activation, airline submission, settlement/payout) stay pessimistic; test both                                                 | T-401, T-002               | PROD, UX, PERF  |
| T-411 | M4  | Shared Smart Next Step component library (SVC-DASH ⊕ FLIGHT-09 boundary) | E   | One shared presentation library for service-catalog + Smart Next Step cards consumed by **both** SVC-DASH and FLIGHT-09 (no duplication); each component ≤150 lines (T-007b); next-step states **typed/derived** from case/recovery lifecycle + `domain_events` (not hardcoded per surface); skeleton fallback; SQ/MK/EN; protective-disclaimer slot; E2E on `ida.localhost`. T-411 owns the components; SVC-DASH/FLIGHT-09 are thin consumers wiring domain data in                                                                                 | T-401, SVC-CORE, FLIGHT-03 | PROD, BND, UX   |

| T-117 | M1 | Unified member dashboard shell: RSC + parallel-route slots + PPR (Rev 22) | D | `@cases`/`@actions`/`@timeline` parallel routes with independent Suspense boundaries; static chrome prerenders (PPR), member data streams in; `'use client'` only at leaf surfaces; session + `resolveTenantContext()` deduped via `React.cache()` — **exactly one resolution per render tree, test-proven**; dashboard issues **≤2 projection queries per view** (case summaries + T-206 timeline; no per-card fetch fan-out — this is the frontend half of the ENT-PERF p95 budget); protective disclaimer rendered at slot-layout level so no vertical can omit it (T-007 passes structurally); IA assumes `ida.*` + session context, never country hosts (OD#10) | T-115, T-114 | ROUTE, UX, PERF |
| T-118 | M1→M4 | `ui/crystal` design-system primitives — Crystal Home V2 Matte Layered (Rev 22) | E | `MatteAnchorCard`, `RefractiveGlassPanel`, `Stepper`, `Timeline` per `2026-01-28-matte-layered-dashboard-design.md`: pure presentational, **zero domain imports** (lint-enforced layer boundary); each ≤150 lines (T-007b); CSS-only glass with a **bounded count of concurrent `backdrop-filter` layers per viewport**; motion via `LazyMotion`/`m` at leaves only (no motion runtime in the shared bundle); `prefers-reduced-motion` respected; WCAG 2.2 AA contrast on matte surfaces; size-limit budget green (OD#17); shell-first at M1, domain card surfaces land with their verticals M2–M4 | T-115 | UX, PERF, BND |
| T-116 | M1→M2 | `CaseSummary` discriminated union + static case-kind renderer registry (Rev 22) | E | One server projection returns every case as `CaseSummary = AccidentSummary \| GreenCardSummary \| FlightDelaySummary \| …` discriminated on `case_kind`, carrying **presentation-safe fields only** (lifecycle states, next-step token, document counts — never a price on proof, per T-402); `Record<CaseKind, CaseKindDescriptor>` registry is **compile-time exhaustive** (a new `case_kind` without a descriptor fails the build — the UI twin of `canTransition` exhaustiveness); descriptors are RSC leaves ≤150 lines; vertical folders never import each other (import-boundary lint); accident kind registered at M1, green-card/flight kinds as their verticals land | T-103, T-115, T-118 | PROD, BND, UX |
| T-210 | M2 | Timeline event-renderer registry over `domain_events` (Rev 22) | C,E | `Record<event-namespace, EventRow>` renderers for `case.*`/`recovery.*`/`membership.*`/`flight.*`/`assistance.*`, rendering from the **single T-206 query**; an unknown event name/version renders a safe fallback row (never throws, never blocks the timeline); PII is never rendered from payloads (reference-only contract upheld — redacted fallback when a key is shredded, per T-104f/g); snapshot tests per namespace; one visual grammar across accident and VONESA cases | T-206, T-116 | OBS, UX |
| T-309 | M3 | Tenant-keyed cache guard — the cache twin of `assertNoTenantLeak` (Rev 22) | A,E | Lint/test fails any `use cache`/`unstable_cache` usage in member/staff/agent/admin route groups whose key set omits `access_tenant_id` (+ `member_id` where member-scoped); shared data caches permitted **only** for rule packs, airline registry, plan catalogs, and locale messages; invalidation is **event-driven**: an outbox relay consumer calls `revalidateTag` per case/member tag (no client polling); cross-tenant cache-leak test (two tenants, same route, overlapping ids) green; documented alongside T-302b as part of the isolation story | T-302, T-302b | ISO, PERF |

**UI/UX exit gates:** modularity → existing `T-007b` (≤150 lines, already CI-enforced); performance → new size-limit CI lane enforcing OD#17 (Lighthouse mobile > 90, entry bundle < 120 KB gzipped, public-page TTFB < 100ms from Edge CDN); E2E → dashboard smoke on consolidated layouts on `ida.localhost` (extends T-114), asserting no `legacy/` import and no host-derived theming. **Rev 22 adds:** composition → T-116 exhaustiveness build-check + layer-boundary lint (`ui/crystal` imports zero domain code; verticals never import each other); data-fetching → T-117 ≤2-projection-queries assertion + `React.cache()` dedupe test; caching → T-309 tenant-keyed cache lint + cross-tenant cache-leak test.

---

## Score-impact map (which tasks move which dimension to 10)

| Dimension (now→10)                                         | Tasks that close the gap                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Explicit state machines (5→10)                             | T-000/001/001b/002/002c/003 (single transactional command + optimistic lock), T-103, T-202/203/203b/205, T-503                                                                                                                                                                                                                                                                       |
| Observable workflows (5→10)                                | T-104/104b (outbox+relay+per-consumer deliveries), T-104f/g/h (reference-only PII), T-105/105b (audit derived from events), T-204, T-206                                                                                                                                                                                                                                             |
| Predictable routing (5→10)                                 | T-011, T-108/109/114 (ida-first host model + test lanes), T-115 (front-door shell), T-304, T-307, T-501/502 (layout consolidation)                                                                                                                                                                                                                                                   |
| Frontend / UX quality + performance (new)                  | T-115 (shell+skeleton), T-206 (timeline streaming), T-310 (theme tokens), T-410 (optimistic boundary), T-411 (Smart Next Step lib), T-502 (layout dedup), **T-116/117/118 (CaseSummary union + RSC shell + `ui/crystal`), T-210 (event-renderer registry), T-309 (tenant-keyed cache guard), FLIGHT-09b (5-step stepper)**; gated by `T-007b` modularity + size-limit budget (OD#17) |
| Type-level guards (illegal states unrepresentable, Rev 22) | T-002d (branded `AuthorizedTransition`), FLIGHT-05b (`Signed<>`/`Accepted<>` witnesses + closed `CompensationEvidence` — no-win-no-fee at compile time), T-403b (brand-minted `AICallContext`), T-116 (exhaustive case-kind registry); each backed by `@ts-expect-error` compile-fail fixtures in CI (ADR-22)                                                                        |
| CQRS read models (Finance/Reporting decoupling, Rev 22)    | CQRS-00…04 — outbox-only consumers with per-consumer cursors, import-boundary + zero-grant enforcement, `fee_receivables` (30% success fee) + funnel projections, no second ledger (ADR-21); enterprise evidence via `ent-tm`/`ent-dlv` lanes                                                                                                                                        |
| Product-model alignment (6→10)                             | T-007/008, T-101/102, T-401/402, T-403/404/405, **T-111/112/113 + T-407/408 (entity-of-record + disclosure + tax binding)**                                                                                                                                                                                                                                                          |
| Low coupling (6→10)                                        | T-201, T-204, T-306, T-408                                                                                                                                                                                                                                                                                                                                                           |
| Separation of concerns (6→10)                              | T-302, T-302c (extend wrapper + import boundary), T-307                                                                                                                                                                                                                                                                                                                              |
| Least privilege (6.5→10)                                   | T-006/006b (role lint), T-301 (+global_support), T-304, T-306                                                                                                                                                                                                                                                                                                                        |
| Clear boundaries (7→10)                                    | T-201, T-402                                                                                                                                                                                                                                                                                                                                                                         |
| Tenant isolation (8→10)                                    | T-004/005, T-011, T-108/114 (real no-tenant context + explicit E2E session context), T-302/302b/302c/302d (extend `withTenantContext` + migrate RLS to `app.current_access_tenant_id`), T-303, T-305/305b, T-310 (theme tokens never cross access_tenant)                                                                                                                            |
| Testable rules (8→10)                                      | T-205, T-007/008, T-404                                                                                                                                                                                                                                                                                                                                                              |
| Debuggability (7→10)                                       | T-009, T-105, T-206, T-004                                                                                                                                                                                                                                                                                                                                                           |
| Compliance / jurisdiction (legal correctness)              | T-104c/d/f/h (reference-only PII + crypto-shredding + erasure across all stores), T-111/112/113 (entity-of-record), T-208/208b (Rome II + no-fallback proof), T-209 (handoff), T-407 (disclosure), T-506/507 (migration guard + residence/DSR), T-007 (protective-message presence), FLIGHT-05 + SVC-04 (transition invariants), SVC-03/05/10 (consent + isolated legal docs)        |
| Product verticals (VONESA + 10 services)                   | WS-F (FLIGHT-00…11), WS-G (SVC-00…10) — ride the spine behind feature flags; gate **nothing** on the critical path                                                                                                                                                                                                                                                                   |
| Overall architecture (6.5→10)                              | M2 + M3 + M5 collectively                                                                                                                                                                                                                                                                                                                                                            |

---

## Critical path & parallelization

```
T-001 ─┬─ T-002
       ├─ T-003
       └─ T-103 ── T-201 ─┬─ T-202 ── T-503 (M5)
                          ├─ T-203 ── T-205
                          └─ T-204
T-104 ─┬─ T-104b (relay/idempotency) ── T-204
       ├─ T-104c (PII allowlist) ── T-104d (crypto-shredding)
       └─ T-105 ── T-206
T-006 ── T-006b ── T-301 ─┬─ T-304 ── T-306
                └─ (M1) ── T-302 ─┬─ T-303
                                  ├─ T-302b (isolation → access_tenant_id)
                                  └─ T-305 (four context columns)
                                  └─ T-307 ── T-502 (M5)
T-011 ── T-108 ─┬─ T-109 ── T-502 (M5)     (ida-first host model: foundation at M1)
                ├─ T-110
                └─ T-114 ── T-302 ── T-501 (M5, also needs T-202)
T-403 ── T-404 ── T-405        T-401 ── T-402
T-305 ── T-504 ─── T-506 (M5, entity migration; also needs T-112)
T-101 ── T-111 ── T-112 ─┬─ T-407 (M4 disclosure)   (entity-of-record chain)
                         ├─ T-408 (M4 tax binding)
                         └─ T-208/209 (M2 jurisdiction)

  WS-F (VONESA) — parallel team, behind feature flag, gates nothing on the spine:
  FLIGHT-00 ── FLIGHT-SCHEMA ─┬─ FLIGHT-01 ─┐
                             ├─ FLIGHT-02 ─┤
                             └─ FLIGHT-05 ──┴─ FLIGHT-06 ── FLIGHT-07
  FLIGHT-04 (M3, needs T-302b/T-404)   FLIGHT-03/09/10 (M4)   FLIGHT-08 (M5, needs T-408/T-204)

  WS-G (10 services) — parallel team, behind feature flag, gates nothing on the spine:
  SVC-00 ── SVC-CORE ─┬─ SVC-06 (Priority-1, no-login) ─┬─ SVC-01/02/03/04
                      └─ SVC-05/07/09/10 (recovery)     └─ SVC-DASH (M4) · SVC-08 (M5)
```

**Longest chain:** T-001 → T-103 → T-201 → T-202 → T-503. Everything else fits around it. **The ida-first host model (T-011 + T-108/109/110/114) sits at M0/M1 and is the prerequisite for dashboard gate design** — it only rejoins the full architecture at M5's live flip (T-501), which is gated on T-202 (case/recovery) + T-302 (single tenant-context). M4 (product-model) and the C-stream (events) have no dependency on the B-stream critical path until T-204/T-206 join — assign them to a second owner to run fully parallel. **WS-F (VONESA) and WS-G (10 services) add no node to the critical path** — they consume the spine's exit criteria (case/recovery split M2, four-context resolver M3, entity-of-record billing M5) and ship behind their own feature flags, so neither the **Design Gate** (front-door/session stabilization) nor **Go-Live** (`ida.*` cutover) depends on them. Help Now (SVC-06) is the highest business priority and, being rules-only with no login/PII, can ship as soon as SVC-CORE lands. **WS-H (CQRS, Rev 22) likewise adds no node to the critical path:** CQRS-00 hangs off T-104b (already DONE), CQRS-01 off T-302c, and CQRS-02/03/04 off the M5 billing nodes — all behind DOM promotion. The Rev 22 UI/UX nodes (T-116/117/118/210/309, FLIGHT-09b) ride the existing Design-Gate and milestone gates exactly like the Rev 18 nodes; T-117/T-118 can start the moment T-115 is green.

## Open decisions (resolve before starting the dependent milestone)

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Needed by                                     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| 1   | Owners per workstream (A/B/C/D/E)                                                                                                                                                                                                                                                                                                                                                                                                                                          | M0                                            |
| 2   | Cadence: sprint length & how many milestones per sprint                                                                                                                                                                                                                                                                                                                                                                                                                    | M0                                            |
| 3   | `domain_events` storage: same Postgres vs separate — and retention policy                                                                                                                                                                                                                                                                                                                                                                                                  | M1 (T-104)                                    |
| 4   | Backfill ownership for incident-country + claim_number (data team vs eng)                                                                                                                                                                                                                                                                                                                                                                                                  | M1 (T-102/106)                                |
| 5   | Flag/rollout mechanism for `ida.*` host (existing flag system vs new)                                                                                                                                                                                                                                                                                                                                                                                                      | M5 (T-501)                                    |
| 6   | **Legal counsel sign-off on the entity-of-record model** — per-country obligations (non-EU Balkan entities + EU-resident diaspora pulling in EU consumer/GDPR), and whether recovery/success-fee is a regulated legal-services activity per jurisdiction                                                                                                                                                                                                                   | M1 (T-111/112), gates M2 (T-208) & M4 (T-407) |
| 7   | Which markets get a full local `legal_entity` now vs a home/hub entity serving diaspora until migration (Revolut hub→branch sizing)                                                                                                                                                                                                                                                                                                                                        | M1 (T-112), M5 (T-506)                        |
| 8   | ~~Crypto-shredding vs reference-only for PII in `domain_events`~~ → **RESOLVED (Rev 5): reference-only encrypted PII payloads + crypto-shred the keys** (T-104f/g/h). Remaining sub-decision: event retention window                                                                                                                                                                                                                                                       | M1 (T-104f)                                   |
| 9   | Residence-change trigger: immediate re-acceptance+migration vs defer-to-renewal; run-off rule for active-recovery members                                                                                                                                                                                                                                                                                                                                                  | M5 (T-507)                                    |
| 10  | Dashboard design gate timing: final member/admin/agent/staff dashboard IA starts after T-011 + T-108 + T-114 are green, so UX assumes `ida.*` rather than country hosts                                                                                                                                                                                                                                                                                                    | M0/M1, gates dashboard design                 |
| 11  | **VONESA business inputs (VONESA §24):** final Assignment/POA/Success-Fee texts (SQ/MK/EN) + versions; success-fee %, legal-action-fee %, minimum fee, member-discount; first flight-data provider + terms; initial airline contact list; payout rules + net-payout deadlines; document-retention policy; which cases escalate to partner lawyers                                                                                                                          | FLIGHT-00 gates FLIGHT-05/06/08               |
| 12  | **Services packs (services-alignment §8):** Rule Matrix (MK/KS/AL × TPL/Kasko/Green Card/property/health/injury/vehicle/invalidity/flight); Procedure Packs; legal documents; UI copy; governance (versioning/provenance/retention)                                                                                                                                                                                                                                        | SVC-00 gates SVC-01..10                       |
| 13  | **Highest-blast-radius change (Rev 5, G3):** migrating the central `tenant.ts` `withTenantContext` + every existing RLS policy to `app.current_access_tenant_id` (T-302c/d). Real RLS already exists — sequence as dual-setting transitional, gated by an extended `rls-engaged.test.ts`. Confirm rollout/rollback plan                                                                                                                                                    | M3 (T-302c/d)                                 |
| 14  | **Operational governance model (Rev 6):** decide detailed business RACI for pricing, discounts, fee changes, recovery terms, finance administration, claims/recovery/agent management, and per-country legal-entity administration. Pilot implementation only needs ADR-09 role model, SoD constraints, and break-glass audit; do not add 6+ operational roles until named owners/processes exist.                                                                         | M3 (ADR-09/T-301), gates later ops expansion  |
| 15  | **Design-token strategy** for dynamic theme resolution (T-310): confirmed direction = CSS custom properties injected by Server Components + tenant logo via CDN URL on the tenant row (no static per-tenant imports). Open sub-point: token naming scheme + neutral-default palette for `{kind:'public'}`                                                                                                                                                                  | M3 (T-310)                                    |
| 16  | **Skeleton/loading library + streaming strategy** (T-115/T-206/T-411): adopt a skeleton lib vs custom; RSC streaming vs client incremental hydration for timeline/dashboards                                                                                                                                                                                                                                                                                               | M1/M2/M4 (T-115/206/411)                      |
| 17  | ~~Frontend performance budget~~ → **RESOLVED (Rev 18):** Lighthouse mobile **> 90**; entry-point bundle **< 120 KB gzipped** (size-limit CI check); public-page TTFB **< 100ms** from Edge CDN. Remaining sub-point: per-route bundle ceilings for heavier authenticated dashboards + client caching choice — **Rev 22 narrows this:** RSC-first with tag-based revalidation driven by the outbox relay (T-309); no client polling/SWR on member surfaces                  | M4 (T-410, perf gate)                         |
| 18  | **CQRS consumer promotion timing (Rev 22):** `finance`/`reporting` consumer skeletons (CQRS-00) may land with M1 outbox plumbing as no-op consumers, but projection builds (CQRS-02/03) and the gated-command path (CQRS-04) stay behind DOM-program promotion post-M3/M5 per `current-program.md`. Confirm the promotion slice and owners before any projection table ships                                                                                               | M5 (CQRS-02/03/04)                            |
| 19  | **Fee-receivable retention residuals (Rev 22):** the Art. 17(3) legal-basis residual list for `fee_receivables` and financial aggregates (tax/accounting retention on success-fee invoices, per entity-of-record jurisdiction) must be enumerated with owner sign-off so ENT-DLV verification records can classify them as allowed residuals, not erasure failures. Retention window per `legal_tenant_id`/`recovery_legal_tenant_id` jurisdiction; counsel input via OD#6 | M5 (CQRS-02), `ent-dlv` records               |
