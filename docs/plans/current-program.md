---
plan_role: canonical_plan
status: active
source_of_truth: true
owner: platform
last_reviewed: 2026-06-02
tracker_path: docs/plans/current-tracker.md
execution_log_path: docs/plans/2026-03-03-implementation-conformance-log.md
status_command: pnpm plan:status
---

# Current Program

> Authority: This is the only document allowed to define the current phase, committed priorities, and sequencing for repository execution.

## Active Architecture Finalization Authority

Effective 2026-05-31, the active execution authority is extended by `docs/plans/architecture-finalization-program-2026-05-29.md`. That document is now the canonical new-architecture program for the M0→M5 path: M0 guardrails, M1 ida-first foundation and event/outbox schema, M2 case/recovery split, M3 tenant-context/RLS migration, M4 product-model hardening, and M5 live cutover.

The historical phase queue below remains the evidence ledger for completed Phase C work. New product expansion such as `IDA-MH03` is paused behind the architecture-finalization tracker unless explicitly re-promoted after the architecture spine is stable.

Phase C guardrails still apply during architecture finalization: `apps/web/src/proxy.ts` remains gated, canonical routes stay `/member`, `/agent`, `/staff`, `/admin`, clarity markers remain contractual, Paddle stays the billing provider, and all tenancy/RLS changes must follow the approved tracker tasks and gates.

## Current Phase

Architecture Finalization is now the active Phase C execution lane as of 2026-05-31. The canonical new-architecture program is `docs/plans/architecture-finalization-program-2026-05-29.md`, the active architecture queue is `docs/plans/architecture-finalization-tracker-2026-05-29.md`, and the next implementation work starts from M0 guardrails. The previously promoted `IDA-MH03` product slice is paused behind this architecture lane unless explicitly re-promoted.

M0 guardrails have completed through `ARCH-M0-18`: writer inventory (`T-000`) landed in PR `#880`, transition guard plus lifecycle foundation (`T-001` and `T-001b`) landed in PR `#881`, staff transition writer adoption (`T-003` plus the first bounded `T-002` adoption step) landed in PR `#883`, tenant leak harness extraction (`T-004`) landed in PR `#885`, tenant leak guard adoption (`T-005`) landed in PR `#887`, role exhaustiveness/distinctness proof (`T-006`) landed in PR `#889`, raw role-array lint proof (`T-006b`) landed in PR `#892`, brand discipline lint (`T-007`) landed in PR `#894`, modularity guard (`T-007b`) landed in PR `#896`, compensation-guaranteed copy lint (`T-008`) landed in PR `#897`, ActionResult claim-status cleanup (`T-009`) landed in PR `#899`, ADR claim-transition / role-separation records (`T-010`) landed in PR `#901`, Playwright tenant-host lane inventory/guard proof (`T-011`) landed in PR `#903`, package admin transition writer adoption landed in PR `#905` with Sonar duplicate-lines follow-up PR `#906`, package agent transition writer adoption landed in PR `#908`, package generic claim-status writer adoption landed in PR `#910`, package legacy helper transition writer adoption landed in PR `#912`, member draft cancellation transition writer adoption landed in PR `#916`, app-local admin ops transition writer adoption landed in PR `#918`, and claim creation/submit initialization classification landed in PR `#920`. `T-002` remains WIP only for the event-append and atomic rollback portion because `transitionClaimStatus()` does not yet append a domain event. The next promoted architecture slice is `ARCH-M1-01` / `T-104`, limited to the additive `domain_events` transactional outbox table and typed `appendEvent(tx, ...)` helper needed before later `T-105` / `T-002c` claim-transition event wiring, as recorded in `docs/plans/2026-06-03-arch-m1-01-domain-events-outbox-promotion.md`.

`v1.0.0` release authority is declared and active.

The release-convergence slice `V01` through `V05` is complete. The current program has now also completed six measured AI runway slices, `AI01` through `AI06`, without reopening routing, auth, tenancy, or request-path risk.

`P-1` Infrastructure Debt Closure is now complete as the post-AI tranche. `D01` is complete as the full PR `e2e:gate` CI slice, `D02` is complete as the cross-agent messaging isolation slice, `D03` is complete as the blocking repository coverage floor slice, `D04` is complete as the canonical blocking `gitleaks` coverage slice, `D05` is complete as the staging Supabase plus preview-environment separation slice, `D06` is complete as the canonical incident playbook slice, `D07` is complete as the Sentry burn-rate alert wiring slice, and `D08` is complete as the narrow critical-table RLS proof slice.

`P1C` Commercial Contract Foundations is now complete as the post-infrastructure commercial-contract tranche. `C05` is complete as the canonical claims-first scope-tree and referral-boundary slice, `C01` is complete as the canonical coverage-matrix publication slice, `C04` is complete as the canonical annual billing, cancellation, refund, and cooling-off publication slice, `C02` is complete as the canonical success-fee calculator and example-pricing slice, `C03` is complete as the canonical Free Start and hotline disclaimer slice, and `C06` is complete as the canonical commercial-funnel instrumentation slice. The committed commercial contract defined by the business-model blueprint is now fully published.

`P1T` Free Start And Trust UX is now complete as the live post-`P1C` tranche. `T01` is complete as the canonical hero, trust-strip, and footer-safety-net slice, `T02` is complete as the canonical Free Start claim-pack generator shell slice, `T03` is complete as the canonical confidence-score and recommended-next-step slice, `T04` is complete as the canonical evidence prompts, privacy badge, and SLA microcopy slice, `T05` is complete as the canonical `/services` alignment slice for the coverage matrix and referral boundaries, and `T06` is complete as the canonical i18n plus component-test coverage slice for the new trust surfaces.

`P2` Billing And Terms Hardening is now complete as the post-`P1T` tranche. `B01` is complete as the Stripe residue cleanup and billing-surface contract reconciliation slice, `B02` through `B06` are complete as the in-place Paddle safety and billing auditability queue, and `B07` through `B10` are complete as the commercial terms enforcement queue.

`P3` Commercial Actions And Contract Enforcement is now complete as the post-`P2` tranche. `M01` is complete as the commercial mutation-path audit slice, `M02` is complete as the typed Server Action migration slice for the top commercial mutations, `M03` is complete as the commercial idempotency slice for those canonical mutations, `M04` is complete as the matter-consumption guard slice for canonical staff-led recovery, `M05` is complete as the audit-safe escalation decision slice, and `M06` is complete as the superseded commercial write-path retirement slice. Free Start completion now routes through a typed server action, escalation request metadata now comes from `submitClaimCore`, agreement acceptance plus cancellation remain on canonical typed server actions, duplicate Free Start, escalation, and billing submissions now reuse explicit idempotency keys instead of double-creating work or double-applying commercial state, staff transitions into `negotiation` or `court` now consume annual matter allowance once per claim, block exhausted allowance by default, and accept a staff-only explicit override reason when recovery must begin without available allowance, escalation acceptance plus decline now persist actor, decision time, reason, and resulting next state on the canonical staff action path, and migrated Free Start, claim submission, staff commercial action, and membership cancellation callers now import the `.core.ts` server actions directly with no compatibility-only commercial shim left on the active path. Roster import now routes through the canonical agent write path, and completed `P4G` work now also preserves the aggregate-only dashboard, sponsored-seat activation, and privacy-consent proof boundary on those same canonical surfaces.

`P4` Staff Operations And Acceptance Control remains active as the post-`P3` tranche. `S01` is now complete as the canonical staff claim queue and filter work slice, `S02` is now complete as the canonical manual claim assignment slice, `S03` is now complete as the canonical claim stage history and member-visible tracker slice, `S04` is now complete as the canonical SLA-state visibility slice, `S05` is now complete as the canonical internal-notes isolation slice, `S07` is now complete as the canonical matter-ledger and allowance-visibility slice, intentionally promoted ahead of `S06`, `S06` is now complete as the next promoted `P4` slice after `S07`, `S08` is now complete as the explicit recovery-decision gate slice after `S06`, `S09` is now complete as the accepted-case agreement and collection prerequisite slice after `S08`, and `S10` is now complete as the guidance-only and referral-only commercial-enforcement slice after `S09`. The live tracker now preserves the actionable staff queue surface, public claim-history continuity, explicit incomplete-vs-running SLA visibility, internal-note isolation, annual matter-allowance visibility, secure member-staff messaging, the explicit accepted-or-declined recovery gate, accepted-case agreement-plus-collection-path enforcement, and the launch-scope block that keeps guidance-only and referral-only matters out of staff-led recovery and success-fee handling on the canonical staff claim-detail surfaces before load-based auto-assignment is promoted. `GA01` is now complete as the canonical group roster import slice, `GA02` is now complete as the canonical aggregate-only group dashboard slice, `GA03` is now complete as the canonical sponsored-seat activation and self-upgrade slice, and `GA04` is now complete as the canonical privacy and consent negative-test slice.

`P6` v1.0.0 RC Gate is now complete as the next committed tranche after the completed `P4` and `P4G` queue. `G07` is now complete as the first explicit release-gate slice, `G08` is now complete as the Free Start and aggregate-only privacy-boundary proof slice, `G09` is now complete as the matter-count and SLA-state enforcement slice, and `G10` is now complete as the escalation-agreement and collection-fallback enforcement slice.

`P7` Pilot Readiness And Release Evidence is now complete after completed `P6`. `R01` is now complete as the canonical pilot-entry artifact-set slice, `R02` is now complete as the canonical readiness-command authority slice, `R03` is now complete as the deterministic daily evidence capture slice, `R04` is now complete as the continue-or-rollback proof slice, `R05` is now complete as the pilot-ready tag-discipline slice, `R06` is now complete as the canonical 3-day repo-backed readiness cadence slice, `R07` is now complete as the canonical observability-and-incident-evidence slice, and `R08` is now complete as the canonical ranked operator-flow slice required before live pilot operation is treated as governable across member and agent activity, branch-manager branch oversight, staff claim processing, and admin decision custody.

`P8` Pilot Reset Gate And Seven-Day Rehearsal is now complete after completed `P7`. `P8R` reset-gate hardening is now complete through `RG01` through `RG05`, using the new pilot id `pilot-ks-7d-rehearsal-2026-03-16` to prove a fresh `pilot:check`, fresh memory top-hit retrievals, a fresh GO release gate, and a fresh copied pilot evidence index without reusing the failed 14-day pilot. `P8P` seven-day pilot rehearsal is now complete with `SP01` complete as the new `PD01` release-and-artifact baseline, `SP02` complete as the green `PD02` rollback-and-resume baseline, `SP03` complete as the green `PD03` closed-loop role-flow proof, `SP04` complete as the bounded-amber `PD04` SLA, matter, and branch-pressure proof, `SP05` complete as the green `PD05` privacy, RBAC, and multi-tenant stress proof, `SP06` complete as the green `PD06` communications-and-incident proof, and `SP07` complete as the bounded-amber `PD07` executive review with a `repeat_with_fixes` recommendation and canonical `pause` decision; the repo-backed week-1 KPI or SLA rollup is now checked in and confirms that claim-based threshold proof is still required before any controlled live-pilot expansion decision.

`P9` Regional Wedge Foundation And Diaspora Service Line is now complete after completed `P8`. `E1` Regional Wedge Foundation is now complete through the membership-first acquisition restart, deferred tenant-classification runtime hygiene, and deterministic admin tenant-classification gate-proof slices landed in PRs `#410`, `#414`, and `#418`, keeping the default-public-tenant acquisition model intact while making tenant-classification review and proof inspectable on canonical member and admin surfaces. `E4` Diaspora And Green Card is now complete through the member diaspora quickstart, preserved claim handoff context, persisted diaspora provenance, staff/admin origin visibility, and staff/admin origin filtering slices landed in PRs `#419`, `#420`, `#421`, `#427`, and `#429`, making diaspora / Green Card support and downstream claim operations inspectable without reopening proxy, auth, or tenancy architecture.

`P10` Membership Funnel And Verification Hardening is now complete after completed `P9`. `E2` Membership And Intake Funnel is now complete through the conversion-surface, assisted-business, success-onboarding, checkout-reconciliation contract, checkout-user write-path reconciliation, frictionless OTP onboarding, public CTA alignment, claim-number/Paddle alignment, member follow-up, subscription-lifecycle hardening, and salvage/browser-proof slices landed in PRs `#430`, `#431`, `#432`, `#437`, `#439`, `#442`, `#446`, `#447`, `#452`, `#453`, `#460`, and `#462`. `H01` Phase C Verification And Public-Entry Hardening is now complete through the Codex review workflow, shell-guard follow-up, QA Phase C tool-surface work, Sonar-safe cleanup, member-number salvage, and post-`E2` public-entry coverage slices landed in PRs `#448`, `#451`, `#454`, `#455`, `#461`, and `#462`, keeping the post-funnel verification path inspectable on canonical repo surfaces.

`P11` Bounded Production Operations And Expansion Proof is now complete after completed `P10`, promoting Program A into the repo-canonical execution line and closing `A01` through `A04` with repo-backed KPI, privacy / RBAC, and executive-decision custody.

`P12` Program C Business-Model Integrity Hardening is now complete after completed `P11`. Repo naming uses `P12` and `PC01` through `PC06` to avoid colliding with the already-completed `P1C` Commercial Contract Foundations tranche. `PC01` completed the membership-fulfillment canonicalization line, `PC02` completed the agent ownership and attribution unification line across PRs `#479`, `#481`, and `#486`, `PC03` completed the commission and referral finance hardening line in PR `#488`, `PC04` completed the branch operations and cash-truth line in PR `#491`, `PC05` completed the membership lifecycle and reporting-truth line in PR `#492`, and `PC06` completed the enterprise-safe controls and edge-case hardening line in PR `#493`. `P12` remained a domain-integrity tranche, not a portal redesign, not a route or auth or tenancy refactor tranche, and not a modernization tranche; it was explicitly limited to business-model truth beneath the existing member, agent, staff, admin, branch, and tenant surfaces.

`P13` v1.0.0 Funnel Completion is now complete after completed `P12`. This tranche delivered the minimum viable v1.0.0 path from the 2026-04-24 evaluation: the existing public Free Start shell now produces a real generated claim-pack product, confidence scoring now runs through a reusable deterministic domain engine with factor breakdown, and critical lifecycle notification wiring now covers claim-pack generation and claim handling. `P13` stayed an experience-layer tranche on top of the completed commercial and operations foundations; it did not reopen `apps/web/src/proxy.ts`, canonical routes, auth layering, tenancy, database schema, Stripe, request-path AI, or broad portal redesign.

`P14` Member Experience Guidance is now complete after completed `P13`. This tranche promotes the first narrow Program D experience slice: `P14-MD01` adds guided next-action cards to the canonical member dashboard so active claim state, document organization, membership readiness, and support handoff are visible before members need to search. `P14` stayed limited to the `/member` dashboard experience layer and did not reopen `apps/web/src/proxy.ts`, canonical routes, auth layering, tenancy, database schema, billing, or broad design-system scope.

`P15` Production Professionalism Hardening is now complete after completed `P14`. This tranche promoted the 2026-04-24 senior-staff repository review findings into the canonical execution queue. `P15` was a blocker-removal tranche, not a redesign or modernization tranche: the P0 registration authorization and protected-route fail-open findings are complete, the P1 token, upload-confirmation, activity-read, and production-runtime hardening gaps are complete, and `P15-QA01` landed in PR `#510` to restore E2E contract proof, docs/env drift proof, and full launch-gate evidence. `P15` did not authorize canonical route renames, auth-layer collapse, tenancy architecture refactors, Stripe reintroduction, or broad portal redesign; `apps/web/src/proxy.ts` remains the sole routing and access-control authority.

`P16` Production Professionalism Re-Review is now complete after completed `P15`. This tranche re-checked the completed hardening line before pilot launch, preserved a repo-backed Go/No-Go artifact, and named missed non-blocking issues without widening scope into proxy, routing, auth, tenancy, Stripe, or broad portal redesign work. `P16-PR01` verified the original five findings are closed, inspected adjacent registration, upload, share-pack, document, AI, settings, and activity paths, recorded residual non-blocking risks, and merged through PR `#512` with local required gates, SonarCloud, PR finalizer, and remote checks green.

`P17` Production Professionalism Maturity is now complete after completed `P16`. This tranche closed the remaining non-blocking issues that kept the bounded-pilot score below 9/10 without reopening proxy, routing, auth layering, tenancy architecture, Stripe, or broad portal redesign. `P17-PR01` is complete through PR `#514`: the legacy claim-wizard upload path uses server-issued upload intents plus storage-object validation before claim evidence is persisted. `P17-PR02` is complete through PR `#517`: tenant-scoped AI run, AI review, and notification settings API handlers now reject missing tenant identity at the route boundary with explicit contract errors before downstream core or DB logic runs. `P17-PR03` is complete through PR `#519`: push subscription settings now reject missing tenant identity at the route boundary and return explicit endpoint-collision conflicts for cross-tenant or cross-user ownership, including raced unique-constraint collisions. `P17-PR04` is complete through PR `#521`: sensitive registration, upload, document, share-pack, AI, settings, billing webhook, and verification route surfaces now have a repo-backed ownership map naming owners, entrypoints, boundary helpers, expected failure contracts, proof references, and future candidates. `P17-PR05` is complete through PR `#523`: the verification detail API now resolves tenant identity at the route boundary, preserves the unauthorized session contract, and has focused negative route tests for missing user and tenant identity. `P17-PR06` is complete through PR `#525`: the share-pack POST API now resolves tenant identity at the route boundary, preserves the unauthorized session contract, rejects missing tenant identity before share-pack core execution, and has focused route-level proof for missing user, missing or malformed tenant identity, invalid IDs, and authorized creation. Post-merge tracker closeouts through PR `#526` record the slice-level proof, and this aggregate closeout records the complete tranche proof. All six slices merged with local gate evidence, SonarCloud, PR finalizer, full E2E, Pilot Gate, and remote checks green.

`P18` Pilot-Learned Trust And Activation UX is now complete after completed `P17`. `P18-DG01` is complete as the repo-canonical design gate that promoted the next experience-oriented era from the existing pilot-readiness blueprint after P17 closeout, using `P18` instead of the blueprint's historical `P8` label because `P8` is already a completed live-program tranche. `P18` was explicitly limited to pilot-evidence-informed trust and activation work on existing canonical surfaces. It did not authorize `apps/web/src/proxy.ts` changes, canonical route renames, auth layering changes, tenancy architecture changes, Stripe reintroduction, broad portal redesign, or implementation work that was not first bounded in the live tracker. `P18-UX01` is complete as the evidence-ranking slice: repo-custodied operational progression and trust/SLA proof were ranked above partial member-return and activation data, no agent-demo feedback artifact was found, and the slice recommended exactly one bounded implementation slice. `P18-UX02` is complete as that implementation slice: the canonical member claim detail surface shows current state, latest member-visible public update, and expected next action from the existing member-scoped read model without proxy, route, auth, tenancy, schema, Stripe, portal, or broad dashboard changes. `P18-DG02` is complete through PR `#536` as the next design-gate slice after `P18-UX02`: it records that the rank-1 operational-progression recommendation is consumed and promotes exactly one next bounded implementation slice, `P18-UX03 Member Trust And SLA Clarity`, from the strongest remaining repo-custodied SLA/trust posture evidence. `P18-UX03` is complete through PR `#538`: the canonical member claim detail read model now derives a member trust/SLA summary from existing status and SLA phase after member-scoped access is resolved, and the member detail surface renders localized handling assurance, latest public update timing, and support handoff without proxy, route, auth, tenancy, schema, Stripe, portal, broad dashboard, analytics, or agent-demo changes. CRM and agent-workspace redesign were not implemented in P18; either requires a later repo-canonical design gate before any bounded implementation slice is promoted.

`P19` Agent CRM Readiness And Boundary Hardening is now complete after completed `P18`. `P19-DG01` is complete as the repo-canonical design gate that promotes the post-`P18` queue without product-code changes. It selected exactly one bounded implementation slice, `P19-CRM01 Agent CRM Tenant-Scoped Read Contracts`, because the repo already has an `/agent/crm` surface, CRM lead-detail read model, and tenant-bearing CRM tables, while broad CRM and agent-workspace redesign remain unsupported by repo-custodied agent-demo evidence. `P19-CRM01` is complete through PR `#542`: the existing `/agent/crm` page and CRM lead-detail page now resolve tenant identity at the route boundary, pass tenant identity into their read cores, and scope tenant-bearing CRM reads by tenant plus agent identity without changing proxy, canonical routes, auth layering, tenancy architecture, schema, Stripe posture, product analytics, broad CRM redesign, or broad agent-workspace redesign. `P19-DG02` is complete as the next CRM-readiness design gate after CRM01, and promoted `P19-CRM02 Agent Dashboard CRM KPI Tenant Read Contracts` as the next bounded implementation slice. `P19-CRM02` is complete through PR `#545`: the existing canonical `/agent` dashboard V2 resolves tenant identity before CRM KPI reads, passes tenant identity into `getAgentDashboardV2StatsCore`, scopes tenant-bearing CRM KPI reads and branch lookup by tenant, preserves the current dashboard DTO and visual behavior, and adds focused route/component/core tests. `P19-DG03` is complete through PR `#547` as the next CRM-readiness design gate after CRM02, and promoted `P19-CRM03 Agent CRM Mutation Tenant-Boundary Contracts` as the next bounded implementation slice for existing legacy agent CRM status and activity mutations. `P19-CRM03` is complete through PR `#549`: the existing legacy agent CRM status and activity server actions now resolve tenant identity at the action boundary, reject missing tenant identity with the expected action error before DB mutation cores run, scope CRM lead reads and status writes by tenant plus agent ownership, and insert activity rows with the boundary-resolved tenant identity. `P19` closes the bounded CRM readiness and tenant-boundary hardening line for v1.0.0; remaining CRM feature work, broad CRM redesign, agent-workspace redesign, and product analytics remain deferred to a post-v1.0.0 backlog unless a later repo-canonical design gate explicitly promotes them.

`P20` v1.0.0 Release Declaration And Version Authority is now complete after completed `P19`. `P20-DG01` is complete through PR `#551` as the repo-canonical design gate that selected the bounded release-governance slice after the CRM readiness line closed. `P20-RG01` is complete through PR `#553` as the release declaration and version-authority implementation slice: the live program now declares `v1.0.0` release authority, the repo/app/package manifests that carry release-version authority now declare `1.0.0`, and `docs/plans/2026-04-27-p20-rg01-v1-release-declaration.md` records the evidence basis. P20 did not change product behavior, proxy, routes, auth or tenancy architecture, schema, Stripe posture, product analytics, broad CRM redesign, or broad agent-workspace redesign.

`P21` v1.0.0 Live Surface Revalidation is now complete after completed `P20`. `P21-QA01` completed through PR `#556`: the live-entry confidence gate now verifies launch-critical public, member, agent, CRM, staff, admin, tenant-host, role-boundary, unified-agent, and Paddle-production posture surfaces before any post-v1.0.0 product expansion is promoted. `P21` stayed a verification and hardening tranche only; it did not authorize product behavior expansion, `apps/web/src/proxy.ts` changes, canonical route renames, auth or tenancy refactors, schema changes, Stripe reintroduction, product analytics expansion, broad CRM redesign, or agent-workspace redesign.

`P22` Production Go-Live Operations is now complete after completed `P21`. `P22-GO01` is the operational go/no-go slice that proved the real production deployment identity, tenant-host mapping, production environment posture, Paddle production posture, database backup and rollback path, observability, alerting, and launch-critical production smoke surfaces before live traffic opened. `P22-GO02` is complete through PR `#561` as the narrow `/register` G07 commercial-promise blocker fix: `/register` still redirects to `/pricing`, but the redirect now tags `entry=register` so the existing pricing commercial sections expose the register-specific G07 markers. `P22-GO03` is complete through PR `#568` as the narrow member-claim SLA copy fix for the remaining production `G09` blocker. `P22-GO04` is complete as the non-scripted launch operations acceptance record for backup/recovery point, rollback owner and procedure, alert/on-call owner, incident decision owner, launch window, and final go/no-go authority. `P22` was an operations-readiness tranche only; it did not authorize product behavior expansion, UI/UX redesign, CRM redesign, agent-workspace redesign, `apps/web/src/proxy.ts` changes, canonical route renames, auth or tenancy refactors, schema changes, Stripe reintroduction, or product analytics expansion.

The first `P22-GO01` execution pass is recorded in `docs/plans/2026-04-27-p22-go01-production-readiness-execution.md` with a `NO-GO` decision. After the production Git deployment of PR `#568` at merge commit `c7e038d97de52586deb8c1c8807bf867cad75088`, the fresh `P22-GO01` release-gate rerun against `https://www.interdomestik.com` passed all scripted checks on Vercel deployment `dpl_AtRyCJu5RV4sHmw9w9DtXQEZAgiw`; canonical evidence is preserved in `docs/release-gates/2026-04-28_production_dpl_AtRyCJu5RV4sHmw9w9DtXQEZAgiw.md`. Production health then confirmed deployment `dpl_DSrbEwyPNhuUUAy4QrQvEmkAVfXx` running commit `ad9dd21d0eba8f58b8a18fe5d3ee747b7cbc4e73`, and `docs/plans/2026-04-28-p22-go04-launch-operations-acceptance.md` records the final non-scripted operations acceptance with a `GO` decision.

`P23` Live Product Responsiveness is now complete after completed `P22`. `P23-PERF01` is complete through PR `#572`, merged at `8b7b5c693fa1e42df0a080126f591c630ba48251`: the existing authenticated member and admin shells now expose shared route-transition feedback, route-level loading skeletons cover member and admin navigation, fast and cancelled navigation edge cases are handled, and E2E gate selectors were hardened against transient duplicate transition DOM. `P23-DG01` is complete through PR `#574` as the bounded design gate after `P23-PERF01`: it ranks remaining responsiveness candidates and promotes exactly one implementation slice, `P23-PERF02 Staff Claims Queue Responsiveness`. `P23-PERF02` is complete through PR `#575`, merged at `2fdf66b0ec140f4bd7416c2161cf3bffad18a1ea`: the existing staff claims queue now has route-level loading feedback, deterministic search/filter pending feedback, blocked overlapping client navigations, and preserved query semantics. `P23-DG02` is complete as the bounded design gate after `P23-PERF02` and promotes `P23-PERF03 Admin Claims Filter Responsiveness`. `P23-PERF03` is complete through PR `#578`, merged at `9e438e519c9e0e8a49011d81bce0a85936b3dcb6`: the existing admin claims queue now exposes deterministic local pending feedback for status, assignment, origin, and search transitions. `P23-DG03` is complete as the bounded design gate after `P23-PERF03` and promotes `P23-PERF04 Member Claims Filter Responsiveness`. `P23-PERF04` is complete through PR `#581`, merged at `d8af60052975f459335267f4d2c54e8af81e552e`: the canonical `/member/claims` surface now has deterministic local pending feedback for search, status, and pagination transitions. `P23-DG04` is complete through PR `#582`, merged at `1f302b287b32d934409df4a8d587ad928405064c`, and promotes `P23-PERF05 Admin Users Filter Responsiveness`. `P23-PERF05` is complete through PR `#583`, merged at `9d7f6583cc7a4aa1deffd15796127b9c4311394d`: the canonical `/admin/users` search, assignment controls, Link-backed role tabs, and table viewport behavior now have deterministic pending feedback, inert in-flight controls, and responsive behavior. `P23-DG05` is complete as the bounded design gate after `P23-PERF05` and promoted `P23-PERF06 Agent Clients Search Responsiveness`. `P23-PERF06` is complete through PR `#585`, merged at `65a5b0c7317c3f507bd366e9050331e442838510`: the canonical `/agent/clients` search surface now preserves query semantics, suppresses redundant same-query navigation, exposes deterministic pending/status feedback, and makes in-flight search inert. P23 did not change `apps/web/src/proxy.ts`, canonical routes, auth layering, tenancy architecture, database schema, Stripe posture, CRM scope, agent-workspace scope, product analytics, README, AGENTS, or architecture docs. Further responsiveness work requires bounded slice promotion from repo-canonical evidence rather than broad UI/UX redesign.

`P24` Lint Warning Burn-Down is now complete after completed `P23`. `P24-DG01` is complete as the repo-canonical design gate that ranked the web lint-warning baseline and promoted exactly one bounded implementation slice, `P24-LINT01 Unused Symbols And Stale Suppressions`. `P24-LINT01` is complete: the stale eslint-disable directive and all `@typescript-eslint/no-unused-vars` warnings were removed without changing lint policy, route behavior, auth, tenancy, schema, Stripe, CRM, agent workspace, product analytics, README, AGENTS, or architecture docs. `P24-DG02` is complete as the next repo-canonical explicit-`any` design gate and promoted exactly one bounded implementation slice, `P24-LINT02 Claims Submit Test Typed Mocks`. `P24-LINT02` is complete: the claim submit action test harness now uses typed mocks and wrapper-derived test data/session types, reducing the web lint baseline to `0` errors and `173` warnings, all in `@typescript-eslint/no-explicit-any`, without production-code changes. `P24-DG03` is complete as the next repo-canonical explicit-`any` design gate and promoted exactly one bounded implementation slice, `P24-LINT03 Admin Claims List Test Typed Fixtures`. `P24-LINT03` is complete: the admin claims list test harness now uses typed routing, UI component, translation, and DTO fixtures, reducing the current web lint baseline to `0` errors and `162` warnings, all in `@typescript-eslint/no-explicit-any`, without production-code changes. `P24-DG04` is complete as the explicit-`any` zero-baseline design gate and promoted exactly one implementation slice, `P24-LINT04 Web Explicit Any Zero Baseline`, following the 2026-04-29 directive to continue until lint warnings are `0`. `P24-LINT04` is complete: the remaining wrapper tests, route/API cores, feature read adapters, and component view adapters no longer produce explicit-`any` lint warnings, and `pnpm --filter @interdomestik/web lint` now exits with `0` errors and `0` warnings without changing lint policy, TypeScript configuration, Node tooling, `apps/web/src/proxy.ts`, canonical routes, auth, tenancy, schema, Stripe, CRM redesign, agent-workspace redesign, product analytics, README, AGENTS, or architecture docs.

`P25` Commercial Product Surface Polish is now complete after completed `P24`. `P25-DG01` is complete as the post-`P24` design gate that ranked the first post-stabilization product-quality candidates and promoted exactly one bounded implementation slice, `P25-UX01 Public Commercial Entry Polish`. `P25-UX01` is complete through PR `#596`, merged at `81343427da8f950ae05376695e8cde69555d2ffe`: the existing localized public home, pricing, services, and Free Start/claim-pack entry surfaces now use a consistent Free Start path, reduced-motion support, finish-intake pending/inert behavior, mobile header touch-target sizing, sticky mobile CTA safe-area handling, and Vercel preview deploy suppression through `apps/web/vercel.json`; the services call CTA uses the localized `hero.callNow` strings with `tel:+38349900600`; live production validation passed on deployment `dpl_6TPzUNRKAHNGLjApVATsQSdocZ3J`; and PR Vercel checks passed as `Canceled by Ignored Build Step`. `P25-DG02` is complete as the post-`P25-UX01` design gate: it records that UX01 consumed the only promoted implementation slice, ranks narrow public-entry follow-up, monitoring console noise, broad CRM redesign, agent-workspace redesign, product analytics expansion, and broad SaaS redesign, and does not promote a next P25 implementation slice. Further public-product, CRM, agent-workspace, analytics, broad SaaS redesign, lint, responsiveness, observability, proxy, route, auth, tenancy, schema, Stripe, README, AGENTS, or architecture-doc work requires a new bounded repo-canonical design gate.

`P26` Agent CRM Productization is now complete after completed `P25`. `P26-DG01` is complete as the post-`P25` design gate that ranked domain-CRM continuation above broad SaaS redesign, broad CRM redesign, agent-workspace redesign, product analytics expansion, monitoring console-noise cleanup, and additional lint/responsiveness cleanup. It promoted exactly one bounded implementation slice, `P26-CRM01 Agent Lead Detail Productization`, on the existing canonical `/agent/leads/[id]` detail surface. `P26-CRM01` is complete through PR `#602`, merged at `f6e3b01c5b8091b731c35dd9853c225fd2293574`: the existing lead-detail page now uses route locale for localized rendering and login redirects, replaces hardcoded detail/deal/activity copy with supported agent-locale messages, uses bounded stage and deal-status labels, exposes the unbacked Create Deal affordance as a disabled unavailable action, and adds focused unit plus golden E2E proof while preserving existing route, auth, tenant, read-model, activity, deal-data, schema, Stripe, analytics, broad CRM IA, and agent-workspace contracts. SonarCloud, Copilot review threads, PR finalizer, E2E, security, audit, and required checks were green before merge. `P26-DG02` is complete as the post-`P26-CRM01` closeout gate: it records that the only promoted P26 implementation slice is consumed, ranks remaining CRM/productization, broad CRM redesign, agent-workspace redesign, product analytics, observability cleanup, repo QA MCP tooling, and broad SaaS redesign candidates, and does not promote another implementation slice from the current evidence. Further CRM productization, broad CRM redesign, agent-workspace redesign, product analytics, observability cleanup, proxy, route, auth, tenancy, schema, Stripe, README, AGENTS, or architecture-doc work requires a future bounded repo-canonical design gate.

`P27` Agent Workspace Query Contract Polish is now complete after completed `P26`. `P27-DG01` is complete through PR `#607` as the post-`P26` design gate that ranked remaining repo-backed candidates and promoted exactly one bounded implementation slice, `P27-AGENT01 Agent Workspace Claim Selection Close Contract`, on the existing canonical `/agent/workspace/claims` surface. `P27-AGENT01` is complete through PR `#608`: the existing agent workspace claims surface now clears both `selected` and direct `claimId` selection aliases when the drawer closes, preserves unrelated query parameters, keeps direct-link open behavior and inaccessible-claim handling intact, and has focused unit plus gate E2E proof for the direct `claimId` close path. P27 did not authorize or change broad CRM redesign, broad SaaS redesign, agent-workspace redesign, product analytics expansion, observability cleanup, proxy behavior, canonical routes, auth or tenancy architecture, schema, Stripe posture, README, AGENTS, or architecture docs. Further agent workspace, CRM, analytics, observability, route, auth, tenancy, schema, Stripe, README, AGENTS, or architecture-doc work requires a future bounded repo-canonical design gate.

`P28` Membership Success Runtime Guard Polish is now complete after completed `P27`. `P28-DG01` completed the post-`P27` design gate that ranked remaining repo-backed candidates and promoted exactly one bounded implementation slice, `P28-MEM01 Membership Success Billing Test Activation Guard`, on the existing canonical `/member/membership/success` surface. `P28-MEM01` is complete as the bounded implementation that aligns the membership success page's client-side billing test activation trigger with the existing `isBillingTestActivationEnabled()` runtime guard while preserving the guarded server action and current success-page behavior. P28 did not authorize or change broad CRM redesign, broad SaaS redesign, agent-workspace redesign, product analytics expansion, observability cleanup, proxy behavior, route behavior, auth or tenancy architecture, billing provider architecture, schema, Stripe posture, README, AGENTS, or architecture docs. Further membership success, billing-test, CRM, agent-workspace, analytics, observability, route, auth, tenancy, schema, Stripe, README, AGENTS, or architecture-doc work requires a future bounded repo-canonical design gate.

`P29` Repo QA MCP Active Checkout Guard is now complete after completed `P28`. `P29-DG01` completed the post-`P28` design gate that ranked the observed repo QA MCP active-session root mismatch above additional CRM productization, agent-workspace follow-up, membership success follow-up, product analytics expansion, observability cleanup, and broad redesign candidates. It promoted exactly one bounded implementation slice, `P29-QA01 Repo QA MCP Active Checkout Guard`, within existing repo QA MCP tooling and contracts. `P29-QA01` is complete: repo QA MCP project-map, status, and branch helpers now expose resolved repo root and root-source signals in structured output, status/branch text output includes repo-root identity before branch/head/dirty-state details so agents can detect wrong-checkout sessions quickly, and the Pilot Gate Sonar decision path now waits longer for SonarCloud Automatic Analysis, uses the `continue-on-error` step outcome for fallback decisions, and skips invalid manual fallback when SonarCloud Automatic Analysis owns PR analysis. P29 did not authorize or change product code, `apps/web/src/proxy.ts`, canonical routes, auth or tenancy architecture, billing provider architecture, schema, Stripe posture, broad CRM redesign, broad SaaS redesign, agent-workspace redesign, product analytics, README, AGENTS, or architecture docs.

`P30` Commercial UX North Star Alignment is now complete after completed `P29`. The promoted slice is `P30-MEM01 Member North Star Copy Alignment`, limited to localized member-dashboard copy that makes the active member value proposition concrete around the case file, required steps, evidence upload, status tracking, organized documents, unique case number, and clear next step. `P30-MEM01` is complete through PR `#613`: the existing member dashboard North Star copy and trust cues now use the case-file workflow language across supported dashboard locales, and the slice preserves existing routes, auth, tenancy, schema, billing, analytics, homepage structure, partner activation flow, and dashboard component architecture. `P30` did not authorize homepage variant implementation, partner activation flows, product analytics expansion, broad SaaS redesign, CRM redesign, agent-workspace redesign, `apps/web/src/proxy.ts` changes, canonical route changes, auth or tenancy refactors, schema changes, Stripe changes, README, AGENTS, or architecture-doc updates.

`P32` Domain CRM Operating System is now active after the landed member-support handoff alignment on `main` at `fb32f027`. `P32-DG01` is complete as the design gate that confirms `support_handoffs` as the next durable handoff entity, keeps CRM01 staff-first with `branch_manager` visibility inherited through existing staff access, defines member support request submission as the first creation trigger, defers direct claim-detail handoff creation, derives urgency and trust-risk server-side, defines accept/reassign/close lifecycle semantics, allocates the journaled Drizzle schema migration to CRM01, keeps Relationship as a read-model projection, keeps Matter claim-only for V1, rejects stored `TrustSignal`, and promoted exactly one implementation slice: `P32-CRM01 Staff Member Support Handoff Receiving Queue`. `P32-CRM01` is complete through PR `#619`, merge commit `9c1c9749365f96cc85ff82c948a71609f239fce1`: the member-owned `/member/help` support request path now creates tenant-scoped `support_handoffs` with authenticated server-derived tenant/member/branch/optional-claim/actor identity, the staff queue under `/staff/support-handoffs` supports staff accept/reassign/close lifecycle with optimistic accept concurrency, branch managers retain read-only queue visibility, urgency and trust-risk are derived, Relationship stays a read-model projection, Matter stays claim-only for V1, and no `TrustSignal` table or stored signal was added. `P32-DG02` is complete as the post-CRM01 design-review gate for the deferred claim-detail handoff trigger, and `P32-CRM02` is complete through PR `#622`, merge commit `c438cb4023049a39d49701cd1287544e924e0acf`: the existing member claim detail support CTA now carries server-verified claim context into the route-owned `/member/help` form, displays the selected claim before explicit member submission, reuses the governed support-handoff creation core, and proves staff queue receipt with linked claim context without creating a handoff on page render or CTA click. `P32-DG03` is complete as the post-CRM02 design-review gate for staff-side handoff detail context, and `P32-CRM03` is complete through PR `#626`, merge commit `fb0a57b8330f1fbb231b70d8e50ba599f1383baf`: the existing `/staff/support-handoffs` surface now exposes on-demand detail context for staff and read-only branch managers, including contact preference, source, full message, linked claim context, relationship projection, and lifecycle timeline metadata before staff lifecycle actions. `P32-DG04` is complete as the post-CRM03 design-review gate for support-handoff origin source tracking, and `P32-CRM04` is complete through commit `1d1e0f3bd4353b9fefa3b3d1dcd23ffb811f80fb`: claim-detail-originated support handoffs now persist and audit the bounded `member_claim_detail` source, generic help-page submissions remain `member_help`, unsupported source values safely fall back, and staff detail proof renders the claim-detail source label without proxy, route, auth, tenancy, schema, Stripe, Relationship, Matter, TrustSignal, staff lifecycle, branch-manager authority, README, AGENTS, or architecture-doc changes. `P32-DG05` is complete as the post-CRM04 next-slice design gate and promoted exactly one bounded implementation slice, `P32-CRM05 Duplicate Open-Handoff Advisory Guard`, so members can see non-blocking guidance when an active support handoff already exists before submitting another request. `P32-DG06` is complete as the implementation-design review for CRM05: active means `open` or `accepted`, `closed` is excluded, the advisory must use a named member-safe tenant/member-scoped read contract with a narrow member-only signature, selected server-validated claims get same-claim priority, generic `/member/help` gets member-level copy only, active filtering uses an explicit `IN` allowlist, advisory rendering is isolated in a co-located component with localized timestamp copy, field-exclusion and E2E proof are required, submission remains non-blocking, and the possible `(tenant_id, member_id, status)` index is deferred until a later schema/index authorization gate. `P32-CRM05` is complete through PR `#635`, merge commit `6867fe96130f5be9c147ee81e6c6c3ac0f428a92`: the existing `/member/help` form now shows localized, member-safe, non-blocking active-handoff advisory copy for same-claim and generic member-level states while preserving support submission. `P32-DG07` is complete as the post-CRM05 closeout and next-slice gate, ranking staff support-handoff reply/follow-up design above direct implementation, index optimization, hard duplicate blocking, new staff detail routes, and broad CRM abstractions, and promoting exactly one next bounded design-review slice: `P32-DG08 Staff Support Handoff Reply/Follow-up Design Review`. `P32-DG08` is complete as the implementation-design review for staff support-handoff public responses: CRM06 must use support-handoff-specific public response fields rather than claim messages, keep branch managers read-only, allow only assigned staff to respond to accepted handoffs, keep close/reassign reasons staff-only, show the latest member-safe public response on `/member/help`, defer notifications and conversation threads, and preserve CRM05 advisory behavior. `P32-CRM06` is complete through PR `#638`, merge commit `0183b354f0ab25c639888c7eab3da95b6ed0eb0b`: assigned staff can send or update one member-visible public response on accepted support handoffs, branch managers remain read-only, member help shows the latest member-safe public response while preserving CRM05 advisory and non-blocking support submission behavior, and the implementation added the bounded schema fields plus supporting latest-response index after review. `P32-DG09` is complete as the post-CRM06 closeout and next-slice gate, ranking public-response notification semantics design above direct notification implementation, member replies, read receipts, new staff detail routes, and broad CRM abstractions, and promoting exactly one next bounded design-review slice: `P32-DG10 Support Handoff Public Response Notification Design Review`. `P32-DG10` is complete as the implementation-design review for public-response notification awareness: the first approved notification slice must use the existing in-app notification center and `notifications` table, fire only after successful assigned-staff public-response version updates, resolve the member recipient from tenant-scoped handoff data, link to the existing `/member/help` surface, and keep email, push, support-handoff-specific unread counters, read receipts, acknowledgements, SLA timers, member replies, conversation threads, new queues, and schema extensions deferred. It promoted exactly one bounded implementation slice: `P32-CRM07 Support Handoff Public Response In-App Notification`. `P32-CRM07` is complete through PR `#643`, merge commit `8b56bd563d3f48ee7fb81080019aceed0b42f197`: assigned-staff public-response writes now create member-scoped in-app notifications, notification action links target the originating handoff on the existing `/member/help` surface, stale public-response notifications are cleared when handoffs close, and the CRM gate proves notification display, action routing, updates, and close-path suppression while preserving CRM05 advisory behavior and CRM06 member-safe response display. `P32-DG11` is complete as the post-CRM07 closeout and next-slice gate, ranking support-handoff response acknowledgement design above direct acknowledgement implementation, member replies, SLA timers, support-handoff-specific unread counters, new staff routes, and broad CRM abstractions, and promoting exactly one next bounded design-review slice: `P32-DG12 Support Handoff Response Acknowledgement Design Review`. P32 still does not authorize broad CRM redesign, broad SaaS redesign, agent-workspace redesign, PR workflow/documentation work as a product slice, `apps/web/src/proxy.ts` changes, canonical route changes, auth or tenancy refactors outside the approved CRM06 schema extension, Stripe reintroduction, Relationship table, abstract Matter, stored TrustSignal, README, AGENTS, or architecture-doc updates.

`P32-DG12 Support Handoff Response Acknowledgement Design Review` is complete as the implementation-design gate after `P32-DG11`. It decides that generic notification-center `isRead` is insufficient for support-handoff acknowledgement, approves a narrow member-authored acknowledgement of the current public-response version on the existing `/member/help` banner, makes acknowledgement staff-visible and branch-manager read-only on the existing `/staff/support-handoffs` detail panel, authorizes only bounded nullable support-handoff acknowledgement fields for a later implementation, and promotes exactly one bounded implementation slice: `P32-CRM08 Support Handoff Response Acknowledgement`.

`P32-CRM08 Support Handoff Response Acknowledgement` is complete through PR `#646`, merge commit `ef0ca9628ea9ad9a81e9195c5dbb0530f37e1d50`, with follow-up acknowledgement UX fix PR `#647`, merge commit `88aafa06f9488d94c9a8854911e27d454d1cc8ed`. Members can explicitly acknowledge the current staff public-response version from the existing `/member/help` banner, acknowledgement remains distinct from notification-center `isRead`, staff and read-only branch managers can inspect acknowledgement status on the existing `/staff/support-handoffs` detail panel, duplicate acknowledgements are idempotent, stale response versions fail closed, and audit logging records first acknowledgement without raw response text. `P32-DG13` is complete as the post-CRM08 closeout and next-slice gate, ranking member reply semantics design above direct reply implementation, full conversation threads, SLA timers, support-handoff-specific unread counters, new staff routes, and broad CRM abstractions. It promotes exactly one next bounded design-review slice: `P32-DG14 Member Support Handoff Reply Semantics Design Review`. P32 still does not authorize broad CRM redesign, broad SaaS redesign, agent-workspace redesign, PR workflow/documentation work as a product slice, `apps/web/src/proxy.ts` changes, canonical route changes, auth or tenancy refactors outside approved bounded schema extensions, Stripe reintroduction, Relationship table, abstract Matter, stored TrustSignal, README, AGENTS, or architecture-doc updates.

`P32-DG14 Member Support Handoff Reply Semantics Design Review` is complete as the implementation-design gate after `P32-DG13`. It decides that member replies belong in the V3 pilot support-handoff model as one optional scalar follow-up per response cycle, stored as three nullable fields on `support_handoffs`, gated on active status, an existing public response, and acknowledgement of the current response version. The scalar design intentionally does not retain durable earlier-cycle reply history; a later reply for a newer response cycle overwrites the scalar fields, while full reply history or conversation threading stays deferred. CRM09 must align with existing Drizzle timestamp and length-check conventions, reject overlong replies explicitly, expose typed staff detail reply fields, preserve acknowledgement-style CAS fallback idempotency, and show a non-blocking staff warning when the current cycle has a member reply. Staff and branch managers read replies only in the existing `/staff/support-handoffs` detail panel, notifications and queue counters are deferred, audit logs store structural metadata only, and the gate promotes exactly one bounded implementation slice: `P32-CRM09 Member Support Handoff Reply`. P32 still does not authorize broad CRM redesign, broad SaaS redesign, agent-workspace redesign, PR workflow/documentation work as a product slice, `apps/web/src/proxy.ts` changes, canonical route changes, auth or tenancy refactors outside approved bounded schema extensions, Stripe reintroduction, Relationship table, abstract Matter, stored TrustSignal, README, AGENTS, or architecture-doc updates.

`P32-CRM09 Member Support Handoff Reply` is complete through PR `#650`, merge commit `9c1afed18745158cc03adcdbee5dac128a35a8f1`. CRM09 added member-authored replies for the current acknowledged support-handoff public-response cycle on the existing `/member/help` surface, persisted bounded nullable scalar reply fields on `support_handoffs` with a database length check, exposed read-only current-cycle reply state in the existing staff support-handoff detail panel, added staff warning copy, covered the second response/reply cycle in the focused gate, and shipped supported-locale i18n. It preserved the DG14 non-goals: no durable reply history, conversation threads, attachments, staff notifications, unread counters, SLA timers, queue badges, new routes, proxy changes, canonical route changes, auth or tenancy architecture changes, Stripe, Relationship, Matter, TrustSignal, README, AGENTS, or architecture-doc changes.

`P33-DG01 Production Maturity 10/10 Roadmap` is complete as a docs-only maturity roadmap overlay after the updated production-professionalism re-review. It records that `P17` closed the application-layer gaps from the April 25 review, that the remaining path to `10/10` is categorical maturity work, and that the roadmap does not reopen or supersede the completed `P32-CRM09` product implementation. It ranked `P33-SEC01 RLS Connection Role Startup Assertion` as the smallest optional post-CRM09 hardening slice; `P33-SEC01` is now complete through PR `#664`, merge commit `b3b02ca5ab56b22d28d078a46a568ea890cf375d`, adding the fail-closed RLS connection role assertion while preserving proxy, canonical route, product UX, support-handoff, auth and tenancy architecture, schema, Stripe, README, AGENTS, and architecture-doc boundaries. CSP nonce Phase 0, `withTenantContext` build-guard design, storage RLS backstop design, supply-chain attestation, restore drills, threat modeling, incident drills, data lifecycle automation, performance budgets, and repo hygiene remain deferred into later bounded category gates.

`P33-DG02 CSP Nonce Phase 0 Selection` is complete as the bounded post-SEC01 maturity selection gate. It records that SEC01 closed the first connection-layer tenant-defense assumption and promotes exactly one next implementation slice, `P33-SEC02 CSP Nonce Phase 0 Report-Only`, because the approved CSP nonce design and current proxy-owned CSP implementation make a reversible report-only nonce rollout the smallest high-value browser-edge hardening step. The gate keeps CSP enforcement, style nonce enforcement, durable CSP violation storage, analytics rewrites, `withTenantContext` build guards, storage RLS backstops, supply-chain attestation, restore drills, threat modeling, incident drills, data lifecycle automation, performance budgets, and repo hygiene deferred into later bounded gates. It does not authorize `apps/web/src/proxy.ts` edits, canonical route changes, auth or tenancy architecture changes, schema changes, Stripe, product UX changes, README, AGENTS, or architecture-doc updates.

`P33-SEC02 CSP Nonce Phase 0 Report-Only` is complete through PR `#676`, merge commit `7c7f193f`. SEC02 added feature-flagged report-only nonce CSP observation through the existing security-header/proxy-logic path, preserved the current enforced CSP in default/off mode, added a privacy-minimized `/api/csp-report` ingestion route with Sentry warning capture, propagated `x-nonce` only when nonce mode is active, extended header E2E coverage, and stamped standalone builds by `CSP_NONCE_MODE`. The off-mode rollback path is pinned by focused CSP nonce and proxy tests. Browser smoke found first-party `script-src` report-only violations, so CSP Phase 1 enforcement remains blocked until the first-party script nonce coverage gap is diagnosed and fixed.

`P33-DG03 Post-SEC02 Next Slice Selection` is complete as the post-SEC02 closeout and next-slice gate. It records that Phase 1 enforcement is not promotable from current SEC02 evidence, appends a hypothesis-driven first-party script nonce coverage investigation protocol to `docs/security/csp-nonce-migration.md`, defines the first/third/extension/noise taxonomy, locks a six-part numerical Phase 1 promotion bar, and promotes exactly one next bounded design gate: `P33-DG04 CSP First-Party Script Nonce Coverage Design Review`. The gate also records that after DG04 lands, DG04's resulting CSP fix slice and the `withTenantContext` build-guard design track may run in parallel because they touch disjoint maturity categories and should not be serialized unnecessarily.

`P33-DG04 CSP First-Party Script Nonce Coverage Design Review` is complete as the post-DG03 hypothesis gate. It confirms H1: first-party Next framework/static chunk and runtime inline scripts are still rendered without nonce attributes in report mode even though the Report-Only header contains a nonce and `strict-dynamic`, while the `data-csp-nonce-probe` canary proves hand-authored nonce propagation is not globally broken on representative public flows. H2 is eliminated for repo-owned analytics scripts, H3/H4 remain unconfirmed until post-H1 smoke, and H5 extension noise was not observed locally. DG04 keeps CSP Phase 1 enforcement blocked and promotes exactly one bounded implementation slice: `P33-SEC03 First-Party Script Nonce Coverage`. The `withTenantContext` build-guard design track remains parallelizable after DG04 lands.

`P33-DG05 withTenantContext Build Guard Design Review` is complete as the parallel post-DG04 tenancy guard gate. It records that the existing DB access guard protects against new direct `db.*` callsites but does not classify tenant-context posture, and originally promoted `P33-SEC04 withTenantContext Build Guard Baseline`. DG05 is amended after implementation measurement: `P33-SEC04A DB Access Posture Baseline` landed the first guard/reporting baseline with `262` known `unclassified` existing entries, and `P33-SEC04B DB Access Posture Burn-Down` is complete through PR `#686`, merge commit `a3e620ea0a1234a4bee35bfe2bb5cd8df7bf4fcf`, reducing the count to the target ceiling of `80` through reviewed `tenant-scoped` and `system-exempt` directives plus removal of e2e-only setup routes from the production posture scan. The remaining `80` entries are intentionally left as hard cases for targeted migration or future design review rather than mass-stamped. `P33-SEC03` is now `blocked-by-architecture` after the designed report-mode CSP framework-script nonce stop and SEC05 triage. `P33-SEC05 Next CSP Nonce Propagation Triage` classified the blocker as a report-only architecture mismatch with the current two-header Phase 0 model: documented Next nonce propagation works when a nonce-bearing `Content-Security-Policy` is the effective header, but fails when the existing non-nonce enforced CSP header is present beside nonce Report-Only. `P33-DG07 CSP Nonce Migration Architecture Rebaseline` is complete as the post-SEC05 decision gate: it keeps CSP Phase 1 enforcement blocked, sets Phase 0 production posture to `CSP_NONCE_MODE=off` by default with report mode available only for bounded diagnostics, records the unlock/review cadence in `docs/security/csp-nonce-migration.md`, and promoted `P33-DG08 Storage RLS Backstop Design Review` as the next bounded P33 design gate. `P33-DG08` is complete through PR `#690`, merge commit `ba6c862ed0b8a6ac5a40a2ae256af6b1d477e038`; it records the Storage RLS/service-role backstop design, adds `docs/security/storage-access-baseline.md` with the bucket, policy, tenant-prefix path, service-role callsite, and signed URL risk inventory, and promotes exactly one bounded implementation slice: `P33-SEC06 Storage Tenant-Prefix Backstop`. `P33-SEC06` is complete as the implementation slice: it adds tenant-prefix Storage RLS with a legacy-object preflight, centralizes all baseline service-role Supabase Storage callsites behind path-asserting wrappers, wires a security guard against new direct service-role Storage access, and records signed URL residual risk in `docs/security/storage-access-baseline.md`. `P33-DG09 Post-SEC06 Security Slice Selection` records that the DG07 CSP blocker has not changed on resolved Next `16.2.4`, keeps SEC03/SEC03R and CSP Phase 1 enforcement blocked, ranks signed URL exposure hardening above CSP re-architecture, Vercel TypeScript build suppression cleanup, CI seed credential hardening, DB posture hard-case burn-down, and operational maturity candidates, and promotes exactly one bounded implementation slice: `P33-SEC07 Signed URL Exposure Hardening`. `P33-SEC07` is complete through PR `#695`, merge commit `6d2f030f521525929a63f9789d68cba080f0044a`; it materially reduces signed URL bearer-exfiltration risk by centralizing signed URL no-store/no-referrer response behavior, preserving documented download TTL defaults, bounding the voice-note preview TTL exception, redacting signed URL tokens from errors/logging, adding focused route/storage/guard tests, and wiring `scripts/check-signed-url-exposure.mjs` into `pnpm security:guard`. `P33-DG10 Post-SEC07 Security Slice Selection` records SEC07 closeout, confirms that the DG07 CSP blocker remains unchanged on resolved Next `16.2.4`, keeps SEC03/SEC03R and CSP Phase 1 enforcement non-promotable, ranks Vercel TypeScript build suppression cleanup first because `apps/web/next.config.mjs` still sets `typescript.ignoreBuildErrors` for Vercel builds, and promotes exactly one bounded implementation slice: `P33-SEC08 Vercel TypeScript Build Suppression Cleanup`. CSP Phase 1 enforcement remains blocked until a later gate records one of DG07's unlock conditions and the CSP migration promotion bar passes, and full hardened-production or `9+/10` posture remains blocked until remaining production-professionalism residual categories are fixed or formally accepted.

`P33-SEC08 Vercel TypeScript Build Suppression Cleanup` is complete through PR `#698`, merge commit `366741dda756501220ba3cbd07506dff68465a49`; it removed the Vercel-only Next TypeScript build suppression from `apps/web/next.config.mjs`, added the `Next TypeScript build integrity guard`, wired that guard into `pnpm security:guard`, and proved local `VERCEL=1` builds fail on a synthetic TypeScript error after suppression removal. Hosted Vercel deployment proof remains impractical because the repository's Vercel check is skipped by the configured ignored-build step. `P33-DG11 Post-SEC08 Security Slice Selection` records SEC08 closeout, confirms resolved Next remains `16.2.4`, keeps the DG07 CSP blocker active because current Next guidance still requires a nonce-bearing `Content-Security-Policy` plus `x-nonce`, rejects direct credential implementation before design review, and promotes exactly one bounded next design-gate slice: `P33-DG12 CI/Release Seed Credential Hardening Design Review`.

`P33-DG12 CI/Release Seed Credential Hardening Design Review` is complete as the credential-hardening design gate. It inventories CI, release, seed, E2E, and pilot-gate credential surfaces; preserves loopback-only local/ephemeral CI database fallbacks and GitHub-secret-backed staging/production release-gate credentials; classifies deterministic seeded-user passwords as acceptable local fixtures only; classifies workflow literal `GoldenPass123!` release/pilot role passwords and `E2E_API_SECRET: test-secret-placeholder` as unsafe shared workflow values; rejects requiring GitHub secrets for all PR/nightly seeded E2E because that would break deterministic verification; and promotes exactly one bounded implementation slice: `P33-SEC09 CI/Release Seed Credential Hardening`.

`P33-SEC09 CI/Release Seed Credential Hardening` is complete as the DG12 implementation slice through PR `#701`. It adds a canonical effective E2E seed-password resolver that preserves the deterministic local default while accepting generated CI overrides, moves KS workflow-pack seeded credentials onto the same effective password path, exports masked per-run E2E credentials in seeded CI/release/pilot workflows before seed/auth/release-gate work, removes workflow-level `GoldenPass123!` release-gate password literals and `E2E_API_SECRET: test-secret-placeholder`, keeps CD staging/production release-gate credentials secret-backed, and wires the `Workflow seed credential guard` into `pnpm security:guard` plus CI contract tests. Proxy, canonical routes, auth provider/session shape, tenancy architecture, schema, Storage redesign, Stripe, README, AGENTS, and architecture docs remain unchanged.

`P33-DG13 Post-SEC09 Security Slice Selection` is complete as the docs-only decision gate through PR `#702`. It records SEC09 closeout after PR `#701`, merge commit `4c919c2fa4d33ffe7aea67b2b27e29568f1940a7`; preserves the DG07 CSP blocker because no concrete unlock condition has changed; keeps SEC03/SEC03R and CSP Phase 1 enforcement non-promotable; rejects direct DB posture implementation because SEC04B intentionally left the remaining `80` unclassified entries as hard cases; and promotes exactly one bounded design-gate slice: `P33-DG14 DB Access Posture Hard-Case Design Review`.

`P33-DG14 DB Access Posture Hard-Case Design Review` is complete as the DB posture hard-case design gate through PR `#703`. It inventories all seven SEC04B residual clusters, keeps the `37` one-off paths out of scope because they are not coherent enough for a single safe implementation slice, rejects a full `80`-entry burn-down because SEC04B explicitly prohibited mass-stamping, ranks the billing webhook/provider-event tenant-resolution cluster first because it has `14` entries in one production-facing domain boundary with existing focused tests, and promotes exactly one bounded implementation slice: `P33-SEC10 Billing Webhook Tenant Resolution Hardening`.

`P33-SEC10 Billing Webhook Tenant Resolution Hardening` is complete through PR `#704`, merge commit `8c4a4ea0b75527db1485b7ee280ad6b262edbc46`. SEC10 hardens Paddle webhook tenant resolution so canonical subscription/user state takes precedence over provider `customData`, fails closed before tenant-scoped subscription, dunning, checkout-reconciliation, and transaction-audit writes when canonical tenant/user/subscription mapping is missing or conflicting, preserves invalid-signature and duplicate webhook audit behavior with nullable `tenantId`, fixes the app route resolver to prefer canonical subscription state before `customData.userId` fallback, and reduces the DB access posture baseline to `unclassified=67` with all current `packages/domain-membership-billing/src/paddle-webhooks/**` unclassified entries resolved.

`P33-DG15 Post-SEC10 Security Slice Selection` is complete as the docs-only post-SEC10 gate. It records SEC10 closeout, carries forward the named Paddle lead conversion residual, keeps the remaining DB access posture hard-case clusters out of scope for mass-stamping, and promotes exactly one bounded implementation slice: `P33-SEC11 Paddle Lead Conversion Tenant Guard Hardening`. SEC11 must remove the `tenantId || 'unknown'` fallback in the `transaction.completed` lead conversion branch, validate provider `customData.leadId` before use, reconcile the lead against canonical repo state for the resolved tenant, preserve duplicate/invalid webhook audit behavior, and avoid broad Paddle, schema, Stripe, proxy, auth, tenancy, Storage, README, AGENTS, or architecture-doc changes.

`P33-SEC11 Paddle Lead Conversion Tenant Guard Hardening` is complete through PR `#706`, merge commit `e8b9904bb8700ea48e5dde9ee027354044696afe`. SEC11 removed the Paddle `transaction.completed` `tenantId || 'unknown'` fallback, validates provider `customData.leadId` before use, reconciles lead ownership against canonical repo state for the resolved tenant before conversion, preserves duplicate and invalid-signature webhook audit behavior, keeps SEC10 tenant-resolution precedence intact, and records the post-SEC11 DB posture baseline at `67` unclassified entries. `P33-DG16 Post-SEC11 Security Slice Selection` is complete as the docs-only gate that ranks the five-entry commercial action idempotency hard-case cluster above the remaining DB posture residuals and promotes exactly one bounded implementation slice: `P33-SEC12 Commercial Action Idempotency Tenant Scope Hardening`.

`P33-SEC12 Commercial Action Idempotency Tenant Scope Hardening` is complete through PR `#708`, merge commit `87dbead35e2c436ae695518109e09994511dac75`. SEC12 made commercial idempotency scope explicit, requires canonical tenant scope for tenant-bound commercial actions before reservation writes or cached response release, keeps public idempotency limited to explicit public flows, fails closed on cross-tenant key conflicts, and records the post-SEC12 DB access posture baseline at `67` to `62` unclassified entries.

`P34` Domain CRM Production Roadmap is complete as the post-`P33-SEC12` CRM product line. `P34` resumed from completed `P32-CRM09 Member Support Handoff Reply` and records that new CRM behavior must go through a small `packages/domain-crm` boundary rather than adding more route-local CRM logic. `P34-CRM-DG01 Domain CRM Contract` is complete on branch `codex/p34-crm-dg01-domain-contract`: it creates only the domain package skeleton and freezes the actor/tenant context contract, support-handoff aggregate/state machine, `STAFF_OWNED_STATES`, terminal `staff_followed_up` bounded reply-cycle decision, repository interface, parameter-injected notification port, read-only timeline invariant, dependency direction, and pure transition-helper tests before product implementation begins. `P34-CRM10 Staff Follow-Up After Member Reply` is complete on branch `codex/p34-crm10-staff-follow-up`: the existing `/staff/support-handoffs` surface now exposes needs-follow-up attention filtering for current-cycle member replies, lets staff send a follow-up that clears that attention state, preserves the bounded one-cycle member-reply model after staff follow-up, and adds same-PR authorization/side-effect regression proof. `P34-CRM11 Unified CRM Timeline Read Model` is complete on branch `codex/p34-crm11-unified-crm-timeline`: `domain-crm` now owns a derived/read-only timeline read model for lead activity and support-handoff response, acknowledgement, member-reply, and staff-follow-up events, with explicit tenant and actor-scope read authorization and no writer migration. `P34-CRM12 Agent Follow-Up Workflow` is complete on branch `codex/p34-crm12-agent-follow-up`: agent lead follow-up scheduling and completion now route through `domain-crm`, existing `/agent/crm` surfaces due follow-ups, existing `/agent/leads/[id]` renders the next follow-up action, and negative tests prove tenant, branch, and agent ownership failures suppress writes. `P34-DG02 CRM E2E Gate Coverage Selection` is complete on branch `codex/p34-crm13-agent-crm-follow-up-gate`: it records that support-handoff follow-up already has gate coverage and promotes exactly one remaining readiness slice, `P34-CRM13 CRM E2E Gate Coverage`. `P34-CRM13` is complete on the same branch: the blocking PR gate suite now has focused agent CRM follow-up coverage for scheduling a lead follow-up, surfacing it as due work, excluding future and other-tenant due work, opening it from `/agent/crm`, completing it, proving the due queue clears, and wiring default gate specs into `gate-mk-contract`. `P34-DG03 Post-CRM13 CRM Production Readiness Selection` promoted exactly one final readiness slice, `P34-CRM14 Agent Lead Mutation Domain Boundary`, to move create-lead, update-lead-stage, and record-lead-activity writes behind `domain-crm` while naming remaining direct-DB read-model and `memberLeads` exceptions. `P34-CRM14` is complete through PR `#715`, merge commit `0a3af52325e38341a383925bbf69b45d55ea5bd4`: create lead, update lead stage, and record lead activity now delegate through explicit `domain-crm` functions with tenant, role, branch, and agent ownership authorization, thin route-facing `.core.ts` wrappers, adapter-owned tenant-scoped DB writes, and tests proving failed authorization suppresses side effects. `P34-DG04 Post-CRM14 CRM Production Readiness Closeout` closes the roadmap without promoting another P34 implementation slice or opening a P35 designation because the remaining CRM direct-DB exceptions are classified as allowed `domain-crm` infrastructure adapters, tenant/actor-scoped read models, out-of-P34 `memberLeads` surfaces, or a legacy `domain-activities` writer that predates P34 and does not mutate agent CRM lead state. `P34` does not authorize broad CRM redesign, broad SaaS redesign, agent-workspace redesign, proxy edits, canonical route changes, auth or tenancy refactors, Stripe reintroduction, full conversation threads, attachments, SLA timers, campaign automation, cron/NPS tenant-job architecture, or a broad historical DB baseline burn-down.

`P35 DB Access Posture Residual Hardening` is complete as the post-`P34` security/posture tranche. `P35-DG01 Post-P34 Next Tranche Selection` inspected repo-canonical program/tracker state, Notion sync, recent PR history, and the current DB access posture guard evidence, then promoted exactly one bounded implementation slice: `P35-SEC01 Commission Tenant Proof Contract`. SEC01 is complete through PR `#719`, merge commit `ebe91632c9feb058edf9c3e33564b2d66b0ad389`: the commission creation core no longer performs the non-Paddle fallback `agentId` to `tenantId` lookup, requires explicit non-empty tenant scope before idempotency checks or writes, preserves tenant-scoped webhook and renewal callers, passes session-derived tenant scope from the app-facing wrapper, and records the reviewed DB posture movement from `unclassified=62` to `unclassified=61` without refreshing unrelated P34 CRM adapter drift. `P35-DG02 Agent Dashboard Tenant Read Contract` promoted `P35-SEC02` for the five-entry legacy agent-dashboard claim read cluster. SEC02 is complete on `main` at commit `781de6d19a3eb6408fd75746c466fcf76a51c8de`: staff/admin dashboard reads now require session-derived tenant scope before any DB reads, all five claim reads carry direct `claims.tenantId` predicates, existing agent empty-state behavior is preserved, and the reviewed DB posture baseline moves from `unclassified=61` to `unclassified=56` without refreshing unrelated P34 CRM adapter drift. P35 did not authorize broad DB baseline burn-down, mass-stamping guard directives, proxy edits, canonical route changes, auth or tenancy refactors, schema/migration work, Storage redesign, Stripe, README, AGENTS, architecture-doc changes, or unrelated CRM continuation.

`P36 CRM Data-Model Hardening` is complete after completed `P35`. `P36-DG01 CRM Data-Model Hardening Re-Review` records the CRM re-review finding that `crm_leads` does not persist durable `branch_id`, while the app adapter reconstructs lead branch ownership from the assigned agent's current branch and domain authorization trusts that reconstructed value. DG01 promoted exactly one bounded implementation slice, `P36-CRM-DM01 Durable CRM Lead Branch Ownership`, which is complete through PR `#722`, merge commit `14bad1bf236e30621abe88659182d3291242f718`: `crm_leads` now has nullable durable `branch_id`, existing rows were backfilled from the assigned agent's current branch with audit evidence, new CRM lead creation persists branch id from `actor.scope.branchId`, lead mutation and follow-up adapters read branch ownership from the CRM lead row instead of current `user.branchId`, `resolveLeadBranch` is removed, missing lead/actor branch scope fails closed, and focused plus real local DB proof confirms agent branch movement does not retroactively change existing CRM lead authorization.

`P36-DG02 Post-DM01 Closeout And Next CRM Data-Model Slice` is complete through PR `#723`, merge commit `9300a57d496ff6fa9aba9ad71438c9981ca1e03d`: it closed the stale PR-pending state and promoted exactly one next bounded implementation slice, `P36-CRM-DM02 CRM Lead Lifecycle History And Stage Discipline`. DM02 is complete through PR `#724`, merge commit `f374f858ca154707e51f11d560d459a338379fa4`: it adds `crm_lead_stage_history`, RLS-protects the new tenant-scoped table, makes authorized stage transitions append one history row inside the scoped update transaction, suppresses history on same-stage, failed-authorization, and missed-update paths, adds database stage and terminal timestamp constraints for `crm_leads.stage`, keeps `won_at` / `lost_at` tightly tied to terminal `won` / `lost` transitions, and resolved review feedback by making stage-history rows cascade on CRM lead deletion. `P36-DG03 Post-DM02 Closeout And Next CRM Data-Model Slice` closes the stale branch-implemented DM02 state and promotes exactly one next bounded implementation slice, `P36-CRM-DM03 CRM Lead Ownership History And Transfer Discipline`. DM03 is complete through PR `#726`, merge commit `3f03ef158c3e1a1c7bd25051413af5c484fc9b0d`: it adds `crm_lead_ownership_history`, backfills existing durable lead ownership, writes initial ownership rows on CRM lead creation, introduces a domain-owned ownership transfer operation, validates transfer targets against tenant/branch custody, rotates open ownership-history rows in the same transaction as successful transfers, and proves failed authorization does not mutate lead ownership or history.

`P36-DG04 Post-DM03 Closeout And Next CRM Data-Model Slice` closes the stale DM03 branch state and promotes exactly one next bounded implementation slice, `P36-CRM-DM04 CRM Activity Discipline And Dashboard Query Readiness`, covering CRM activity branch snapshots, activity-type DB discipline, dashboard/timeline indexes, and a tightly scoped future-write tenant/lead invariant. DM04 is complete through PR `#727`, merge commit `47c76e08`: `crm_activities` now carries dashboard/timeline query indexes, DB-supported activity-type discipline, immutable nullable branch snapshots backfilled from ownership history or durable lead branch custody, and a bounded tenant/lead future-write invariant. `P36-DG05 Post-DM04 Closeout And Next CRM Slice Selection` closes the stale DM04 in-progress state and promotes exactly one next bounded implementation slice, `P36-CRM-DM05 CRM Dashboard Read-Side Domain Port`, to move existing agent CRM dashboard reads behind `domain-crm` while preserving dashboard output behavior. DM05 is complete through PR `#733`, merge commit `c771307684850368909ef21d5a6cfa0a565b09ad`: the existing agent CRM dashboard now reads through `domain-crm/dashboards` with tenant, agent, and branch scope carried through `CrmActorContext`, preserves dashboard output, resolves Copilot and Sonar findings, and leaves only the app-side domain adapter as the dashboard DB boundary.

`P36-DG06 Post-DM05 Closeout And Next CRM Slice Selection` closes the stale DM05 in-progress state and promotes exactly one next bounded implementation slice, `P36-CRM-DM06 CRM Lead Detail Read-Side Domain Port`, to move existing agent lead-detail lead/deal reads behind `domain-crm` while preserving lead-detail output behavior. DM06 is complete through PR `#735`, merge commit `cfcc14c74ed66a26658340494a50cf3ce9b0a78a`: the existing agent lead-detail lead/deal read path now delegates through `domain-crm/lead-details`, preserves lead-detail UI/output behavior, and enforces tenant, agent, and branch scope through `CrmActorContext` before deal data is returned. `P36-DG07 Post-DM06 Closeout And Next CRM Slice Selection` closes the stale DM06 in-progress state and promotes exactly one next bounded implementation slice, `P36-CRM-DM07 CRM Lead Activity Timeline Read-Side Domain Port`, to move the existing agent lead-detail activity feed read behind `domain-crm` while preserving the current ActivityFeed and follow-up output behavior. DM07 is complete through PR `#737`, merge commit `c2d904b8a5b8cf71fe15757cadbb7dc430ca8154`: the existing agent lead-detail activity-feed read now delegates through `domain-crm/lead-activities`, preserves the existing activity feed and follow-up next-action output, enforces tenant, agent, and branch scope through `CrmActorContext`, and leaves the app-side adapter as the CRM lead-activity DB boundary. `P36-DG08 Post-DM07 Closeout And AI Production Readiness Selection` closes P36 after the final promoted CRM read-side port and promotes exactly one next bounded design-gate slice: `P37-AI-DG01 AI Production Readiness Gate And Eval Contract`. P36 did not authorize proxy edits, canonical route changes, auth or tenancy refactors, Stripe, README, AGENTS, broad architecture docs, pipeline UI, task aggregate work, automation, AI runtime work, campaign, cron/NPS architecture, `member_leads` unification, timeline UI rewrites, dashboard analytics expansion, or broad DB posture burn-down.

`P37 AI Production Readiness` is closed after P36 completes the CRM data-model/read-side hardening prerequisite and P37 completes the AI readiness contract, strict policy extraction output, and formal AI eval release gate sequence. `P37-AI-DG01 AI Production Readiness Gate And Eval Contract` records the current AI surface inventory, keeps new AI runtime behavior out of scope until the contract exists, defines workflow/model/prompt/schema/eval/guardrail/observability/release requirements for AI-affected slices, and promotes exactly one bounded implementation slice: `P37-AI01 Policy Extract Strict Output Contract And Eval Gate`. `P37-AI01` is complete through PR `#740`, merge commit `a300d3c02fcce2983ce65d58143ee62d85005af9`: policy extraction now emits and persists the strict `policyExtractSchema` output, the policy eval normalization shim is removed, sparse/adversarial no-hallucination fixtures are in place, and `pnpm ai:eval` is proven as blocking local evidence while the existing queued policy upload/background workflow is preserved. `P37-DG02 Post-AI01 Closeout And Next AI Slice Selection` closes the stale AI01 in-progress state, ranks the remaining AI-readiness candidates, and promotes exactly one next bounded implementation slice: `P37-AI02 Formal AI Eval CI And Release Gate`. `P37-AI02` is the last completed marker for this tranche through PR `#742`, merge commit `ad4406cd44da011d7d9045c8ba7614e6eb645743`: it wires `pnpm ai:eval` into CI as a blocking AI-surface/mainline lane, adds release-candidate command-pack proof, and guards the workflow contract against removal or downgrade while keeping the deterministic fixture suite network-free. `docs/plans/2026-05-13-p37-ai-production-readiness-closeout.md` records the closeout decision: "AI readiness is sufficiently closed for the purposes of opening Dashboard Professionalism. Remaining P37-\* items, if any, are deferred and tracked separately." P37 does not authorize new model calls, prompt rewrites, RAG, assistants, agentic workflows, broad UX redesign, data-retention changes, proxy edits, auth/tenancy architecture changes, Stripe, README, AGENTS, broad architecture-doc rewrites, CRM continuation, task aggregate work, automation, campaigns, cron/NPS architecture, or `member_leads` unification outside separately tracked future work.

`P38 CRM State-Of-The-Art Foundations` is now complete after completed `P37`. `P38-DG01` is recorded in `docs/plans/2026-05-13-p38-crm-state-of-art-roadmap.md` as the repo-canonical roadmap for advancing `packages/domain-crm` toward professional dashboard credibility without regressing the existing hexagonal boundary, typed actor context, append-only history, support-handoff lifecycle, read/write separation, timeline projection, or follow-up next-action derivation. It ranks the first shippable sequence as outbox, accounts/contacts/lead conversion, configurable pipelines/deals, pipeline/deal persistence, reporting/forecast snapshots, lead dedupe, routing, tasks/templates/sequences, activity channel specializations, scoring, and consent. `P38-CRM01 CRM Domain Outbox Contract` is complete through PR `#744`, merge commit `2741c45cdc8c056ac4c6740c69ea82e843fc6051`: it adds a domain-only outbox contract, typed CRM domain events, deterministic clock/ID event-data builders, enqueue helpers, and in-memory proof without adding schema, workers, route behavior, UI, notifications, AI runtime behavior, or external sync. Required-gate triage also fixed existing web-flow blockers in agent dashboard reads and member support-handoff form navigation so the full pilot gate passes. `P38-DG02 Accounts, Contacts, And Lead Conversion Design` promoted `P38-CRM02 Accounts, Contacts, And Lead Conversion Foundation`, complete through PR `#745`, merge commit `1160d8dd1c6ca5d35329b099bd4dd22ebd432430`: `packages/domain-crm` now exports account, contact, account-contact, and lead-conversion domain modules, repository ports, deterministic clock/ID injection, append-only conversion-history contracts, typed conversion outbox events, and in-memory proof, while schema, SQL adapters, UI, dashboards, and backfill remain deferred to later explicitly promoted slices. `P38-DG03 Pipeline And Deal Aggregate Design` promoted `P38-CRM03 Pipeline And Deal Domain Foundation`, complete through PR `#747`, merge commit `a717a5afaec0064a71309a09d7bc4b94f5549dfd`: `packages/domain-crm` now exports pipeline create/add/update/reorder/archive contracts, deal create/move/win/lose/reopen/archive contracts, atomic deal+stage-history repository ports, append-only stage history, forecast/loss-reason contracts, next-action derivation, and typed deal outbox-event extensions. Copilot review hardening added archived pipeline/reference/deal guards, staff-created deal assignment, blank contact normalization, lost-stage creation rejection, reopen preconditions, no-op move rejection, duration validation reasons, and reorder uniqueness. `P38-DG04 Lead Dedupe And Routing Slice Selection` promoted `P38-CRM06 Lead Dedupe Domain Foundation`, complete through PR `#750`, merge commit `c7412618c9f55adf85a75d8f06d7b5de51961254`: `packages/domain-crm` now exports pure lead identity normalization, pinned match scoring, candidate listing, explicit merge contracts, append-only merge-history output, dedupe-aware aggregate policy, and `crm.lead.merged` outbox support. `P38-DG05 Lead Routing Design After Dedupe Foundation` promoted `P38-CRM07 Lead Routing Domain Foundation`, complete through PR `#751`, merge commit `dce513248cee825ae5e7d616f17c489a80422a1e`: `packages/domain-crm` now exports routing-rule, workload, cursor, assignment-decision, audit-record, and repository-port contracts; pure `selectCrmLeadAssignee`; typed rejection reasons; explicit dedupe-state eligibility; advisory cursor advancement; and `crm.lead.routed` outbox support. `P38-DG06 Pipeline And Deal Persistence Design Review` closes CRM07 and promotes the reserved `P38-CRM04 Pipeline And Deal Persistence` slice because the app still persists deals through legacy `crm_deals.stage` text and lacks durable pipelines, stages, deal stage history, and loss reasons. The amended DG06 contract pins nullable-first migration staging, index and invariant enforcement plans, composite tenant FKs, history `kind` and idempotency fields, quarantine for unsafe backfill rows, same-transaction compatibility mirrors, concurrency proof, and later CRM10/CRM11 cleanup/tightening reservations. `P38-CRM04 Pipeline And Deal Persistence` is complete through PR `#754`, merge commit `40a3e15221d473086a8746f5905385d3cf9e4678`: it adds additive pipeline/stage/history/loss-reason/quarantine schema, nullable-first normalized deal columns, checked-in backfill/quarantine/RLS migration steps, app-side SQL adapters for the CRM03 ports, focused adapter/concurrency proof, and same-transaction legacy deal mirrors while keeping UI, proxy, canonical routes, auth/tenancy architecture, reporting, forecast snapshots, and routing persistence unchanged. The numbering correction preserves `P38-CRM05` for reporting/forecast snapshots after persistence, and keeps `P38-CRM08` for routing persistence/cursor adapters plus `P38-CRM09` for routing admin UX/rule management. P38 does not authorize proxy edits, canonical route changes, auth/tenancy architecture changes, Stripe, README, AGENTS, broad architecture-doc rewrites, broad CRM UI redesign, dashboard analytics expansion before accounts/contacts and pipelines, campaign automation, cron/NPS architecture, or `member_leads` unification outside later bounded slices.

`P38-DG07 Reporting And Forecast Snapshot Design Review` closes the post-CRM04 design gap and promotes exactly one next bounded implementation slice: `P38-CRM05 Reporting Read-Models And Forecast Snapshots`. The design authorizes a `packages/domain-crm/src/reporting` namespace with pure funnel conversion, stage velocity, source breakdown, win-rate, leaderboard, weighted-pipeline, and forecast-snapshot derivations; app-side reporting adapters under `apps/web/src/lib/domain-crm`; and additive append-only `crm_pipeline_snapshots` persistence. The amended contract pins period-entry funnel semantics, stage-history-only velocity intervals, open-interval handling, current-probability weighted pipeline, deterministic rounding, snapshot concurrency/versioning, PII-shape regression, and reserved `P38-CRM12 Reporting Dashboard UI` plus `P38-CRM13 Forecast Snapshot Scheduler`. Dashboard UI, scheduled snapshot jobs, proxy, canonical routes, auth/tenancy architecture, routing persistence, routing admin UX, legacy deal column retirement, CRM04 nullability tightening, Stripe, README, AGENTS, and broad architecture docs remain out of scope.

`P38-CRM05 Reporting Read-Models And Forecast Snapshots` is complete through PR `#756`, merge commit `e9cc7eef34c6b1ce29be2aeb492cb5a299fe9190`: it adds `packages/domain-crm/src/reporting`, deterministic reporting derivations, app-side reporting/snapshot adapters, additive `crm_pipeline_snapshots` schema/migration/RLS, PII-shape regression proof, and remote SonarCloud/Copilot/Pilot Gate/unit/E2E proof while preserving dashboard UI, scheduled jobs, proxy, canonical routes, auth/tenancy architecture, routing persistence, routing admin UX, legacy deal column retirement, CRM04 nullability tightening, Stripe, README, AGENTS, and broad architecture docs. `P38-DG08 Post-CRM05 Closeout And Next Slice Selection` promoted exactly one bounded implementation slice: `P38-CRM12 Reporting Dashboard UI`, because CRM05 created the reporting read-model foundation and the next credibility unlock was consuming it on the existing `/agent/crm` dashboard. The amended DG08 contract incorporates the implementation-review gaps by hard-scoping CRM12 to three chart-free agent widgets, weighted pipeline by currency, source breakdown, and win rate by source, requiring CRM05 reporting repository/domain read-model consumption, localized empty-state copy, fail-closed denial behavior, widget markers, multi-currency display without conversion, PII-safe aggregate output, and focused unit proof. `P38-CRM12` is complete through PR `#757`, merge commit `3a220ea88eb0c2b765b56d3d926edd21952b3def`: it adds the first chart-free `/agent/crm` reporting widgets, preserves `agent-crm-page-ready`, keeps output aggregate-only, addresses Copilot review feedback, passes remote SonarCloud/Copilot/Pilot Gate/unit/E2E proof, and records Notion sync under `PR 757 P38 CRM12 Reporting Dashboard UI`. `/admin/crm`, `/staff/crm`, charting-library introduction, visual regression snapshots, forecast snapshot scheduling, proxy, canonical route, auth/tenancy architecture, schema, Stripe, README, AGENTS, and broad architecture-doc changes remain out of scope. `P38-CRM13 Forecast Snapshot Scheduler`, `P38-CRM14 Admin Reporting Dashboard UI`, `P38-CRM15 Staff Reporting Dashboard UI`, `P38-CRM16 Reporting Charting Foundation`, `P38-CRM08 Routing Persistence And Cursor Adapter`, `P38-CRM09 Routing Admin UX And Rule Management`, `P38-CRM10 Legacy Deal Column Retirement`, and `P38-CRM11 Deal Nullability Tightening` were left for later gates.

`P38-DG09 Forecast Snapshot Scheduler Design Review` closes the post-CRM12 slice-selection gap and promotes exactly one bounded implementation slice: `P38-CRM13 Forecast Snapshot Scheduler`. The design authorizes a protected `GET /api/cron/crm/forecast-snapshots` route using the existing `Authorization: Bearer $CRON_SECRET` cron helper, a thin route/core split, CRM05 reporting derivation and append-only snapshot repository composition, previous-UTC-day default snapshot dates, authorized ISO date overrides, shared `sourceRunId` generation, row-level idempotency-key population, aggregate-only PII-safe output, partial-failure accounting, and focused route/core/repository proof. Dashboard UI, admin/staff reporting UI, charting, schema/migrations, run-ledger tables, historical backfill, tenant-local timezone snapshots, proxy, canonical route, auth/tenancy architecture, Stripe, README, AGENTS, and broad architecture-doc changes remain out of scope. `P38-CRM14 Admin Reporting Dashboard UI`, `P38-CRM15 Staff Reporting Dashboard UI`, `P38-CRM16 Reporting Charting Foundation`, `P38-CRM17 Forecast Snapshot Backfill`, `P38-CRM18 Forecast Snapshot Observability`, `P38-CRM08 Routing Persistence And Cursor Adapter`, `P38-CRM09 Routing Admin UX And Rule Management`, `P38-CRM10 Legacy Deal Column Retirement`, and `P38-CRM11 Deal Nullability Tightening` remain reserved for later gates.

`P38-CRM13 Forecast Snapshot Scheduler` is complete through PR `#759`, merge commit `5544ffdea752031086a5bf5cda8b5892af6e3a83`: it adds the protected cron route, scheduler core, normalized work-item projection helper, and app-level Vercel cron entry; hardens soft-timeout, deferred-work, structured logging, version-conflict, and PII-safe aggregate response behavior after Copilot review; passes remote SonarCloud/Copilot/Pilot Gate/unit/E2E proof; updates the repo-size budget required by CI audit; and records Notion sync under `PR 759 P38 CRM13 Forecast Snapshot Scheduler`. Dashboard UI, schema/migrations, run-ledger persistence, proxy, canonical routes, auth/tenancy architecture, Stripe, README, AGENTS, and broad architecture-doc boundaries remained out of scope.

`P38-DG10 Admin Reporting Dashboard UI Design Review` closes CRM13 after PR `#759` and promotes exactly one bounded next implementation slice, `P38-CRM14 Admin Reporting Dashboard UI`: a chart-free `/admin/crm` aggregate reporting surface that consumes CRM05 reporting and snapshot repositories, exposes `admin-crm-page-ready` plus reserved `admin-crm-reporting-*` widget markers, keeps multi-currency metrics grouped without conversion, and remains PII-safe. The amended contract pins typed widget output shapes, a 90-day default live reporting window, source top-10 cap and sort order, snapshot freshness thresholds, mandatory admin-sidebar discoverability, three-layer authorization, denial-to-message-key mapping, and a colocated PII regression test. It does not authorize `/staff/crm`, branch-manager reporting, charting dependencies, schema/migrations, scheduler changes, run-ledger persistence, historical backfill, observability integrations, routing persistence, cleanup/tightening gates, proxy edits, canonical route changes, auth/tenancy architecture changes, Stripe, README, AGENTS, or broad architecture-doc changes.

`P38-CRM14 Admin Reporting Dashboard UI` is complete through PR `#761`, merge commit `de4e60c4ed79d81a6f92e721a4ad04ab1944151b`: it adds a chart-free `/admin/crm` route/core split that reuses CRM05 reporting and forecast-snapshot repositories, maps admin-like session roles into the CRM `admin` actor for tenant-wide aggregates, denies branch-manager/staff/agent/member access before repository reads, renders latest forecast snapshot, branch pipeline, and source-breakdown widgets with the DG10 markers, adds the mandatory admin sidebar entry, localizes the new surface for `sq`, `en`, `sr`, and `mk`, updates the repo-size budget for the committed CRM14 file-count delta required by CI audit, and hardens Copilot review findings by forwarding real reporting-denial reasons and failing closed for tenantless admin-like sessions. Local and remote proof passed: focused route/sidebar tests, page markers, PII regression, i18n completeness/purity, DB-access guard, protected-route browser smoke, static and required-gates slice verification, SonarCloud, Copilot/pr-finalizer, Pilot Gate, unit, static, E2E, e2e-gate, audit, gitleaks, pnpm-audit, commitlint, validation-surface, and Vercel ignored-build proof. Notion sync was recorded under `PR 761 P38 CRM14 Admin Reporting Dashboard UI`. `/staff/crm`, branch-manager reporting, charting, schema/migrations, scheduler behavior, proxy, canonical routes, auth/tenancy architecture, Stripe, README, AGENTS, and broad architecture docs remained out of scope.

`P38-DG11 Staff Reporting Dashboard UI Design Review` closes CRM14 after PR `#761` and closeout sync PR `#762`, and promotes exactly one bounded next implementation slice, `P38-CRM15 Staff Reporting Dashboard UI`: a chart-free `/staff/crm` operational reporting surface for staff-role users only. The design pins three widgets, pipeline workload, funnel movement, and stage velocity, all consuming CRM05 reporting repositories/domain read-models, a 90-day default live window, output sort/cap constants including post-currency pipeline caps and post-dedup funnel caps, explicit `mode: 'period_entry'` funnel use, stage-velocity sample-count filtering, `staff-crm-page-ready` plus derived `staff-crm-reporting-*` markers, staff-sidebar discoverability gated to staff-role users, explicit branch-manager denial, denial-to-message-key mapping, label fallback/no-branch rendering, and colocated PII regression proof. It does not authorize branch-manager reporting, `/admin/crm` changes, charting dependencies, schema/migrations, scheduler changes, run-ledger persistence, historical backfill, observability integrations, routing persistence, cleanup/tightening gates, proxy edits, canonical route changes, auth/tenancy architecture changes, Stripe, README, AGENTS, or broad architecture-doc changes.

`P38-CRM15 Staff Reporting Dashboard UI` is complete through PR `#764`, merge commit `7315b1b1548abb156f91a16cdb0a1dce7c863bff`: it adds the chart-free `/staff/crm` operational reporting route with pipeline workload, funnel movement, and stage velocity widgets, consumes CRM05 reporting repositories/domain read-models through the existing app-side `domain-crm` boundary, preserves aggregate-only PII-safe output, exports the DG11 constants and typed shapes, renders `staff-crm-page-ready` plus derived `staff-crm-reporting-*` markers, applies deterministic staff sort/cap semantics after local pipeline workload aggregation, localizes `staff-crm` copy for `sq`, `en`, `sr`, and `mk`, and gates staff-sidebar `/staff/crm` visibility to staff-role users only. Local and remote proof passed: focused route/sidebar/domain/PII tests, web type-check, focused lint, i18n completeness and purity, DB-access guard, plan/tracker/docs audits, repo-size check, `git diff --check`, Playwright MCP protected-route smoke, `interdomestik_qa.scope_audit`, static slice verification, `pr:verify`, standalone `security:guard`, standalone `e2e:gate`, SonarCloud, Copilot/pr-finalizer, Pilot Gate, unit, static, E2E, e2e-gate, audit, gitleaks, pnpm-audit, commitlint, validation-surface, and Notion sync under `PR 764 P38 CRM15 Staff Reporting Dashboard UI`. Vercel deployment was queued and non-required at merge time. Branch-manager reporting, `/admin/crm`, `/agent/crm`, member UI, charting, schema/migrations/RLS, scheduler behavior, run-ledger persistence, backfill, observability, proxy, canonical routes, auth/tenancy architecture, Stripe, README, AGENTS, and broad architecture docs remained out of scope.

`P38-DG12 Reporting Charting Foundation Design Review` closes CRM15 after PR `#764` and closeout sync PR `#765`, and promotes exactly one bounded next implementation slice, `P38-CRM16 Reporting Charting Foundation`: a shared CRM reporting chart contract that reuses the already-installed `recharts` `^3.8.1` package, adds client-only accessible supplemental chart renderers over existing aggregate route-core outputs, preserves table/metric fallbacks as the source of truth, keeps existing page-ready markers independent from chart paint, keeps multi-currency values grouped by ISO code without conversion, requires deterministic bounded screen-reader summaries and PII-safe chart labels, pins named-import and shared-component-boundary discipline, and caps newly affected route chunks at `30 kB` gzip delta. It does not authorize new charting dependencies or version bumps, branch-manager reporting, backfill, observability, schema/migrations/RLS, scheduler changes, run-ledger persistence, routing persistence, cleanup/tightening gates, proxy edits, canonical route changes, auth/tenancy architecture changes, Stripe, README, AGENTS, or broad architecture-doc changes. `P38-CRM21 Visual Regression Baseline` is reserved for later visual-proof infrastructure.

`P38-CRM16 Reporting Charting Foundation` is complete through PR `#767`, merge commit `c73ab4a8df98bdd6237110bbb7fb466634839ca9`: it adds a shared client-only CRM reporting chart contract under `apps/web/src/components/crm/charts`, preserves the existing chart-free tables and metric bands as the accessible source of truth, renders the required pipeline amount chart on `/agent/crm`, `/admin/crm`, and `/staff/crm`, and renders the DG12 staff-only funnel movement and stage velocity charts on `/staff/crm`. The implementation derives chart rows only from existing CRM12/CRM14/CRM15 aggregate page outputs, keeps page-ready markers independent from chart paint, disables animations and tooltips, uses a small native SVG renderer after PR review proved the `recharts` route payload exceeded the exported `30 kB` gzip budget, keeps the existing `recharts` dependency unchanged without importing it into CRM reporting chunks, exports the DG12 marker/cap/bundle constants, localizes deterministic screen-reader summaries for `sq`, `en`, `sr`, and `mk`, proves PII-safe chart labels/data keys with colocated regressions, and extends `pnpm --filter @interdomestik/web check:size` to aggregate every JS file loaded by each CRM chart dynamic import per affected route. Local and remote proof passed before merge; Notion closeout sync is pending because the connector token expired during post-merge sync. Proxy, canonical routes, auth/tenancy architecture, schema/migrations/RLS, scheduler/backfill/observability, new chart dependencies, package metadata, Stripe, README, AGENTS, and broad architecture docs remain unchanged.

`P38-DG13 Admin Reporting Branch-Manager Surface Design Review` closes the post-CRM16 charting tranche and promotes exactly one bounded next implementation slice, `P38-CRM20 Admin Reporting Branch-Manager Surface`: branch-manager access to the existing `/admin/crm` reporting route using a CRM `branch_manager` actor with mandatory branch scope. The design pins pre-actor-construction fail-closed behavior for missing branch scope, a split route-core model with separate admin and branch-manager cores, branch-only snapshot/pipeline/source rendering, no tenant-wide snapshot fallback, frozen 90-day reporting windows, branch-manager widget markers derived from `branch-manager-crm-reporting-`, admin-sidebar CRM visibility only for branch-scoped branch managers, staff-sidebar non-regression, aggregate-only PII-safe output, and focused route-core/page/sidebar/domain/PII/i18n/E2E proof. It does not authorize proxy edits, canonical route changes, auth/tenancy architecture changes, `/staff/crm` branch-manager access, `/agent/crm` or member UI changes, schema/migrations/RLS, scheduler/backfill/observability, new chart dependencies, branch comparison, peer comparison, drilldowns, Stripe, README, AGENTS, or broad architecture docs.

`P38-CRM20 Admin Reporting Branch-Manager Surface` is complete through PR `#768`, merge commit `6d1175116d44c60bd320ac67d479e822180d6503`: it adds a separate branch-manager `/admin/crm` reporting core, dispatches the page to admin or branch-manager cores after role/session validation, constructs branch-manager CRM actors only when tenant, actor ID, and branch scope are present, filters snapshot/pipeline/source rows to the session branch, exposes branch-manager widget markers while preserving `admin-crm-page-ready`, gates admin-sidebar `/admin/crm` visibility to branch-scoped branch managers, keeps `/staff/crm` hidden from branch managers, localizes branch-manager-specific admin CRM copy for `sq`, `en`, `sr`, and `mk`, and adds focused branch-manager core, page, sidebar, PII, CRM05 authorization, and E2E marker-regression tests. PR hardening fixed Copilot frame-copy and tracker-grammar feedback, extracted shared admin CRM dashboard primitives and test fixtures to clear SonarCloud duplication, and kept branch-manager and admin route cores split. Local proof passed for focused web unit tests, `packages/domain-crm/src/reporting/index.test.ts`, targeted `/admin/crm` E2E smoke coverage with admin and branch-manager sessions, plan/tracker/docs audits, i18n completeness/purity, DB-access guard, size checks, `git diff --check`, `interdomestik_qa.scope_audit`, static slice verification at `/Users/arbenlila/development/interdomestik-crystal-home/tmp/multi-agent/verify-slice/verify-slice-20260515T092614Z-97f4b2`, and required-gates slice verification at `/Users/arbenlila/development/interdomestik-crystal-home/tmp/multi-agent/verify-slice/verify-slice-20260515T092710Z-d58e99`. Remote proof passed before merge: SonarCloud Code Analysis, Copilot/pr-finalizer with review threads resolved, audit, unit, static, PR E2E, e2e-gate, Pilot Gate Preflight, Pilot Gate Runner, pilot-gate, commitlint, gitleaks, pnpm-audit, validation-surface, and Vercel ignored-build proof.

`P38-DG14 Forecast Snapshot Observability Design Review` is complete as the post-CRM20 design gate and promotes exactly one bounded next implementation slice, `P38-CRM18 Forecast Snapshot Observability`: a chart-free, admin-only snapshot health band on the existing `/admin/crm` surface. The design pins snapshot-date and `generatedAt` freezing at route-core entry, tenant-scoped expected-work-item discovery through the existing app-side `domain-crm` boundary, exact tenant/pipeline/branch/currency snapshot matching, inclusive delayed/stale threshold semantics, deferred expected-work-item counts, deduped unexpected-observed counts, latest source-run selection, branch-manager exclusion through the DG13 route-core split, aggregate-only PII-safe output, deterministic sort/cap semantics, and focused route-core/page/repository/i18n/PII/E2E proof. It does not authorize run-ledger persistence, schema/migrations/RLS, scheduler mutation, backfill, alerting integrations, new routes, branch-manager observability, `/staff/crm`, `/agent/crm`, member UI, charting changes, proxy edits, canonical route changes, auth/tenancy architecture changes, Stripe, README, AGENTS, or broad architecture docs.

`P38-CRM18 Forecast Snapshot Observability` is complete through PR `#770`, merge commit `a3712de4e0f99dd92999749e036ef6ae4fb770a7`: it keeps CRM18 chart-free and admin-only on `/admin/crm`, adds app-side `domain-crm` observability helpers, derives summary/coverage/batch markers from aggregate snapshot/work-item rows, keeps branch-manager sessions on the separate CRM20 branch-manager core with no CRM18 rendered markers, localizes the band for `sq`, `en`, `sr`, and `mk`, and records focused route-core/page/repository/i18n/PII/E2E proof plus full `pr:verify`, standalone `security:guard`, standalone `e2e:gate`, static slice verification, repo MCP scope-audit proof, SonarCloud, Copilot/pr-finalizer, Pilot Gate, CI unit/static/audit/e2e-gate, PR E2E, commitlint, gitleaks, pnpm-audit, and validation-surface proof in the tracker. Notion sync was recorded under `PR 770 P38 CRM18 Forecast Snapshot Observability` at `https://www.notion.so/361036cff1f8812e890eca27354ed094`. Copilot review hardening removed the raw `unknown` source-run sentinel, split tenant-scoped work-item reads from the scheduler system-exempt path, removed a misleading DB-access directive, and aligned Serbian no-branch copy. Proxy, canonical routes, auth/tenancy architecture, schema/migrations/RLS, scheduler mutation, backfill, run-ledger persistence, alerting, new routes, branch-manager observability, `/staff/crm`, `/agent/crm`, member UI, charting changes, Stripe, README, AGENTS, and broad architecture docs remained unchanged.

`P38-DG15 Forecast Snapshot Backfill Design Review` is complete and promotes `P38-CRM17 Forecast Snapshot Backfill`: a tenant-scoped, manually invoked `POST /api/cron/crm/forecast-snapshots/backfill` route protected by the existing cron bearer-secret boundary. The promoted implementation contract pins dry-run and write modes, bounded UTC date ranges, strict request validation, per-date work-item caps, per-date tenant-row cache isolation, previous-UTC date bounds, CRM13/CRM05 derivation reuse, distinct backfill system actor/log/source-run namespaces, per-work-item soft timeouts, append-only snapshot semantics, aggregate-only nested PII-safe output/logging, scheduler non-regression proof, and no all-tenant fleet backfill in the first slice. The 2026-05-15 automated design review amendments are incorporated. Implementation must land separately after the promotion gate merges.

`P38-CRM17 Forecast Snapshot Backfill` is complete through PR `#773`, merge commit `00076c953d8b34f9e13f0f9822891404f27523f0`: the implementation adds the promoted protected backfill API under the existing cron boundary, with strict tenant-scoped request validation, dry-run and write execution, bounded historical UTC date ranges, per-date work-item caps and cache isolation, CRM13/CRM05 derivation reuse, distinct backfill actor/log/source-run/idempotency namespaces, per-work-item soft timeout handling, append-only snapshot inserts, aggregate-only nested PII-safe responses/logs, and focused scheduler non-regression plus work-item repository proof. Local pre-PR proof passed through static slice verification at `/Users/arbenlila/development/interdomestik-crystal-home/tmp/multi-agent/verify-slice/verify-slice-20260515T142837Z-3066a4` and required-gates slice verification at `/Users/arbenlila/development/interdomestik-crystal-home/tmp/multi-agent/verify-slice/verify-slice-20260515T142919Z-0d1bc1`, covering `pr:verify`, `security:guard`, and standalone `e2e:gate` with `124` passed / `4` skipped. Remote proof passed before merge for SonarCloud, Copilot/pr-finalizer, audit, unit, static, PR E2E, e2e-gate, Pilot Gate, commitlint, gitleaks, pnpm-audit, validation-surface, and Vercel ignored-build checks. Notion sync was recorded under `PR 773 P38 CRM17 Forecast Snapshot Backfill` at `https://www.notion.so/361036cff1f881e59872f127976985ff`. Proxy, canonical routes, auth/session/tenancy architecture, schema/migrations/RLS, scheduler behavior, Vercel cron config, UI, sidebars, charting, alerting, run-ledger persistence, Stripe, README, AGENTS, and broad architecture docs remain out of scope.

`P38-DG16 Forecast Snapshot Backfill Operator UX Design Review` is complete and promotes exactly one bounded next implementation slice, `P38-CRM19 Forecast Snapshot Backfill Operator UX`. CRM19 is complete through PR `#775`, merge commit `fde3a6c7d19268c5b8935f169dacfc1780cc7c59`, and adds a compact admin-only operator band on existing `/admin/crm` route that invokes CRM17 backfill core through an admin-authenticated server action rather than the cron API, with HMAC dry-run confirmation using `CRM_BACKFILL_CONFIRMATION_SECRET`, action-entry admin checks, session-tenant matching, CRM17 bounds, rate limits, branch-manager exclusion, aggregate-only PII-safe output, localized copy, browser proof, and no `CRON_SECRET` exposure. It does not authorize proxy edits, canonical route changes, auth/tenancy architecture changes, schema/migrations/RLS, run-ledger tables, scheduler changes, Vercel cron config, CRM17 public API contract changes beyond safe internal extraction, staff/agent/member UI, branch-manager controls, charting, alerting, all-tenant fleet backfill, Stripe, README, AGENTS, or broad architecture docs.

`P38-DG17 Forecast Snapshot Alerting Design Review` is complete and promotes exactly one bounded next implementation slice, `P38-CRM22 Forecast Snapshot Alerting`. It closes the CRM19 operator UX line after PR `#775`, merge commit `fde3a6c7d19268c5b8935f169dacfc1780cc7c59`, and closeout sync PR `#776`, merge commit `e0df5f30f576679d0daee789077aec598bbab97e`; pins CRM22 to derive an aggregate-only admin alert from the real CRM18 forecast observability widget once per request; uses the CRM18 fields `expectedWorkItems`, `expectedWorkItemsDeferred`, `observedWorkItems`, `missingWorkItems`, `delayedWorkItems`, `staleWorkItems`, `unexpectedObservedWorkItems`, `snapshotDate`, and `generatedAt`; maps the CRM18 widget states `data` / `empty` / `error` into deterministic severity precedence; enforces branch-manager exclusion at the admin route-core split; and keeps proxy, canonical-route, auth/tenancy, schema/RLS/run-ledger, scheduler, Vercel cron, CRM17 public API, staff/agent/member UI, branch-manager output shapes, charting, external notifications, all-tenant fleet work, Stripe, README, AGENTS, and broad architecture docs out of scope.

`P38-CRM22 Forecast Snapshot Alerting` is complete through PR `#778`, merge commit `7ad9aa8b528b6a25fc47875c23ca8c06350d2a5c`: it adds the DG17-promoted compact admin-only `/admin/crm` forecast alert band, derives one aggregate-only severity result per request from the existing CRM18 observability widget, keeps branch-manager sessions on the separate route-core output with no alert markers, localizes `sq` / `en` / `sr` / `mk` copy, and records focused derivation/component/i18n/PII/E2E proof plus remote SonarCloud, Copilot/pr-finalizer, audit, unit, static, PR E2E, e2e-gate, Pilot Gate, commitlint, gitleaks, pnpm-audit, validation-surface, and Vercel Preview Comments proof. Copilot review feedback was resolved by aligning the Serbian OK explanation with the alert metrics. Notion sync is recorded under `PR 778 P38 CRM22 Forecast Snapshot Alerting` at `https://www.notion.so/361036cff1f881879272c84b09d5dbf1`. The Vercel deployment check remained pending and was intentionally not used as a merge gate. Proxy, canonical routes, auth/tenancy architecture, schema/migrations/RLS/run-ledger, scheduler, Vercel cron, CRM17 API, staff/agent/member UI, branch-manager output shapes, charting, external notifications, all-tenant fleet work, Stripe, README, AGENTS, and broad architecture docs remained unchanged.

`P38-DG18 Visual Regression Baseline Design Review` is complete through PR `#782`, merge commit `b9d92771eb1f6727e7a07593ded1e6a1f0901538`, and promotes exactly one bounded next implementation slice, `P38-CRM21 Visual Regression Baseline Infrastructure`. It scopes CRM21 to an opt-in CRM visual baseline lane for the existing `/agent/crm`, `/admin/crm`, branch-manager `/admin/crm`, and `/staff/crm` reporting surfaces, Linux/Chromium snapshots, deterministic role/tenant/locale fixtures, PII-safe screenshot masks, stable snapshot naming, and no product UI, routing, auth/tenancy, schema/RLS, scheduler, Vercel behavior, claims visual quarantine, Stripe, README, AGENTS, or broad architecture-doc changes. `P38-CRM21` is complete through PR `#783`, merge commit `e2ab25f2ab995145528387cee9c181d93946e24e`: it adds the opt-in CRM visual lane, commits four Linux/Chromium CRM baselines, keeps ordinary Playwright projects isolated from visual specs, updates the repo-size budget only for the committed snapshot delta, resolves the Sonar helper-nesting issue to `0` new issues, and records Notion sync at `https://www.notion.so/362036cff1f88115bae2fdd14a5e1d32`. Vercel remained intentionally skipped/ignored as non-blocking.

`P38-DG19 Post-CRM21 Next Slice Selection` is recorded in `docs/plans/2026-05-16-p38-dg19-post-crm21-next-slice.md` and promotes exactly one bounded next implementation slice, `P38-CRM08 Routing Persistence And Cursor Adapter`: add durable routing rule persistence, compare-and-swap cursor advancement, and append-only routing audit rows behind the existing `packages/domain-crm` routing repository ports. The tightened implementation contract pins table shape, nullability, indexes, CHECK constraints, composite tenant FKs, RLS pattern sources, cursor-conflict retry semantics, audit idempotency, typed `crm.lead.routed` event preservation, `crm_lead_ownership_history` separation, db-access baseline discipline, acceptance criteria, coverage discipline, risks, sequencing, approval bar, and sign-off. It does not authorize proxy edits, canonical route changes, auth/tenancy architecture changes, new routing UI, automatic lead assignment, Stripe, README, AGENTS, or broad architecture-doc changes; `P38-CRM25` is reserved for future routing audit retention.

`P38-CRM08 Routing Persistence And Cursor Adapter` is complete through PR `#785`, merge commit `3475283a26cde1d80f88b463b77adb619e00ae55`: durable routing rules, cursors, audit rows, RLS, composite tenant FKs, compare-and-swap cursor updates, idempotent audit inserts, CRM07 SQL adapters, focused adapter proof, db-access baseline proof, and Notion sync at `https://www.notion.so/362036cff1f8811c92cdfe3aa10eff18`. SonarCloud, Copilot/pr-finalizer, audit, unit, static, PR E2E, e2e-gate, Pilot Gate, commitlint, gitleaks, pnpm-audit, validation-surface, and Vercel proof passed before merge. Proxy, routes, auth/tenancy, routing UI, automatic routing, Stripe, README, AGENTS, and broad architecture docs remained unchanged.

`P38-DG20 Routing Admin UX Design Review` is complete and promotes exactly one bounded next implementation slice, `P38-CRM09 Routing Admin UX And Rule Management`: admin-only rule visibility and bounded create/update/enable-disable/archive/reorder management on existing `/admin/crm` using CRM08 persistence, with branch-manager exclusion, strict server-action authorization, PII-safe output, localized `admin-crm.routing.*` copy, focused proof, and no automatic routing, ownership transfer, new routes, proxy, auth/tenancy, Stripe, README, AGENTS, or broad architecture-doc work.

`P38-CRM09 Routing Admin UX And Rule Management` is complete through PR `#791`, merge commit `a59397cd9305fdc5b1419f08f1fc2a1ae56bf039`: it adds the DG20-approved admin-only routing-rule panel, sibling route core, strict server actions, app-side CRM08 write-path extensions, localized copy, branch-manager exclusion, focused proof, and targeted `@admin-crm-routing-rules` E2E while avoiding automatic routing, ownership transfer, new routes, proxy, canonical route, auth/tenancy, Stripe, README, AGENTS, and broad architecture-doc changes. `P38-DG21 Post-CRM09 Next Slice Selection` is recorded in `docs/plans/2026-05-16-p38-dg21-post-crm09-next-slice.md` and promotes exactly one next bounded implementation slice, `P38-CRM23 Controlled Routing Application Service`: a service-boundary slice that composes CRM07 routing selection with CRM08 persistence and existing lead ownership discipline for explicit tenant-scoped lead routing, without lead-creation wiring, cron, campaign automation, UI controls, new routes, proxy, auth/tenancy architecture changes, schema/RLS by default, Stripe, README, AGENTS, or broad architecture-doc work. DG21 also sets the P38 CRM Foundation completion bar: after CRM23 lands, P38 should close when lead/account/contact/deal foundations exist, pipeline/reporting/forecasting surfaces are usable, routing rules can be managed and applied through the controlled service, legacy deal cleanup/nullability risks are closed or explicitly deferred, and tasks/templates/sequences/scoring/consent move to a later program unless required for pilot operation. `P38-CRM23` is complete through PR `#793`, merge commit `6674b0e022a6e8b0d828826b40ef49fbb62d8151`: it adds the controlled routing application service, app-side transaction coordinator, idempotent replay, cursor retry, audit append, ownership-transfer, outbox append, rollback, stale-lead, and no-write failure behavior, plus focused domain/app proof. `P38-DG22 CRM23 And CRM Foundation Closeout` is recorded in `docs/plans/2026-05-16-p38-dg22-crm23-foundation-closeout.md` and closes P38 with no promoted next implementation slice. Legacy deal column retirement (`P38-CRM10`) and nullability tightening (`P38-CRM11`) are explicitly deferred behind future parity/zero-null/quarantine/backfill proof; tasks/templates/sequences/scoring/consent, routing audit retention, automated routing triggers, and broader CRM workflow expansion move to a later program unless required for pilot operation.

`P39 IDA Assistance And Recovery Program` is now open after completed `P38-DG22`. `P39-DG01 IDA Assistance And Recovery Program Design Gate` is recorded in `docs/plans/2026-05-17-p39-dg01-ida-assistance-recovery-program-design-gate.md` and promoted exactly one initial slice, `P39-ASSIST-00 Program Spec And Domain Contracts`. `P39-ASSIST-00` is now recorded in `docs/plans/2026-05-17-p39-assist-00-program-spec-domain-contracts.md` and completes the program spec/domain-contract layer before runtime work: it pins `packages/domain-assistance/` as a pure rules-first package, the minimum `AssistanceOutcome` taxonomy, assistance pack contracts, country-rule metadata, `0.80` launch-floor confidence threshold, fail-closed semantics, `domain-country-guidance` read-only adapter strategy, disclaimer and PII/logging contracts, Help Now consented escalation, Professional Recovery state/activation rules, human-review and AI provenance limits, and the `AssistanceSessionDigest` integration boundary. `P39-ASSIST-01 domain-assistance Core Package` is complete on the pure package lane, and `P39-ASSIST-02 Help Now / Incident Scene Pack` is complete through PR `#808`, merge commit `dd423a03cc8ad157ea974b930810742514732e5c`, with Notion closeout sync at `https://www.notion.so/364036cff1f8816dbbdee924c9dc0966`. `P39-DG02 Green Card Country Rules Adapter Design Gate` is recorded in `docs/plans/2026-05-18-p39-dg02-green-card-country-rules-adapter-design.md` and promotes exactly one next implementation slice, `P39-ASSIST-03 Green Card / Country Rules Adapter`. `P39-ASSIST-03` is complete through PR `#810`, merge commit `fdef78f32f10f87a9067fd2346e76784eea45222`, with Notion closeout sync at `https://www.notion.so/364036cff1f8815ba34ff05cc94d3845`: `packages/domain-assistance` now exposes typed Green Card scenario, jurisdiction-role, rule-conclusion, conclusion-subject, applicable-jurisdiction, and readiness contracts plus fail-closed Green Card readiness evaluation for missing, metadata-incomplete, stale, conflicting, unsupported-country, unsupported-scenario, unsupported-rule-family, low-confidence, scenario-mismatch, cross-jurisdiction contradiction, invalid stale-threshold input, and lowered-threshold attempts. `domain-country-guidance` remains structurally consumed/read-only because its current data is generic localized accident guidance and does not yet carry curated Green Card source ownership, freshness, or confidence authority. `P39-DG03 Legal Basis Pre-check Design Gate` is recorded in `docs/plans/2026-05-18-p39-dg03-legal-basis-precheck-design.md` and promotes exactly one next implementation slice, `P39-ASSIST-04 Legal Basis Pre-check`. `P39-ASSIST-04` is complete on the pure domain lane: `packages/domain-assistance` now exposes typed legal-basis rule-family, scenario, participant-role, jurisdiction-role, conclusion, readiness, and pack contracts plus fail-closed legal-basis pre-check evaluation for free-zone inputs, Professional-Recovery-like inputs, missing jurisdiction, missing rules, incomplete metadata, stale rules, conflicting and cross-jurisdiction contradictory conclusions, unsupported country, unsupported scenario, unsupported role, unsupported rule family, unsupported legal-basis conclusions, low confidence below the `0.80` floor, lowered-threshold attempts, and legal interpretation beyond encoded rules. AI-assisted provenance is still blocked from final decisioning and now keeps pack-level human-review state aligned with the outcome. P39 explicitly preserves `apps/web/src/proxy.ts`, canonical `/member`, `/agent`, `/staff`, and `/admin` routes, auth/session layering, tenancy architecture, routing architecture, existing CRM and claims boundaries, and the V3 no-Stripe pilot posture. It does not authorize product runtime UI, `/member/incident-guide` redesign, database migrations, CRM/claim/handoff creation, direct `domain-crm` imports from `domain-assistance`, outbox/event emission, autonomous AI decisioning, Professional Recovery activation, Stripe, README, AGENTS.md, or broad architecture-doc changes.

`P39-DG04 Procedure Guide Design Gate` is recorded in `docs/plans/2026-05-18-p39-dg04-procedure-guide-design.md` after `P39-ASSIST-04` closed through PR `#812`, merge commit `cd9f0d466f33b653a535962f7a60ebafe67b540d`, and Notion closeout sync at `https://www.notion.so/364036cff1f88111bec7f6da4a868cf6`. It promotes exactly one next implementation slice, `P39-ASSIST-05 Procedure Guide`, scoped to pure member-zone procedure guide contracts in `packages/domain-assistance`: procedure codes, source-backed deadline references, country-rule metadata, the `0.80` confidence floor, fail-closed deadline uncertainty, required disclaimers, PII/provenance, and human-review state. It does not authorize product runtime UI, `/member/incident-guide` redesign, DB migrations, CRM/claim/handoff creation, outbox/event emission, Professional Recovery activation, autonomous AI decisioning, Stripe, proxy/canonical route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work.

`P39-ASSIST-05 Procedure Guide` is complete on the pure domain lane: `packages/domain-assistance` now exposes typed procedure guide rule-family, scenario, participant-role, jurisdiction-role, conclusion, procedure-code allowlist, deadline-reference, legal-basis reference, readiness, and pack contracts plus fail-closed procedure guide evaluation for free-zone inputs, Professional-Recovery-like inputs, missing jurisdiction, missing rules, incomplete metadata, stale rules, conflicting duplicate and cross-jurisdiction contradictory conclusions, unsupported country, unsupported scenario, unsupported role, unsupported rule family, unsupported or missing procedure conclusions and procedure codes, low confidence below the `0.80` floor, lowered-threshold attempts, missing, stale, low-confidence, conflicting, or ambiguous deadline references, out-of-scope legal interpretation, professional-review-required conclusions, and AI-assisted provenance forcing pack-level human review. It preserves source-backed deadline references as evidence references, not legal deadline guarantees, and does not introduce product runtime UI, `/member/incident-guide` redesign, database migrations, CRM/claim/handoff creation, outbox/event emission, direct `domain-crm` imports, autonomous AI decisioning, Professional Recovery activation, Stripe, proxy/canonical route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work.

`P39-DG05 Privacy, Consent, And Sensitive Document Governance Design Gate` is recorded in `docs/plans/2026-05-18-p39-dg05-privacy-consent-sensitive-document-governance.md` and intentionally interrupts the ordinary P39 assistance sequence before `P39-ASSIST-06 Injury Category Pre-check`. It promotes exactly one cross-cutting prerequisite slice, `P39-PRIV01 Privacy, Consent, And Sensitive Document Foundation`, because injury, invalidity, vehicle evidence, upload, AI extraction, CRM/claim/handoff integration, and Professional Recovery flows require privacy-zone separation, granular consent and authorization events, document sensitivity classification, purpose-limited processing, role-scoped access, document access logs, AI extraction consent/audit boundaries, third-party sharing consent, retention, data-subject request surfaces, and DPIA posture before broader runtime exposure. `P39-ASSIST-06` and later assistance/recovery DGs remain blocked until `P39-PRIV01` lands or a later repo-canonical gate explicitly overrides that dependency. P39-DG05 does not authorize product runtime UI, upload flows, `/member/incident-guide` redesign, database migrations beyond the later bounded PRIV01 slice, CRM/claim/handoff creation, outbox/event emission, direct `domain-crm` imports from `domain-assistance`, autonomous AI decisioning, Professional Recovery activation, Stripe, proxy/canonical route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work.

`P39-PRIV01 Privacy, Consent, And Sensitive Document Foundation` is complete as a contract-only privacy governance slice. `packages/domain-privacy` now provides pure tenant-aware contracts and helpers for Free / Self-service, Member / Assistance, and Professional Recovery privacy zones; granular consent and authorization events; explicit medical-document and Article 9 consent checks; document sensitivity policies with source upload surface, allowed purposes, allowed recipient classes, required consent types, retention, redaction, AI allowance, and human-review metadata; role-scoped document access decisions that deny agent/promoter access to sensitive documents; AI extraction policy checks that require consent, no-training, zero-retention, non-final output, and human review for sensitive workflows; data-subject request deadlines; and DPIA requirement posture. The slice intentionally does not introduce runtime UI, upload flows, product route changes, database migrations/RLS, CRM/claim/handoff creation, outbox/event emission, Professional Recovery activation, autonomous AI decisioning, Stripe, proxy/canonical route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work. `P39-DG06 Injury Category Pre-check Design Gate` is recorded in `docs/plans/2026-05-18-p39-dg06-injury-category-precheck-design.md` and promotes exactly one next implementation slice, `P39-ASSIST-06 Injury Category Pre-check`, scoped to pure member-zone domain contracts in `packages/domain-assistance`: injury category rule families, severity/category codes, evidence references, privacy-purpose and sensitive-health alignment, the `0.80` confidence floor, fail-closed medical/consent uncertainty handling, required medical/legal/insurer disclaimers, PII/provenance, and human-review state. It does not authorize product runtime UI, upload flows, `/member/incident-guide` redesign, database migrations/RLS, CRM/claim/handoff creation, outbox/event emission, Professional Recovery activation, autonomous AI decisioning, Stripe, proxy/canonical route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work.

`P39-ASSIST-06 Injury Category Pre-check` is complete on the pure domain lane: `packages/domain-assistance` now exposes typed injury category rule-family, scenario, participant-role, category-code, severity-band, evidence-reference, privacy-alignment, readiness, and pack contracts plus fail-closed injury pre-check evaluation for free-zone inputs, Professional-Recovery-like inputs, missing jurisdiction, cross-border jurisdiction tie-breaker gaps, missing rules, incomplete/stale/conflicting/unsupported/low-confidence country rules, unsupported category/severity, missing medical-document consent, missing Article 9 consent, out-of-scope medical diagnosis/prognosis/treatment/invalidity/compensation/insurer liability requests, and AI-assisted provenance forcing human review. It preserves structural alignment to `domain-privacy` keys (`injury_category_precheck`, `sensitive_health`, `medical_document_processing`, `ai_document_extraction`), synthetic non-identifiable tests, required medical/legal/insurer disclaimers, PII/provenance, retention posture, and the `0.80` confidence floor. It does not introduce runtime UI, upload flows, product route changes, database migrations/RLS, CRM/claim/handoff creation, outbox/event emission, Professional Recovery activation, autonomous AI decisioning, Stripe, proxy/canonical route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work.

`P39-DG07 Vehicle Damage Pre-check Design Gate` is recorded in `docs/plans/2026-05-18-p39-dg07-vehicle-damage-precheck-design.md` and promotes exactly one next implementation slice, `P39-ASSIST-07 Vehicle Damage Pre-check`, scoped to pure member-zone domain contracts in `packages/domain-assistance`: vehicle damage rule families, damage category codes, inspection and repair-readiness markers, evidence references, privacy-purpose and document-processing alignment, insurer-sharing consent expectations, the `0.80` confidence floor, fail-closed valuation/liability/fraud/settlement uncertainty handling, required legal/insurer/professional disclaimers, PII/provenance, retention posture, and human-review state. It does not authorize product runtime UI, upload flows, `/member/incident-guide` redesign, database migrations/RLS, CRM/claim/handoff creation, outbox/event emission, Professional Recovery activation, autonomous AI decisioning, Stripe, proxy/canonical route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work.

`P39-ASSIST-07 Vehicle Damage Pre-check` is complete on the pure domain lane: `packages/domain-assistance` now exposes typed vehicle damage rule-family, scenario, participant-role, damage-category, inspection-readiness, repair-assessment-routing, evidence-reference, privacy-alignment, document-sensitivity, readiness, and pack contracts plus fail-closed vehicle pre-check evaluation for free-zone inputs, Professional-Recovery-like inputs, missing jurisdiction, cross-border jurisdiction tie-breaker gaps across incident, registration, and insurer countries, missing rules, incomplete/stale/conflicting/unsupported/low-confidence country rules, unsupported category/inspection/routing/evidence values, missing document-processing, insurer-sharing, bureau-sharing, or AI-extraction consent, health-evidence re-route to injury review, blocked plate/VIN OCR, out-of-scope repair-cost, diminished-value, liability, coverage, fraud, settlement, or legal-advice requests, and AI-assisted provenance forcing human review. It preserves structural alignment to `domain-privacy` keys (`vehicle_damage_precheck`, `document_upload_processing`, `ai_document_extraction`, `share_with_insurer`, `share_with_bureau`, `personal`, `legal_professional_recovery`), synthetic non-identifiable tests, required vehicle/legal/insurer disclaimers, PII/provenance, retention posture, and the `0.80` confidence floor. It does not introduce runtime UI, upload flows, product route changes, database migrations/RLS, CRM/claim/handoff creation, outbox/event emission, Professional Recovery activation, autonomous AI decisioning, Stripe, proxy/canonical route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work.

`P39-DG08 Invalidity Coefficient Review Design Gate` is recorded in `docs/plans/2026-05-19-p39-dg08-invalidity-coefficient-review-design.md` after `P39-ASSIST-07` closed through PR `#821`, merge commit `e7d764f16a2064d099a0a5767c4ba973c69bea65`, and Notion closeout sync at `https://www.notion.so/365036cff1f881659503dfdb9565c653`. It promotes exactly one next implementation slice, `P39-ASSIST-08 Invalidity Coefficient Review`, scoped to pure member-zone, human-review-only domain contracts in `packages/domain-assistance`: invalidity review reason codes, prerequisite injury and vehicle references, structured evidence references, privacy and medical-document consent alignment, Article 9 posture, the `0.80` confidence floor, required disclaimers, PII/provenance, retention/redaction posture, and fail-closed blocking for any request to calculate or finalize an invalidity coefficient. It does not authorize coefficient automation, medical diagnosis, prognosis, treatment advice, compensation valuation, insurer liability or coverage decisions, settlement strategy, legal advice, runtime UI, upload flows, `/member/incident-guide` redesign, database migrations/RLS, CRM/claim/handoff creation, outbox/event emission, Professional Recovery activation, autonomous AI decisioning, Stripe, proxy/canonical route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work.

`P39-ASSIST-08 Invalidity Coefficient Review` is complete on the pure domain lane:
`packages/domain-assistance` now exposes typed invalidity review rule-family, scenario,
participant-role, review-reason, prerequisite-status, prerequisite-reference, structured evidence,
privacy-alignment, document-sensitivity, readiness, and pack contracts plus fail-closed invalidity
review evaluation for free-zone inputs, Professional-Recovery-like inputs, missing jurisdiction,
cross-border jurisdiction tie-breaker gaps across incident, residence, treatment, registration, and
insurer countries, missing injury or vehicle prerequisite references, incomplete prerequisite
metadata, missing rules, incomplete/stale/conflicting/unsupported/low-confidence country rules,
unsupported reason/prerequisite/evidence values, missing medical-document consent, missing Article
9 posture, missing document-processing or AI-extraction consent, incomplete AI posture, out-of-scope
diagnosis/prognosis/treatment/coefficient/compensation/insurer/fraud/settlement/legal-advice
requests, lowered-threshold attempts, and AI-assisted provenance remaining non-final and
human-review-gated. It preserves structural alignment to `domain-privacy` keys
(`invalidity_review`, `medical_document_processing`, `ai_document_extraction`,
`sensitive_health`, `personal`, `legal_professional_recovery`), synthetic non-identifiable tests,
required medical/legal/insurer/professional disclaimers, PII/provenance, retention/redaction
posture, and the `0.80` confidence floor. It does not introduce runtime UI, upload flows, product
route changes, database migrations/RLS, CRM/claim/handoff creation, outbox/event emission,
Professional Recovery activation, autonomous AI decisioning, Stripe, proxy/canonical
route/auth/tenancy/routing changes, README, AGENTS.md, or broad architecture-doc work.

`P39-DG09 Assistance Session Workflow Wiring Design Gate` is recorded in
`docs/plans/2026-05-19-p39-dg09-assistance-session-workflow-wiring-design.md` after
`P39-ASSIST-08` closed through PR `#823`, merge commit
`95805bcd3fb8fcc7a93611eb8fd12137b69142b9`. It promotes exactly one next implementation
slice, `P39-ASSIST-09 Assistance Session Workflow Wiring Contract`, scoped to pure
`domain-assistance` workflow-intent contracts over `AssistanceSessionDigest`: deterministic
non-executing intent envelopes, `executionAllowed: false`, target-surface and blocker codes,
ordered digest summary/outcome reconciliation, existing outcome and disclaimer taxonomy reuse,
hash-safe reference keys, privacy/consent/disclaimer/human-review propagation, Article 9 and DPIA
posture, AI non-finality, and fail-closed blocking for missing identity, missing explicit consent,
missing disclaimers, missing country metadata, sensitive-data/human-review mismatches, AI
provenance, or any request to create records. It does not authorize runtime UI, upload flows,
database migrations/RLS, CRM/claim/handoff creation, outbox/event emission, Professional Recovery
activation, autonomous AI workflows, model calls, prompt changes, proxy/canonical
route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, or broad architecture-doc work.

`P39-ASSIST-09 Assistance Session Workflow Wiring Contract` is complete through PR `#825`, merge
commit `35a011372eff9b16c879ddadbf7313d92bc48bd1`: `packages/domain-assistance` now exposes pure
workflow-intent contracts over `AssistanceSessionDigest`, deterministic readonly intent output,
literal `executionAllowed: false`, fail-closed blocker codes, existing outcome/disclaimer taxonomy
reuse, hash-safe reference keys, privacy/consent/disclaimer/human-review propagation, AI
non-finality, and scoped digest evidence pairing. P39 remains a package-only assistance foundation
and still does not authorize runtime UI, upload flows, database persistence/RLS, CRM/claim/handoff
creation, outbox/event emission, Professional Recovery activation, autonomous AI workflows, model
calls, prompt changes, proxy/canonical route/auth/tenancy/routing changes, Stripe, README,
AGENTS.md, or broad architecture-doc work.

`P40 CRM Product Depth` is now complete after the completed package-only P39 assistance lane.
`P40-DG01 CRM Lane Resumption And Task Foundation Design Gate` is recorded in
`docs/plans/2026-05-20-p40-dg01-crm-resume-task-foundation.md` and promotes exactly one next
implementation slice, `P40-CRM24 CRM Task Domain Foundation`. The promoted slice resumes the CRM
program lane, not task-state recovery or paused-task runtime behavior, at the first deferred
product-depth item from the P38 roadmap. `P40-CRM24` is complete as the pure
`packages/domain-crm` task-domain foundation: typed task contracts, repository ports,
state-transition helpers, deterministic clock/ID injection, idempotency expectations, PII-safe
subject references, branch-scoped fail-closed mutation rules, and focused unit tests. It does not
authorize task persistence, runtime UI, scheduler/cron, notifications, templates, sequences,
scoring, consent/preference implementation, assistance-intent execution, CRM/claim/handoff creation
from assistance, proxy/canonical route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, or
broad architecture-doc work. Local proof passed focused `domain-crm` type-check and unit tests,
`pnpm security:guard`, `pnpm pr:verify`, and standalone `pnpm e2e:gate` with `128` passed / `4`
skipped. Any later P40 persistence, UI, scheduler, notification, template, sequence, scoring,
consent, or assistance-adapter work requires a separate repo-canonical gate.
`P40-DG02 CRM Task Persistence And Repository Adapter Design Gate` is recorded in
`docs/plans/2026-05-20-p40-dg02-crm-task-persistence-design.md` and promotes exactly one next
implementation slice, `P40-CRM25 CRM Task Persistence And Repository Adapter`. The promoted slice
is limited to additive `crm_tasks` and `crm_task_history` persistence, tenant RLS, database schema
exports, an app-side `CrmTaskRepository` adapter, lifecycle-version compare-and-set proof,
subject-visibility proof for durable `lead`, `deal`, and `support_handoff` subjects, fail-closed
`account` and `contact` handling until durable tables exist, and focused adapter/database tests.
It does not authorize runtime task UI, routes, server actions, scheduler/cron, notifications,
templates, sequences, scoring, consent/preference implementation, lead-follow-up migration,
assistance-intent execution, CRM/claim/handoff creation from assistance, proxy/canonical
route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, or broad architecture-doc work.
`P40-CRM25` is complete through PR `#833`, merge commit
`0c941890e2f2f2ae1f9460879a6857a42e374161`: it adds additive `crm_tasks` and
`crm_task_history` persistence, tenant RLS, database schema exports, app-side
`createCrmTaskRepository`, lifecycle-version compare-and-set writes, idempotent create replay,
subject-visibility proof for `lead`, `deal`, and `support_handoff`, fail-closed `account` and
`contact` handling, and focused adapter/RLS proof. Runtime task UI, routes, server actions,
scheduler/cron, notifications, templates, sequences, scoring, consent/preference implementation,
lead-follow-up migration, assistance-intent execution, CRM/claim/handoff creation from assistance,
proxy/canonical route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, and broad
architecture-doc work remained out of scope.
`P40-DG03 CRM Task Runtime Boundary Design Gate` is recorded in
`docs/plans/2026-05-21-p40-dg03-crm-task-runtime-boundary-design.md` and promotes exactly one next
implementation slice, `P40-CRM26 CRM Task Application Service And Server Action Boundary`. The
promoted slice is limited to `.core.ts`/server-action boundary code over the CRM25 repository
adapter, typed `{ outcome: ... }` result envelopes, session-derived `CrmActorContext`,
`expectedLifecycleVersion` conflict handling, deterministic idempotency posture, PII-safe task DTOs,
`logAuditEvent` audit proof, `enforceRateLimitForAction` mutation throttling, locale-loop
revalidation discipline, and focused service/action tests. It does not authorize runtime task UI,
HTTP route handlers, cron route handlers, scheduler/reminder behavior, notifications, outbox,
templates, sequences, scoring, lead-follow-up migration, assistance-intent execution, database
schema/migration/RLS changes, proxy/canonical route/auth/tenancy/routing changes, Stripe, README,
AGENTS.md, or broad architecture-doc work.
`P40-CRM26` is complete through PR `#835`, merge commit
`a70dda59463eeb664fd97804c8563653f46c9e04`: it adds the CRM task application-service and
server-action boundary over the CRM25 adapter, typed `{ outcome: ... }` results, trusted
session-derived actor context, lifecycle-version conflict mapping, deterministic create
idempotency and replay, PII-safe structural `CrmTask` output, audit events, action rate limiting,
locale-loop CRM revalidation, admin-equivalent role normalization, and focused service/action
tests. Runtime task UI, HTTP route handlers, cron route handlers, scheduler/reminder behavior,
notifications, outbox, templates, sequences, scoring, lead-follow-up migration,
assistance-intent execution, database schema/migration/RLS changes, proxy/canonical
route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, and broad architecture-doc work
remained out of scope.
`P40-DG04 CRM Lead Follow-Up Task Migration Design Gate` is recorded in
`docs/plans/2026-05-21-p40-dg04-crm-lead-follow-up-task-migration-design.md` after
`P40-CRM26`. It promotes exactly one next implementation slice,
`P40-CRM27 Agent Lead Follow-Up To CRM Task Migration`, limited to making new agent lead follow-up
scheduling and completion task-backed through the CRM task aggregate and CRM26 boundary while
preserving bounded compatibility for existing legacy `crm_activities` follow-up rows. Broad task UI,
scheduler/cron, reminders, notifications, outbox, templates, sequences,
scoring, assistance-intent execution, database migration/RLS, historical backfill, proxy/canonical
route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, and broad architecture-doc work
remain blocked.
`P40-CRM27` is complete through PR `#837`: new agent lead follow-up scheduling writes to CRM
tasks through the CRM26 boundary instead of creating new legacy `crm_activities` follow-up rows,
existing open legacy follow-up rows remain visible and completable through bounded compatibility,
task-backed rows are canonical in union reads, duplicate legacy rows shadowed by a task-backed
completion are completed, and stale lifecycle-version conflicts fail closed. Focused proof covers
domain derivation, dashboard union/dedupe behavior, branch-scoped legacy reads, server-action
schedule/complete flows, lead-detail rendering, and the agent CRM follow-up E2E gate. Local proof
passed `pnpm security:guard`, `pnpm pr:verify`, and `pnpm e2e:gate`; remote checks were green
before merge. Broad task UI, scheduler/cron, reminders, notifications, outbox, templates,
sequences, scoring, assistance-intent execution, database migration/RLS, historical backfill,
proxy/canonical route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, and broad
architecture-doc work remained blocked.
`P40-DG05 CRM Task Work Queue UI Foundation Design Gate` is recorded in
`docs/plans/2026-05-21-p40-dg05-crm-task-work-queue-ui-foundation-design.md` after
`P40-CRM27`. It promotes exactly one next implementation slice,
`P40-CRM28 Agent CRM Task Work Queue UI Foundation`, limited to a read-only, agent-scoped,
lead-backed CRM task queue on the existing `/agent/crm` route. The promoted slice may add a pure
task work-queue read model and app-side read adapter using the existing RSC/server `_core.ts` read
pattern, task-only queue DTOs, PII-safe lead display projection, deterministic 10-row ordering,
active app locale labels, and accessibility proof. Staff/admin/member task UI, task mutation controls,
scheduler/cron, reminders, notifications, outbox, templates, sequences, scoring,
assistance-intent execution, database migration/RLS, historical backfill,
proxy/canonical route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, and broad
architecture-doc work remain blocked.
`P40-CRM28 Agent CRM Task Work Queue UI Foundation` is complete through PR `#839`, merge commit
`f18b28b9b438b4ec6247da307d39c709544a35e4`: the slice adds a pure `domain-crm` task work-queue
read model, a task-only app-side read adapter, and a read-only queue panel on the existing
`/agent/crm` page with the `agent-crm-task-queue-ready` marker. The queue remains limited to
visible, assigned, open, lead-backed CRM tasks with PII-safe lead display projection, deterministic
10-row ordering, active app locale labels, and targeted agent CRM follow-up gate proof. Local proof
passed `pnpm security:guard`, `pnpm pr:verify`, and `pnpm e2e:gate`; remote checks were green
before merge. Task mutations, staff/admin/member task UI, new routes,
scheduler/cron/reminders/notifications/outbox/templates/sequences/scoring, assistance-intent
execution, database migration/RLS, historical backfill, proxy/canonical route/auth/tenancy/routing
changes, Stripe, README, AGENTS.md, and broad architecture-doc work remained blocked.
`P40-DG06 CRM Task Queue Lifecycle Controls Design Gate` is recorded in
`docs/plans/2026-05-22-p40-dg06-crm-task-queue-lifecycle-controls-design.md` after
`P40-CRM28`. It promotes exactly one next implementation slice,
`P40-CRM29 Agent CRM Task Queue Start And Complete Controls`, limited to non-optimistic Start and
Complete controls on visible, assigned, open, lead-backed CRM28 queue rows on the existing
`/agent/crm` route. The promoted slice reuses the CRM26 `startCrmTaskAction` and
`completeCrmTaskAction` paths with existing `manual_start` and `resolved` reason codes, preserves
server-side reauthorization, lifecycle-version conflict handling, PII-safe copy, CRM26
rate-limit/audit/revalidation behavior, and legacy due-follow-up separation. Cancellation,
reopening, assignment, due-date editing, staff/admin/member task UI, new routes, scheduler/cron,
reminders, notifications, outbox, templates, sequences, scoring, assistance-intent execution,
database migration/RLS, historical backfill, proxy/canonical route/auth/tenancy/routing changes,
Stripe, README, AGENTS.md, and broad architecture-doc work remain blocked.
`P40-CRM29 Agent CRM Task Queue Start And Complete Controls` is complete through PR `#841`, merge
commit `9dc0a300a89b4501a99879271dac7839d1ab533c`: the existing `/agent/crm` CRM28 task queue now
exposes non-optimistic Start and Complete controls only on visible, assigned, open, lead-backed task
rows. The controls route through a route-local UI action wrapper over the CRM26
`startCrmTaskAction` and `completeCrmTaskAction` boundaries, pin the existing `manual_start` and
`resolved` reason codes, preserve server-side reauthorization, expose only UI-safe result buckets,
keep row-local pending and failure states, refresh after successful mutations, and keep legacy
due-follow-up rows separate and unmutated by the new queue controls. Focused proof passed the
route-local action/control/page unit tests, web type-check, i18n completeness and purity checks, the
targeted agent CRM follow-up Playwright gate for KS/SQ and MK/MK, `pnpm security:guard`,
`pnpm pr:verify`, and `pnpm e2e:gate`. Cancellation, reopening, assignment, due-date editing,
staff/admin/member task UI, new routes, scheduler/cron, reminders, notifications, outbox,
templates, sequences, scoring, assistance-intent execution, database migration/RLS, historical
backfill, proxy/canonical route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, and broad
architecture-doc work remained blocked.
`P40-DG07 CRM Task Queue Due-Date Adjustment Controls Design Gate` is recorded in
`docs/plans/2026-05-22-p40-dg07-crm-task-queue-due-date-controls-design.md` after `P40-CRM29`. It
promotes exactly one next implementation slice,
`P40-CRM30 Agent CRM Task Queue Due-Date Adjustment Controls`, limited to bounded due-date
adjustment and clear controls on visible, assigned, open, lead-backed CRM28/CRM29 queue rows on the
existing `/agent/crm` route. The promoted slice reuses the CRM26 `updateCrmTaskDueAtAction` path
with existing `due_date_changed` and `due_date_cleared` reason codes, preserves server-side
reauthorization, lifecycle-version conflict handling, same-value idempotency, PII-safe result
buckets, CRM26 audit/rate-limit/revalidation behavior, legacy due-follow-up separation, timezone
normalization, and row-local focus behavior. Cancellation, reopening, assignment, staff/admin/member
task UI, new routes, scheduler/cron, reminders, notifications, outbox, templates, sequences,
scoring, assistance-intent execution, database migration/RLS, historical backfill,
proxy/canonical route/auth/tenancy/routing changes, Stripe, README, AGENTS.md, and broad
architecture-doc work remain blocked.
`P40-CRM30 Agent CRM Task Queue Due-Date Adjustment Controls` is complete through PR `#845`, merge
commit `fbfe70c8be9a69079784f6c7f9f31fab1be105c1`: the existing `/agent/crm` task queue now has
row-local due-date edit/save/clear/cancel controls for visible, assigned, open, lead-backed task
rows only. The implementation reuses CRM26 `updateCrmTaskDueAtAction`, preserves Start/Complete
controls, lead links, queue markers, legacy due-follow-up separation, canonical routes, and
`apps/web/src/proxy.ts`, and passed local focused CRM task tests, web type-check,
`pnpm security:guard`, and earlier required gates, with remote validation, audit, static, unit, E2E,
`e2e-gate`, Pilot Gate, SonarCloud, Copilot/pr-finalizer, gitleaks, pnpm-audit, and Vercel checks
green before merge. Scheduler/reminders/notifications/outbox, cancellation, reopening,
reassignment, new routes/API/cron, DB schema/RLS/migrations, assistance execution,
auth/tenancy/routing refactors, Stripe, README, AGENTS.md, and architecture-doc work remained out of
scope.
`P40-DG08 CRM Task Queue Cancellation Controls Design Gate` is recorded in
`docs/plans/2026-05-22-p40-dg08-crm-task-cancellation-controls-design.md` after `P40-CRM30`. It
promotes exactly one next implementation slice,
`P40-CRM31 Agent CRM Task Queue Cancellation Controls`, limited to bounded row-local cancellation
controls for visible, assigned, open, lead-backed CRM task queue rows on the existing `/agent/crm`
route. The promoted slice reuses CRM26 `cancelCrmTaskAction`, exposes only `not_needed`,
`duplicate`, and `created_in_error` in the first queue picker, requires row-local inline
confirmation, preserves server-side reauthorization, lifecycle-version conflict handling,
same-reason idempotency semantics, CRM26 audit/rate-limit/revalidation behavior, active-locale
labels, PII-safe result buckets, legacy due-follow-up separation, and existing Start/Complete and
due-date controls. Reopen UI, terminal-task recovery surfacing, `subject_closed` UI exposure,
assignment/reassignment, staff/admin/member task UI, new routes, scheduler/cron, reminders,
notifications, outbox, templates, sequences, scoring, assistance-intent execution, database
migration/RLS, historical backfill, proxy/canonical route/auth/tenancy/routing changes, Stripe,
README, AGENTS.md, and broad architecture-doc work remain blocked.
`P40-CRM31 Agent CRM Task Queue Cancellation Controls` is complete through PR `#847`, merge commit
`f00a44b3ee4d1d701dc7ba5447db4326841754b0`: the existing `/agent/crm` task queue now has
row-local cancellation controls for visible, assigned, open, lead-backed task rows only. The
implementation reuses CRM26 `cancelCrmTaskAction` through a queue-visibility guard, exposes only
`not_needed`, `duplicate`, and `created_in_error`, requires row-local inline confirmation,
preserves Start/Complete controls, due-date controls, lead links, queue markers, legacy
due-follow-up separation, canonical routes, and `apps/web/src/proxy.ts`, and passed focused unit
proof, `pnpm security:guard`, `pnpm pr:verify`, and `pnpm e2e:gate`, with remote validation,
audit, static, unit, full E2E, `e2e-gate`, Pilot Gate, gitleaks, pnpm-audit, PR finalizer,
SonarCloud, Vercel ignored-build, and Vercel Preview Comments green before merge.
Reopen UI, terminal-task recovery surfacing, `subject_closed` picker exposure,
assignment/reassignment, staff/admin/member task UI, scheduler/reminders/notifications/outbox, new
routes/API/cron, DB schema/RLS/migrations, assistance execution, auth/tenancy/routing refactors,
Stripe, README/AGENTS.md, and architecture-doc work remained out of scope.
`P40-DG09 CRM Task Queue Completed Task Recovery Design Gate` is recorded in
`docs/plans/2026-05-22-p40-dg09-crm-task-completed-recovery-design.md` after `P40-CRM31`. It
promotes exactly one next implementation slice,
`P40-CRM32 Agent CRM Task Queue Completed Task Recovery`, limited to a bounded completed-task
recovery panel below the existing `/agent/crm` open work queue. The promoted slice may show only
recently completed, assigned, lead-backed CRM tasks for the current agent, use a distinct
completed-queue DTO and pure derivation path, and add row-local reopen controls that use existing
CRM26 `reopenCrmTaskAction` with stable reason codes `follow_up_required`, `incomplete`, and
`manually_reopened`. Cancelled tasks have no authorized reopen path and must never appear in the
panel. The slice blocks cancelled-task recovery, full task history, assignment/reassignment,
staff/admin/member task UI, scheduler/cron, reminders, notifications, outbox, new routes,
DB schema/RLS/migrations, assistance execution, proxy/canonical route/auth/tenancy changes, Stripe,
README/AGENTS.md, and broad architecture-doc work.
`P40-CRM32 Agent CRM Task Queue Completed Task Recovery` is complete through PR `#849`, merge
commit `4c086bf3c8964549caa5a11e7bfa81b7c7f66b35`: the existing `/agent/crm` task queue now has a
bounded completed-task recovery panel below the open work queue. The implementation derives a
distinct completed-queue DTO for recently completed, assigned, lead-backed tasks for the current
agent, reuses CRM26 `reopenCrmTaskAction`, exposes `follow_up_required`, `incomplete`, and
`manually_reopened`, excludes cancelled tasks from the completed panel, maps terminal and stale
lifecycle states to non-disclosing copy, preserves existing open queue controls, cancellation
controls, due-date controls, lead links, legacy due-follow-up separation, canonical routes, and
`apps/web/src/proxy.ts`, and passed focused unit proof, `pnpm security:guard`, `pnpm pr:verify`,
and `pnpm e2e:gate`, with remote validation, audit, static, unit, full E2E, `e2e-gate`, Pilot
Gate, gitleaks, pnpm-audit, PR finalizer, SonarCloud, Vercel ignored-build, and Vercel Preview
Comments green before merge. Cancelled-task recovery, full task history, assignment/reassignment,
staff/admin/member task UI, scheduler/reminders/notifications/outbox, new routes/API/cron, DB
schema/RLS/migrations, assistance execution, auth/tenancy/routing refactors, Stripe,
README/AGENTS.md, and architecture-doc work remained out of scope.
`P40-DG10 CRM Task Queue Priority Adjustment Controls Design Gate` is recorded in
`docs/plans/2026-05-23-p40-dg10-crm-task-priority-controls-design.md` after `P40-CRM32`. It
promotes exactly one next implementation slice,
`P40-CRM33 Agent CRM Task Queue Priority Adjustment Controls`, limited to a narrow CRM task
priority mutation and row-local priority controls on existing visible, assigned, open, lead-backed
CRM task queue rows on `/agent/crm`. The promoted slice may add the missing `priority_updated`
domain event, `manual_priority_change` reason code, CRM26 `updateCrmTaskPriorityAction`, and
inline always-visible priority controls that reuse the existing `crm.taskQueue.priority.*` labels,
disable same-priority saves, preserve row-local pending/focus/scroll behavior, and rely on CRM26
rate-limit, audit, lifecycle-version, and revalidation semantics. It blocks assignment,
reassignment, task creation, completed-row priority editing, full task history, filters,
pagination, staff/admin/member task UI, scheduler/cron, reminders, notifications, outbox, new
routes, DB schema/RLS/migrations, assistance execution, proxy/canonical route/auth/tenancy
changes, Stripe, README/AGENTS.md, and broad architecture-doc work.

`P40-DG10A CRM33 Priority History Constraint Hardening Decision` is recorded in
`docs/plans/2026-05-23-p40-dg10a-crm33-priority-history-constraint-hardening.md` after `P40-DG10`
and before `P40-CRM33` implementation. It amends DG10 only to authorize the minimum
`crm_task_history` check-constraint widening required for the DG10-approved `priority_updated`
event and `manual_priority_change` reason code to persist. The authorization is limited to the
active Drizzle schema check definitions, one additive migration plus required migration metadata,
and focused schema/database proof. It does not authorize new tables, columns, indexes, RLS
policies, tenant functions, grants, backfills, destructive migrations, trigger changes, repository
redesign, broader task persistence changes, proxy/canonical route/auth/tenancy changes, Stripe,
README/AGENTS.md, or broad architecture-doc work.

`P40-CRM33 Agent CRM Task Queue Priority Adjustment Controls` is complete through PR `#852`, merge
commit `722576e12ca7d993174aff51f2b95375cde7bd86`: the existing `/agent/crm` task queue now has a
narrow priority mutation foundation, row-local priority controls for visible, assigned, open,
lead-backed CRM task rows, and the DG10A-authorized minimum `crm_task_history` constraint widening
for `priority_updated` and `manual_priority_change`. The implementation preserved Start/Complete
controls, due-date controls, cancellation controls, completed-task recovery controls, lead links,
queue markers, legacy due-follow-up separation, canonical routes, and `apps/web/src/proxy.ts`.
Assignment/reassignment, task creation, completed-row priority editing, full task history,
filters/pagination, staff/admin/member task UI, scheduler/reminders/notifications/outbox, new
routes/API/cron, broader DB schema/RLS/migrations, assistance execution, auth/tenancy/routing
refactors, Stripe, README/AGENTS.md, and architecture-doc work remained out of scope. P40 is now
closed as the CRM task queue product-depth tranche.

`P41 Product Surface UX Professionalism` is now open after completed `P40`. `P41-DG01 Post-CRM33
UI/UX Slice Selection Design Gate` is recorded in
`docs/plans/2026-05-23-p41-dg01-post-crm33-ui-ux-slice-selection.md` after mandatory Sonnet 4.6
architecture/scope review through Copilot CLI. It closes P40/CRM33 and promotes exactly one next
slice, `P41-UX01 Product Surface UX Audit And Slice Ranking`. The promoted slice is
docs-and-evidence only: it reviews existing canonical public, member, agent, staff, and admin surfaces,
records concrete UX and workflow issues, ranks bounded implementation candidates, and recommends at
most one follow-up implementation slice for a later promotion gate. It does not authorize runtime
UI edits, additional CRM behavior, new routes, proxy/canonical route/auth/tenancy changes,
schema/RLS/migrations, Stripe, README/AGENTS.md, or broad architecture-doc work.

`P41-UX01 Product Surface UX Audit And Slice Ranking` is complete as a docs-and-evidence audit in
`docs/plans/2026-05-23-p41-ux01-product-surface-ux-audit.md`. The audit inspected existing
canonical public, member, agent, staff, and admin surfaces and ranked the accumulated `/agent/crm`
task-row action density after P40 as the clearest next product-professionalism risk. It recommends
exactly one next design gate, `P41-DG02 Agent CRM Task Queue Action Density And Mobile Ergonomics
Design Gate`, before any runtime UI implementation. P41-UX01 did not edit runtime UI, routes,
proxy, auth, tenancy, schemas, tests, messages, Stripe, README/AGENTS.md, or architecture docs.

`P41-DG02 Agent CRM Task Queue Action Density And Mobile Ergonomics Design Gate` is complete in
`docs/plans/2026-05-23-p41-dg02-agent-crm-task-queue-ergonomics-design.md` after mandatory
Sonnet 4.6 architecture/scope review through Copilot CLI and Gemini Pro product/mobile/accessibility
review. The gate promotes exactly one next implementation slice, `P41-UX02 Agent CRM Task Queue
Action Density And Mobile Ergonomics`. The promoted slice is presentation-only on the existing
`/agent/crm` task queue: it keeps lifecycle action and lead navigation immediately visible, groups
due-date, priority, and cancellation controls into a row-local secondary-actions disclosure, proves
mobile and dense-desktop behavior, and preserves existing CRM26 actions and P40 behavior. It does
not authorize CRM runtime capability, server-action payload changes, domain state-machine changes,
routes, proxy/canonical route/auth/tenancy changes, schema/RLS/migrations, Stripe, README/AGENTS.md,
or architecture-doc work.

`P41-UX02 Agent CRM Task Queue Action Density And Mobile Ergonomics` is complete through PR `#856`,
merge commit `aa9237a08542651099fc2e1f30820b8aca06a73e`. The implementation hardened the existing
`/agent/crm` task queue presentation by keeping lifecycle work and lead navigation immediately
visible, grouping due-date, priority, and cancellation controls into row-local secondary actions,
and preserving CRM26 server actions, CRM task behavior, completed recovery, lead links, queue
markers, legacy due-follow-up behavior, canonical routes, and `apps/web/src/proxy.ts`. Runtime CRM
capability, server-action payload changes, domain state-machine changes, routes, proxy/canonical
route/auth/tenancy changes, schema/RLS/migrations, Stripe, README/AGENTS.md, and architecture-doc
work remained out of scope.

`P41-DG03 Admin/Ops Filter Wrapping Hardening Design Gate` is complete in
`docs/plans/2026-05-24-p41-dg03-admin-ops-filter-wrapping-design.md` after mandatory Sonnet 4.6
architecture/scope review through Copilot CLI and Gemini Pro product/mobile/accessibility review.
The gate promotes exactly one next implementation slice, `P41-UX03 Admin/Ops Filter Wrapping
Hardening`. The promoted slice is presentation-only on the shared `OpsFiltersBar` and existing
`/admin/claims` filter composition: it hardens wrapping and stacking for tabs, search, assignment,
and diaspora-origin filters; preserves query semantics, pending feedback, active inertness, stable
markers, routes, proxy, auth, tenancy, data reads, and persistence; and proves mobile, dense
desktop, and standard desktop behavior. It does not authorize new pages, filter semantics changes,
query-state architecture changes, data fetching changes, broad ops redesign, CRM runtime behavior,
schemas, migrations, RLS, Stripe, README/AGENTS.md, or architecture-doc work.

`P41-UX03 Admin/Ops Filter Wrapping Hardening` is complete through PR `#858`, merge commit
`599c3fb9a647317346d1f350588f1f4db6ac6d83`. The implementation hardened the shared
`OpsFiltersBar` and existing `/admin/claims` filters so tabs, search, assignment filters, and
diaspora-origin filters wrap or stack predictably while preserving props, links, search behavior,
query parameters, pending feedback, active inertness, stable markers, routes, proxy, auth, tenancy,
data reads, schemas, migrations, Stripe, README/AGENTS.md, and architecture-doc boundaries. Focused
unit, i18n, targeted browser, scope, security, local required-gate, remote E2E, Pilot Gate, Sonar,
and PR checks were green before merge.

`P41-DG04 Member Dashboard Visual Hierarchy Hardening Design Gate` is complete in
`docs/plans/2026-05-24-p41-dg04-member-dashboard-visual-hierarchy-design.md` after mandatory
Sonnet 4.6 architecture/scope review through Copilot CLI and Gemini Pro product/mobile/
accessibility review. The gate promotes exactly one next implementation slice, `P41-UX04 Member
Dashboard Visual Hierarchy Hardening`. The promoted slice is presentation-only on the canonical
`/member` dashboard rendered by `MemberDashboardView`: it makes the first visible dashboard region
task-first, moves the `dashboard-heading` marker into that task-first region, adds accessible
priority/secondary region markers, quiets secondary service cards, removes false interactive
affordances and the no-op `Explore All` control, preserves existing member links and empty/active
claim states, proves authenticated mobile/dense/desktop viewport behavior, and keeps routes, proxy,
auth, tenancy, data reads, runtime behavior, schemas, migrations, Stripe, README/AGENTS.md, and
architecture-doc boundaries unchanged.

`P41-UX04 Member Dashboard Visual Hierarchy Hardening` is complete through PR `#861`, merge commit
`e6be4b2c302df928efe17740196c949c72a35e22`. The implementation made the canonical `/member`
dashboard task-first by adding priority and secondary dashboard regions, moving the
`dashboard-heading` marker into the priority region, quieting the secondary hero and service
ecosystem content, removing the no-op `Explore All` affordance, keeping service ecosystem tiles
static, and hardening localized responsive wrapping for secondary dashboard content. It preserved
existing member links, active/empty claim behavior, routes, proxy, auth, tenancy, data reads,
runtime behavior, schemas, migrations, Stripe, README/AGENTS.md, and architecture-doc boundaries.
Focused unit, i18n, type, lint, security, local PR-style E2E, remote validation, audit, static,
unit, full E2E, `e2e-gate`, Pilot Gate, gitleaks, pnpm-audit, PR finalizer, SonarCloud, Vercel
ignored-build, and Vercel Preview Comments were green before merge.

`IDA-DG02 Auth Portal Visual Refresh Design Gate` is complete in
`docs/plans/2026-05-25-ida-dg02-auth-portal-visual-refresh-design.md` after Sonnet 4.6 review
through the Claude CLI and Gemini Pro product/mobile/accessibility review. The gate follows merged
PR `#863` / `IDA-MH01` and promotes exactly one next implementation slice, `IDA-AUTH01 Portal
Sign-In Visual Refresh v0`. The promoted slice is presentation-only on the existing localized
login route: add a route-local split/stacked auth portal shell around the existing login form,
preserve `auth-ready` and login-form markers, support TenantSelector-present and TenantSelector-absent
states, add localized neutral portal copy for `en`, `sq`, `mk`, and `sr`, preserve `plan`
and `next` query continuity, and prove mobile `360`, `390`, and `430` behavior. The gate blocks
proxy edits, canonical-route changes, auth/session/tenant behavior changes, billing/schema/backend
work, login flow rewrites, forbidden claims, remote asset fetches, Stripe, README, AGENTS, and
architecture-doc changes.

`IDA-AUTH01 Portal Sign-In Visual Refresh v0` is complete through PR `#867`, merged at
`8e9ecab5aacb00daf8ee86d87137fde278851aa0`. The existing localized login route now has a
route-local split/stacked auth portal shell around the existing login form, neutral localized
portal copy for `en`, `sq`, `mk`, and `sr`, preserved `auth-ready` and login-form markers,
TenantSelector-present and TenantSelector-absent behavior, `LoginForm` auth/redirect/OAuth
behavior, and `plan`/`next` query continuity. It preserved canonical routes, proxy,
auth/session/tenant behavior, billing, schema, backend, Stripe posture, README/AGENTS.md, and
architecture-doc boundaries, avoided forbidden claims and remote asset fetches, and kept remote
validation, audit, static, unit, full E2E, `e2e-gate`, Pilot Gate, gitleaks, pnpm-audit, PR
finalizer, SonarCloud, Vercel ignored-build, and Vercel Preview Comments green before merge.

`IDA-DG03 Member-Action Hero Render Design Gate` is complete in
`docs/plans/2026-05-26-ida-dg03-member-action-hero-render-design.md` after mandatory Sonnet 4.6
review through Copilot CLI and Gemini Pro 3.1 premium product/design review through Gemini CLI using
`gemini-3.1-pro-preview`. The gate promotes exactly one next implementation slice, `IDA-MH02
Member-Action Hero Render Contract`, scoped to focused dashboard render proof for the existing
`missing_documents` and `authorization_needed` member-home hero states. The promoted slice keeps the
pure resolver small, preserves one dominant hero CTA, preserves the canonical `/member` route and
dashboard markers, keeps localized `en`/`sq`/`mk`/`sr` copy discipline, requires authorization
matching-breadth proof, interactive primary CTA proof, active-member anti-conversion proof, and
mobile tap-target/copy-density attention if UI copy or classes change, and blocks proxy edits,
canonical-route changes, auth/session/tenant behavior changes, billing/schema/backend work, Stripe,
broad dashboard redesign, README, AGENTS, and architecture-doc changes.

`IDA-MH02 Member-Action Hero Render Contract` is complete through PR `#870`, squash merge commit
`101a0bf67fbb8264ba1836e737e12be0a47ac0fe`. The implementation proved the
`authorization_needed` and `missing_documents` dashboard hero render contracts, centralized
member-action priority in `resolveClaimActionKind`, preserved upload-document and authorization
precedence over generic member actions, made active-member anti-conversion proof explicit, removed a
brittle multi-locale regex from dashboard tests, and kept the canonical `/member` route, dashboard
markers, proxy, auth, tenancy, billing, schema, backend, Paddle, Stripe posture, README,
AGENTS.md, and architecture-doc boundaries unchanged. Remote CI, Pilot Gate, full PR E2E, Sonar,
CodeQL, gitleaks, pnpm-audit, Vercel, and PR finalizer checks were green before merge.

`IDA-DG04 Post-MH02 Closeout And Generic Member-Action Hero Design Gate` is complete in this
program/tracker record. The gate closes `IDA-MH02` and promotes exactly one next implementation
slice, `IDA-MH03 Generic Member-Action Hero State`, scoped to making already-classified generic
`member_action` claims first-class in the member-home hero. The promoted slice may add a generic
hero state, localized administrative copy, typed action-specific CTA labels for `review_offer` and
`provide_info`, contextual accessible CTA text, a stable primary CTA test id, and focused
resolver/dashboard render tests while preserving upload-document and authorization priority,
`NextStepCard` behavior, canonical routes, proxy, auth, tenancy, billing, schema, backend, Paddle,
Stripe, README, AGENTS, and architecture-doc boundaries. The implementation must add
`member_action` to the hero state union, cover every `MemberHero` sub-key consumed by the component,
update the authorization-inference negative resolver test to expect generic `member_action` when a
valid generic `nextMemberAction` is present, and prove active-member anti-conversion plus i18n and
forbidden-claim safety. The gate includes Sonnet 4.6 review through the Copilot fallback after a
hung Claude CLI attempt and Gemini Pro 3.1 preview product/mobile/accessibility review after
`gemini-3.1-pro` returned `ModelNotFoundError`.

The March 3-5 advisory-governance tranche remains valuable background context, but it is no longer the active sequencing mechanism for repository execution.

## Program Goals

1. Keep the completed `V01` through `V05` convergence work inspectable and stable.
2. Keep the completed AI runway boundary slice reversible and off the request path.
3. Keep `AI02` complete as the canonical provenance persistence layer for durable AI runs.
4. Keep `AI03` complete as the canonical queued policy extraction and background execution slice.
5. Keep `AI04` complete as the canonical run-status and human-review surface on top of the existing provenance and workflow layers, with no routing, auth, or tenancy refactors.
6. Keep `AI05` complete as the narrow claim-and-legal workflow slice on top of the existing provenance, background execution, and review surfaces, without reopening routing, auth, or tenancy boundaries.
7. Keep `AI06` complete as the canonical fixture-eval, telemetry, and PR CI signaling slice on top of the existing typed AI boundary and workflow surfaces.
8. Keep `D01` complete as the canonical PR full-`e2e:gate` CI lane for product changes.
9. Keep `D02` complete as the deterministic cross-agent messaging isolation coverage slice without bypassing canonical routes or tenant boundaries.
10. Keep `D03` complete as the canonical blocking 60% repository coverage floor in CI.
11. Keep `D04` complete as the canonical blocking `gitleaks` scan across the live PR, push, and scheduled security surfaces.
12. Keep `D05` complete as the canonical staging Supabase plus preview-environment separation slice without changing production auth, routing, or tenancy architecture.
13. Keep `D06` complete as the canonical incident-response playbook and keep `D07` complete as the Sentry burn-rate alert wiring slice.
14. Keep `D08` complete as the narrow RLS proof slice on four critical tables without reopening a broad repo-wide RLS campaign.
15. Preserve the broader GPT-5.4 runway as input without widening committed `v0.1.0` scope beyond the completed `AI01` through `AI06` tranche, the completed `P-1` tranche, the completed `P1C` tranche, the completed `P1T` tranche, the completed `P2` tranche, the completed `P3` tranche through `M06`, and the newly committed `P4` tranche through `S05`, `S07`, `S06`, `S08`, `S09`, and `S10`.
16. Land `P1T` as the canonical Free Start and Trust UX tranche without reopening routing, auth, tenancy, or request-path AI decisions.
17. Keep `C01` complete as the canonical coverage-matrix publication slice across pricing, checkout, and member surfaces.
18. Keep `C02` as the canonical success-fee calculator and example-pricing slice.
19. Keep `C03` as the canonical Free Start and hotline disclaimer slice.
20. Keep `C04` as the canonical annual billing, cancellation, refund, and cooling-off publication slice.
21. Keep `C05` as the canonical claims-first scope-tree and referral-boundary slice.
22. Keep `C06` as the canonical commercial-funnel instrumentation slice.
23. Keep `T01` as the canonical hero, trust-strip, and footer-safety-net update slice.
24. Keep `T02` as the canonical Free Start claim-pack generator shell slice.
25. Keep `T03` as the canonical confidence-score and recommended-next-step slice.
26. Keep `T04` as the canonical evidence prompts, privacy badge, and SLA microcopy slice.
27. Keep `T05` as the canonical `/services` alignment slice for the coverage matrix and referral boundaries.
28. Keep `T06` complete as the canonical i18n and component-test coverage slice for the new trust surfaces.
29. Keep `P2` complete as the canonical Billing And Terms Hardening tranche without reopening routing, auth, tenancy, or request-path AI decisions.
30. Keep `B01` through `B06` complete as the canonical in-place billing hardening queue for Stripe reference removal, Paddle webhook safety, dunning enforcement, and billing auditability.
31. Keep `B07` complete as the canonical escalation agreement and signed commercial terms persistence slice so accepted staff-led recovery work cannot start without inspectable fee, cap, and payment-authorization state.
32. Keep `B08` complete as the canonical annual membership cancellation, refund, and cooling-off enforcement slice so live product behavior matches the published commercial contract.
33. Keep `B09` complete as the canonical success-fee collection fallback slice so accepted recovery matters have a clear deduction, charge, and invoice order.
34. Keep `B10` complete as the canonical commercial audit trail slice so recovery acceptance, cancellation, and collection actions stay inspectable.
35. Keep `P3` complete as the canonical Commercial Actions And Contract Enforcement tranche without reopening routing, auth, tenancy, or request-path AI decisions.
36. Keep `M01` complete as the canonical commercial mutation-path audit slice so Free Start submission, escalation request, agreement acceptance, matter consumption, cancellation, and roster import stay classified before typed Server Action migration, idempotency rollout, or route retirement work begins.
37. Keep `M02` complete as the canonical typed Server Action migration slice so Free Start, escalation request, escalation acceptance, and cancellation converge on `_core.ts` validation paths before broader idempotency or route-retirement work begins.
38. Keep `M03` complete as the canonical commercial idempotency slice so duplicate Free Start, escalation, and billing submissions on the typed commercial action paths do not double-create work or double-apply commercial state before matter guards or route retirement begin.
39. Keep `M04` complete as the canonical matter-consumption guard slice so staff-led recovery on the canonical typed action path consumes and enforces allowance, blocks exhausted allowance by default, and records an explicit staff override reason before recovery can begin without available allowance.
40. Keep `M05` complete as the canonical escalation decision audit slice so each acceptance or decline on the canonical staff action path records actor, decision time, reason, and resulting next state before route retirement work begins.
41. Keep `M06` complete as the canonical superseded commercial write-path retirement slice so migrated Free Start, claim submission, staff commercial action, and membership cancellation callers stay converged on the `.core.ts`-backed server actions and no compatibility-only commercial write shim reappears on the active path as `P4` begins.
42. Land `P4` as the canonical Staff Operations And Acceptance Control tranche without reopening routing, auth, tenancy, or request-path AI decisions.
43. Keep `S01` complete as the canonical staff claim queue and filter work slice so the existing actionable staff queue surface remains preserved before SLA-state expansion, matter-ledger visibility, acceptance-control enforcement, or guidance-only handling rules widen the live staff operations scope.
44. Keep `S02` complete as the canonical manual claim assignment slice so staff can assign claims manually within the existing tenant and branch guardrails while load-based auto-assignment remains deferred before SLA-state expansion, matter-ledger visibility, acceptance-control enforcement, or guidance-only handling rules widen the live staff operations scope.
45. Land `S03` as the canonical claim stage history and member-visible tracker slice so submitted claims and staff-published status changes keep a durable public lifecycle history on the canonical member claim detail surface before later SLA-state, matter-ledger, acceptance-control, payment-prerequisite, or guidance-only slices widen the live `P4` scope.
46. Land `S04` as the canonical SLA-state visibility slice so staff and member claim surfaces distinguish waiting-for-member-information from actively running SLA time before matter-ledger, acceptance-control, payment-prerequisite, or guidance-only slices widen the live `P4` scope.
47. Land `S07` as the canonical matter-ledger and allowance-visibility slice so staff and member claim detail surfaces show annual matter usage and remaining allowance from the shared `M04` allowance semantics before `S06` messaging refinements, acceptance-control, payment-prerequisite, or guidance-only slices widen the live `P4` scope.
48. Keep `GA01` complete as the canonical group roster import slice so employers and associations can create sponsored member seats by roster or CSV on the active agent write path before broader `P4G` dashboard, activation, or privacy-consent surfaces are committed.
49. Land `S06` as the canonical secure member-staff messaging slice, promoted after `S07`, so the routed member and staff claim-detail surfaces both expose tenant-scoped public claim messaging while staff-only internal notes remain isolated and read-receipt polish stays optional.
50. Keep `S08` complete as the canonical recovery-decision gate slice so staff must explicitly accept or decline recovery matters before `negotiation` or `court` can begin, decline reasons remain categorized, and member claim-detail surfaces continue to receive only the safe decision state.
51. Land `S09` as the canonical accepted-case commercial prerequisite slice so accepted recovery matters cannot move into `negotiation` or `court` without the full agreement snapshot and a usable success-fee collection path on the canonical staff path.
52. Land `S10` as the canonical guidance-only commercial-enforcement slice so guidance-only and referral-only matters cannot move into staff-led recovery or success-fee handling, and staff claim-detail surfaces distinguish that launch-scope restriction from generic accepted-case validation.
53. Keep `GA02` complete as the canonical aggregate-only group dashboard slice so office-tier group operators can see activations, usage, open-case counts, and SLA-safe portfolio metrics without exposing claim facts, notes, or documents.
54. Keep `GA03` complete as the canonical sponsored-seat activation and self-upgrade slice so sponsored members can activate coverage and move from individual to family coverage without reopening portal or tenancy architecture.
55. Keep `GA04` complete as the canonical privacy and consent negative-test slice so group admins cannot see claim facts, notes, or documents without explicit member consent.
56. Keep `G07` complete as the first explicit `P6` release-gate slice so release-candidate builds keep proving the published commercial contract surfaces still expose the coverage matrix, fee calculator, disclaimers, and refund terms before broader staging-only validation is widened.
57. Keep `G08` complete as the explicit `P6` release-gate slice so Free Start still states informational-only status and group dashboards still expose only aggregate data before later staging-only enforcement checks are widened.
58. Keep `G09` complete as the explicit `P6` release-gate slice so members and staff still see correct matter counts and correct SLA states on canonical claim-detail surfaces before later accepted-case enforcement checks are widened.
59. Keep `G10` complete as the explicit `P6` release-gate slice so escalation-agreement acceptance, signature, and collection-fallback enforcement stay validated in release-candidate runs after matter-and-SLA proof is locked in.
60. Land `P7` as the canonical Pilot Readiness And Release Evidence tranche so the completed release-gate and pilot machinery becomes one governable pilot-entry operating system across member and agent activity, branch-manager branch oversight, staff claim processing, and admin decision custody rather than a split set of scripts, reports, and local-only evidence conventions.
61. Land `R01` as the canonical pilot-ready artifact-contract slice so every pilot-entry run creates a release report, a copied pilot evidence index, and a machine-readable evidence pointer row with stable references.
62. Land `R02` as the canonical readiness-command authority slice so `pilot:check`, `release:gate:prod`, and `scripts/pilot-verify.sh` have documented, non-overlapping roles before live pilot entry begins.
63. Keep `R03` complete as the canonical deterministic pilot-evidence slice so daily pilot operation records day/date, owner, status, report path, bundle path, incident count, severity, decision, and branch-manager-reviewed or branch-level context where relevant in one copied per-pilot evidence index instead of relying on memory or ad hoc notes.
64. Keep `R04` complete as the canonical continue-pause-hotfix-stop decision-proof slice so daily and weekly pilot decisions remain repo-backed artifacts that distinguish branch-level recommendation from admin-level decision when applicable and can reference rollback targets and re-validation requirements.
65. Keep `R05` complete as the canonical pilot-ready tag-discipline slice so rollback targets and resume rules point to a real `pilot-ready-YYYYMMDD` tag with fresh re-validation evidence before pilot operations resume.
66. Keep `R06` complete as the canonical readiness-cadence slice so a source-backed 3-day qualifying green cadence replaces any need for historical A22-style inference in live governance.
67. Keep `R07` complete as the canonical pilot observability-and-incident-evidence slice so log sweeps, KPI conditions, incident severity, branch-local versus system-wide classification, and decision artifacts stay linked in one copied evidence index before a live pilot is declared governable.
68. Keep `R08` complete as the canonical ranked pilot-entry operator-flow slice so the pilot-entry command path is inspectable and repeatable across pre-launch, gate proof, launch-day verification, branch-manager review or escalation, and admin-level daily logging before a live pilot is declared governable.
69. Land `P8` as the canonical post-`P7` pilot redesign tranche so the repo preserves completed 14-day pilot-readiness history while superseding the old live-pilot shape with a reset-gated 7-day rehearsal model.
70. Keep `P8R` complete as the canonical reset-gate hardening slice so new pilot runs begin only after fresh memory retrieval top hits, fresh pilot checks, fresh release-gate proof, and clear working-note versus canonical-evidence rules are inspectable.
71. Keep `RG01` complete as the canonical rehearsal-environment check slice so `pnpm pilot:check` passes deterministically in the intended Node 20 pilot environment before a new rehearsal day is allowed to start.
72. Keep `RG02` complete as the canonical operator-log-command alignment slice so pilot docs match installed `vercel logs` CLI behavior and no removed flags survive in ranked operator instructions.
73. Keep `RG03` complete as the canonical observability-to-decision sequencing slice so a fresh observability row can be followed immediately by a fresh decision row when operators respect the documented command order.
74. Keep `RG04` complete as the canonical daily-sheet versus copied-evidence boundary slice so operators can record working notes, memory advisories, and orchestration detail without confusing them for the canonical evidence rows that govern pilot decisions.
75. Keep `RG05` complete as the canonical fresh-preflight slice so a new pilot id produces a GO release report, copied evidence index, pointer row, and reset-gate proof before `PD02+` rehearsal work begins.
76. Land `P8P` as the canonical 7-day rehearsal tranche so pilot execution becomes one bounded week of distinct scenario families rather than a noisy 14-day simulation that mixes reset-gate and scenario-proof concerns.
77. Keep `SP01` complete as the canonical `PD01` release-and-artifact baseline so the new pilot id already has a green day with release proof, copied evidence, observability row, decision row, and daily-sheet notes before rollback and role-flow scenarios widen coverage.
78. Keep `SP02` complete as the canonical `PD02` rollback-and-resume baseline so rollback tag discipline, resume inspection, and decision-proof continuity stay proven for the same pilot id, including the stale local rollback-tag correction after `main` advanced.
79. Keep `SP03` through `SP06` complete as the canonical middle rehearsal slices so closed-loop role flow, SLA or matter pressure, privacy or RBAC stress, and communications or incident handling each keep one explicit day instead of being blended into one vague pilot week.
80. Keep `SP07` complete as the canonical executive-review slice so the 7-day rehearsal ends with one fixed recommendation artifact that links KPI condition, incident evidence, branch-manager input, and admin decision custody.
81. Keep `P12` complete as the canonical Program C business-model integrity tranche after completed `P11`, without reopening routing, auth, tenancy, or broad modernization scope.
82. Keep `PC01` complete as the canonical membership-fulfillment slice so all paid-member paths produce the same durable annual-membership state, annual membership is the only runtime-critical commercial shape left, and member-facing success copy matches back-office truth.
83. Keep `PC02` complete as the canonical ownership-and-attribution slice so subscription, user, and agent-client surfaces converge on one durable ownership graph for operations, renewals, and reporting.
84. Keep `PC03` complete as the canonical commission-and-referral finance slice so tenant-safe commission reads and actions align to canonical ownership and auditable payout rules.
85. Keep `PC04` complete as the canonical branch operating-truth slice so branch dashboards no longer depend on placeholder or stale financial signals.
86. Keep `PC05` complete as the canonical lifecycle-and-reporting slice so renewal, churn, grace-period, conversion, and liability reporting reconcile to one durable membership truth.
87. Keep `PC06` complete as the canonical enterprise-safe control slice so edge cases that can silently distort ownership, membership, branch, or finance state are explicitly hardened before later workflow or modernization tranches.
88. Keep `P13` complete as the canonical v1.0.0 Funnel Completion tranche after completed `P12`, limited to claim-pack generation, deterministic confidence scoring, and critical lifecycle notification wiring.
89. Keep `P13-FC01` complete as the canonical ClaimPack domain generator slice so the existing Free Start shell produces a real pack with intake facts, evidence checklist, complaint or notice letter, timeline, recommended next step, and generation timestamp.
90. Keep `P13-FC02` complete as the canonical public Free Start integration slice so the generated ClaimPack appears on the existing public Free Start surface without moving the flow behind member-only routes.
91. Keep `P13-N01` complete as the canonical critical lifecycle notification slice so missing claim-pack and claim-handling lifecycle events are dispatched through the existing email, push, and in-app infrastructure with verified-recipient guards.
92. Keep `P14` complete as the canonical post-`P13` Member Experience Guidance tranche, limited to the member dashboard experience layer and not a broad portal redesign.
93. Keep `P14-MD01` complete as the canonical member-dashboard next-action guidance slice so members can see claim, document, membership, and support readiness from existing dashboard data.
94. Land `P15` as the canonical post-`P14` Production Professionalism Hardening tranche so launch-blocking authorization, tenant-safety, production-runtime, and release-gate gaps found on 2026-04-24 are closed before pilot launch is treated as professionally ready.
95. Land `P15-G01` as the registration authorization guard slice so `/api/register` cannot create member, subscription, or agent-attribution state unless the caller has an explicit agent role accepted by the canonical agent-registration contract.
96. Land `P15-G02` as the protected-route fail-closed proxy slice so `/member`, `/agent`, `/staff`, and `/admin` requests do not pass through when session introspection is unknown, throttled, or errored.
97. Land `P15-S01` as the production runtime and bearer-token hardening slice so debug/test surfaces, predictable signing fallback, public test activation flags, RLS fallback, rate-limit fail-open behavior, and E2E diagnostic headers cannot weaken production posture.
98. Land `P15-S02` as the claim-upload and activity-read correctness slice so upload confirmations are bound to server-issued storage intent and member activity reads enforce tenant plus role or assignment scope independent of RLS.
99. Land `P15-QA01` as the professionalism proof slice so E2E contract checks, launch-gate commands, lint policy, docs/env drift, and release evidence return to a green, current, auditable state before a fresh production-professionalism review.
100. Keep `P18` complete as the canonical post-`P17` Pilot-Learned Trust And Activation UX tranche without reusing the already-completed `P8` label.
101. Keep `P18-DG01` complete as the design-gate slice that promotes the next experience-oriented queue from repo-backed planning inputs without product-code changes.
102. Keep `P18-UX01` complete as the evidence-ranking slice that ranked repo-custodied pilot-learned activation and trust evidence before implementation work started.
103. Keep `P18-UX02` complete as the bounded member claim progress clarity implementation slice on existing canonical surfaces.
104. Keep `P18-DG02` complete as the design-gate slice that selects the next bounded P18 implementation target after member progress clarity.
105. Keep `P18-UX03` complete as the bounded member trust and SLA clarity implementation slice on existing canonical surfaces.
106. Land `P19` as the canonical post-`P18` Agent CRM Readiness And Boundary Hardening tranche without broad CRM or agent-workspace redesign.
107. Keep `P19-DG01` complete as the design-gate slice that selects the next bounded CRM implementation target from existing repo-backed CRM surfaces.
108. Keep `P19-CRM01` complete as the agent CRM tenant-scoped read-contract implementation slice on existing canonical surfaces.
109. Keep `P19-DG02` complete as the design-gate slice that selects the next bounded CRM-readiness implementation target after CRM01.
110. Keep `P19-CRM02` complete as the agent dashboard CRM KPI tenant read-contract implementation slice on the existing canonical `/agent` dashboard.
111. Keep `P19-DG03` complete as the design-gate slice that selects the next bounded CRM-readiness implementation target after CRM02.
112. Keep `P19-CRM03` complete as the agent CRM mutation tenant-boundary contract implementation slice on existing legacy agent CRM status and activity actions.
113. Land `P20` as the canonical post-`P19` v1.0.0 Release Declaration And Version Authority tranche without reopening product behavior or backlog redesign scope.
114. Keep `P20-DG01` complete as the design-gate slice that selects the next bounded release-governance implementation target after P19.
115. Promote `P20-RG01` as the v1.0.0 release declaration and version-authority implementation slice.
116. Keep `P20-RG01` complete as the v1.0.0 release declaration and manifest-version authority reconciliation slice.
117. Land `P21` as the canonical post-`P20` v1.0.0 live-surface revalidation tranche before any post-v1 product expansion is promoted.
118. Promote `P21-QA01` as the v1.0.0 live-entry confidence slice for launch-critical dashboard, tenant-host, role-boundary, unified-agent, and Paddle production-posture verification.
119. Land `P22` as the canonical post-`P21` Production Go-Live Operations tranche before live traffic is opened.
120. Promote `P22-GO01` as the production go-live readiness and smoke slice for deployment identity, tenant-host mapping, environment posture, Paddle production posture, backup and rollback readiness, observability, alerting, and launch-critical production smoke proof.
121. Keep `P22-GO02` complete as the narrow `/register` G07 commercial-promise blocker fix before rerunning production readiness.
122. Keep `P22-GO03` complete as the narrow member-claim SLA status copy fix that cleared the final production `G09` release-gate blocker.
123. Keep `P22-GO04` complete as the launch operations acceptance record that closes backup/recovery point, rollback owner and procedure, alert/on-call owner, incident decision owner, launch window, and final go/no-go authority.
124. Land `P23` as the canonical post-`P22` Live Product Responsiveness tranche without broad UI/UX redesign, CRM redesign, agent-workspace redesign, proxy changes, canonical route renames, auth or tenancy refactors, schema changes, Stripe reintroduction, or product analytics expansion.
125. Keep `P23-PERF01` complete as the portal navigation responsiveness slice so existing member and admin authenticated surfaces provide deterministic route-transition feedback and loading skeletons while preserving canonical route, tenant, and access-control contracts.
126. Complete `P23-DG01` as the post-`P23-PERF01` design gate that ranks remaining responsiveness candidates and promotes `P23-PERF02 Staff Claims Queue Responsiveness` as the next bounded implementation slice on existing staff surfaces.
127. Complete `P23-DG02` as the post-`P23-PERF02` design gate that ranks remaining responsiveness candidates and promotes `P23-PERF03 Admin Claims Filter Responsiveness` as the next bounded implementation slice on existing admin claims surfaces.
128. Keep `P23-PERF03` complete as the admin claims filter responsiveness slice so existing admin claims search and filter transitions expose deterministic local pending feedback while preserving the canonical admin claims route, query contract, and access-control boundaries.
129. Complete `P23-DG03` as the post-`P23-PERF03` design gate that ranks remaining responsiveness candidates and promotes `P23-PERF04 Member Claims Filter Responsiveness` as the next bounded implementation slice on the existing canonical member claims surface.
130. Keep `P23-PERF04` complete as the member claims filter responsiveness slice so existing member claims search, status, and pagination transitions expose deterministic local pending feedback while preserving the canonical member claims route, query contract, table DTO, and access-control boundaries.
131. Complete `P23-DG04` as the post-`P23-PERF04` design gate that ranks remaining responsiveness candidates and promotes `P23-PERF05 Admin Users Filter Responsiveness` as the next bounded implementation slice on the existing canonical admin users surface.
132. Keep `P23-PERF05` complete as the admin users filter responsiveness slice so existing admin users search, assignment controls, Link-backed role tabs, and table viewport behavior expose deterministic pending feedback and inert in-flight controls while preserving the canonical admin users route, query contract, tenant context, and access-control boundaries.
133. Complete `P23-DG05` as the post-`P23-PERF05` design gate that ranks remaining responsiveness candidates and promotes `P23-PERF06 Agent Clients Search Responsiveness` as the next bounded implementation slice on the existing canonical agent clients surface.
134. Keep `P23-PERF06` complete as the agent clients search responsiveness slice so the existing canonical `/agent/clients` search surface exposes deterministic pending feedback and inert in-flight controls while preserving route, query, auth, tenant, and access-control contracts.
135. Land `P24` as the canonical post-`P23` lint-warning burn-down tranche without broad refactors or product behavior changes.
136. Complete `P24-DG01` as the design-gate slice that ranks lint-warning families and promotes `P24-LINT01 Unused Symbols And Stale Suppressions` as the next bounded implementation slice.
137. Keep `P24-LINT01` complete as the bounded unused-symbol and stale-suppression cleanup slice, reducing the web lint baseline from `219` warnings to `188` warnings without changing runtime contracts.
138. Complete `P24-DG02` as the explicit-`any` design-gate slice that ranks remaining warning surfaces and promotes `P24-LINT02 Claims Submit Test Typed Mocks` as the next bounded implementation slice.
139. Keep `P24-LINT02` complete as the bounded claim submit test typed-mock cleanup slice, reducing the web lint baseline from `188` warnings to `173` warnings without production-code changes.
140. Complete `P24-DG03` as the explicit-`any` design-gate slice that ranks the remaining post-`P24-LINT02` warning surfaces and promotes `P24-LINT03 Admin Claims List Test Typed Fixtures` as the next bounded implementation slice.
141. Keep `P24-LINT03` complete as the bounded admin claims list test typed-fixture cleanup slice, reducing the web lint baseline from `173` warnings to `162` warnings without production-code changes.
142. Complete `P24-DG04` as the explicit-`any` zero-baseline design gate that promotes `P24-LINT04 Web Explicit Any Zero Baseline` after the 2026-04-29 zero-warning directive.
143. Keep `P24-LINT04` complete as the zero-baseline implementation slice, reducing the web lint baseline from `162` warnings to `0` warnings without changing lint policy, route, auth, tenant, schema, Stripe, CRM, agent-workspace, product analytics, README, AGENTS, or architecture-doc contracts.
144. Treat further P24 lint-warning work as complete unless a future repo-canonical gate identifies a new lint category or regression.
145. Land `P25` as the canonical post-`P24` commercial product surface polish tranche without broad SaaS redesign, CRM redesign, agent-workspace redesign, route changes, auth changes, tenancy changes, schema changes, Stripe reintroduction, or product analytics expansion.
146. Complete `P25-DG01` as the post-`P24` design gate that ranks public commercial polish, broad CRM redesign, agent-workspace redesign, product analytics expansion, additional responsiveness/lint cleanup, and broad SaaS redesign candidates.
147. Keep `P25-UX01 Public Commercial Entry Polish` complete as the bounded implementation slice on existing localized public home, pricing, services, and Free Start/claim-pack entry surfaces.
148. Complete `P25-DG02` as the post-`P25-UX01` design gate that records live production validation, closes the active P25 implementation line, and does not promote another implementation slice without fresh bounded evidence.
149. Treat further P25/product work as unpromoted until a future repo-canonical design gate identifies a concrete next slice.
150. Land `P26` as the canonical post-`P25` Agent CRM Productization tranche without broad SaaS redesign, broad CRM redesign, agent-workspace redesign, route changes, auth changes, tenancy changes, schema changes, Stripe reintroduction, product analytics expansion, README, AGENTS, or architecture-doc updates.
151. Complete `P26-DG01` as the post-`P25` design gate that ranks domain-CRM continuation, broad SaaS redesign, broad CRM redesign, agent-workspace redesign, product analytics expansion, monitoring console-noise cleanup, and additional lint/responsiveness cleanup candidates.
152. Complete `P26-CRM01 Agent Lead Detail Productization` through PR `#602`, merge commit `f6e3b01c5b8091b731c35dd9853c225fd2293574`, as the bounded implementation slice on the existing canonical `/agent/leads/[id]` detail surface.
153. Complete `P26-DG02` as the post-`P26-CRM01` closeout design gate that records the completed implementation evidence, ranks remaining CRM/productization and broader candidates, and does not promote another implementation slice without fresh bounded evidence.
154. Treat further P26/CRM/productization work as unpromoted until a future repo-canonical design gate identifies a concrete next slice.
155. Land `P27` as the canonical post-`P26` agent workspace query-contract polish tranche without broad CRM redesign, broad SaaS redesign, agent-workspace redesign, route changes, auth changes, tenancy changes, schema changes, Stripe reintroduction, product analytics expansion, README, AGENTS, or architecture-doc updates.
156. Complete `P27-DG01` as the post-`P26` design gate that ranks agent workspace claim selection close behavior, additional CRM productization, broad CRM redesign, agent-workspace redesign, product analytics expansion, observability cleanup, repo QA MCP tooling, and broad SaaS redesign candidates.
157. Promote `P27-AGENT01 Agent Workspace Claim Selection Close Contract` as the next bounded implementation slice on the existing canonical `/agent/workspace/claims` surface.
158. Complete `P27-AGENT01` through PR `#608` as the bounded implementation slice that restores drawer close-and-query-cleanup behavior for direct `claimId` selections while preserving the existing route, query, tenant/agent read, locale, message persistence, inaccessible-claim, and unrelated-query contracts.
159. Treat further P27/agent-workspace/CRM/productization work as unpromoted until a future repo-canonical design gate identifies a concrete next slice.
160. Land `P28` as the canonical post-`P27` membership success runtime-guard polish tranche without broad CRM redesign, broad SaaS redesign, agent-workspace redesign, route changes, auth changes, tenancy changes, billing provider architecture changes, schema changes, Stripe reintroduction, product analytics expansion, README, AGENTS, or architecture-doc updates.
161. Complete `P28-DG01` as the post-`P27` design gate that ranks membership success billing test activation UI guard, additional CRM productization, broad CRM redesign, agent-workspace redesign, product analytics expansion, observability cleanup, repo QA MCP tooling, and broad SaaS redesign candidates.
162. Promote `P28-MEM01 Membership Success Billing Test Activation Guard` as the next bounded implementation slice on the existing canonical `/member/membership/success` surface.
163. Complete `P28-MEM01` by rendering the billing test activation trigger only when `isBillingTestActivationEnabled()` allows it and the required test params are present, while preserving the guarded server action and success-page active, pending, classification, redirect, refresh, and CTA behavior.
164. Land `P29` as the canonical post-`P28` repo QA MCP active-checkout guard tranche without product-code changes, route changes, auth changes, tenancy changes, billing provider architecture changes, schema changes, Stripe reintroduction, CRM redesign, broad SaaS redesign, agent-workspace redesign, product analytics expansion, README, AGENTS, or architecture-doc updates.
165. Complete `P29-DG01` as the post-`P28` design gate that ranks the observed `interdomestik_qa` active-session checkout mismatch above additional product-surface candidates.
166. Promote `P29-QA01 Repo QA MCP Active Checkout Guard` as the next bounded implementation slice within existing repo QA MCP tooling and tests.
167. Complete `P29-QA01` by making repo QA MCP project-map, status, and branch helpers expose resolved repo root and root-source signals in structured output, with status and branch text output making wrong-checkout sessions obvious, and by fixing the Pilot Gate Sonar decision path observed during PR verification.
168. Land `P30` as the canonical post-`P29` commercial UX north-star alignment tranche without homepage variant implementation, partner activation implementation, broad SaaS redesign, CRM redesign, agent-workspace redesign, route changes, auth changes, tenancy changes, schema changes, Stripe reintroduction, product analytics expansion, README, AGENTS, or architecture-doc updates.
169. Complete `P30-MEM01 Member North Star Copy Alignment` through PR `#613` as the bounded implementation slice on existing localized member-dashboard copy.
170. Treat SOCIAL homepage variants, PARTNER activation variants, Coverage Matrix placement changes, Trust Center work, Green Card public-guide work, and funnel analytics expansion as unpromoted until a future repo-canonical design gate identifies a concrete next slice.
171. Land `P32` as the canonical Domain CRM Operating System tranche after the member-support handoff alignment landed on `main` at `fb32f027`, without broad CRM redesign, broad SaaS redesign, agent-workspace redesign, PR workflow/documentation work as a promoted slice, route changes, auth changes, tenancy changes, Stripe reintroduction, README, AGENTS, or architecture-doc updates.
172. Complete `P32-DG01` as the design gate that resolves the support-handoff entity, staff-first receiving role, member trigger, claim-detail deferral, server-derived urgency, derived trust-risk thresholds, accept/reassign/close lifecycle, migration allocation, Relationship projection, claim-only Matter semantics, no stored `TrustSignal`, and exactly one next implementation slice.
173. Complete `P32-CRM01 Staff Member Support Handoff Receiving Queue` through PR `#619`, merge commit `9c1c9749365f96cc85ff82c948a71609f239fce1`, as the bounded staff-first support handoff receiving queue.
174. Complete `P32-DG02 Claim-Detail Handoff Creation Trigger Design Review` as the post-CRM01 design gate that evaluates the deferred claim-detail support-handoff trigger and promotes exactly one bounded implementation slice.
175. Complete `P32-CRM02 Claim-Detail Support Handoff Trigger` through PR `#622`, merge commit `c438cb4023049a39d49701cd1287544e924e0acf`, as the bounded claim-detail initiated, explicitly member-submitted, server-verified support handoff creation slice through the existing `/member/help` and `support_handoffs` boundaries.
176. Complete `P32-DG03 Staff Support Handoff Detail Context Design Review` as the post-CRM02 design gate that evaluates the staff-side detail/context gap after claim-linked handoffs now exist and promotes exactly one bounded implementation slice.
177. Complete `P32-CRM03 Staff Support Handoff Detail Context` through PR `#626`, merge commit `fb0a57b8330f1fbb231b70d8e50ba599f1383baf`, as the bounded staff and read-only branch-manager detail-context slice on the existing `/staff/support-handoffs` surface.
178. Complete `P32-DG04 Support Handoff Origin Source Tracking Design Review` as the post-CRM03 design gate that evaluates the now staff-visible source/origin gap and promotes exactly one bounded implementation slice.
179. Complete `P32-CRM04 Support Handoff Origin Source Tracking` as the bounded implementation slice that records claim-detail-originated support handoffs with an allowlisted claim-detail source while preserving generic member-help source, existing routes, proxy, auth, tenancy, schema, staff lifecycle, branch-manager read-only, Stripe, Relationship, Matter, TrustSignal, README, AGENTS, and architecture-doc boundaries.
180. Promote `P32-CRM05 Duplicate Open-Handoff Advisory Guard` as the bounded implementation slice that adds non-blocking member-side advisory visibility for already-active support handoffs while preserving support-handoff creation behavior, existing routes, proxy, auth, tenancy, schema, staff lifecycle, branch-manager read-only, Stripe, Relationship, Matter, TrustSignal, README, AGENTS, and architecture-doc boundaries.
181. Complete `P32-DG06 Duplicate Open-Handoff Advisory Guard Design Review` as the implementation-design gate for `P32-CRM05`, locking active statuses to `open` and `accepted`, excluding `closed`, requiring a named member-safe tenant/member-scoped advisory read contract with a narrow member-only signature, prioritizing same-claim matches when a selected claim is server validated, preserving generic member-level advisory behavior on `/member/help`, requiring localized timestamp copy, field-exclusion tests, explicit `IN` filtering, count-plus-primary query behavior, deterministic member-side E2E proof where practical, and keeping submission non-blocking while deferring any new support-handoff index until separately authorized.
182. Complete `P32-CRM05 Duplicate Open-Handoff Advisory Guard` through PR `#635`, merge commit `6867fe96130f5be9c147ee81e6c6c3ac0f428a92`, as the bounded member-safe advisory slice on the existing `/member/help` form.
183. Complete `P32-DG07 Post-CRM05 Next Slice Selection` as the closeout and next-slice gate that records CRM05 completion, keeps the optional advisory index deferred, and promotes exactly one next bounded design-review slice.
184. Promote `P32-DG08 Staff Support Handoff Reply/Follow-up Design Review` as the next bounded P32 design gate to lock member-visible staff follow-up semantics before any reply implementation starts.
185. Complete `P32-DG08 Staff Support Handoff Reply/Follow-up Design Review` as the implementation-design gate for `P32-CRM06`, locking a support-handoff-specific public response contract, keeping claim messages out of support handoff replies, preserving branch-manager read-only behavior, deferring notifications and conversation threads, and authorizing only the bounded public-response schema extension needed by CRM06.
186. Promote `P32-CRM06 Staff Support Handoff Public Response` as the bounded implementation slice that lets assigned staff respond publicly to accepted support handoffs and lets members see the latest member-safe public response on `/member/help` while preserving support submission, CRM05 advisory behavior, existing routes, proxy, auth, tenancy, staff lifecycle, branch-manager read-only, Stripe, Relationship, Matter, TrustSignal, README, AGENTS, and architecture-doc boundaries.
187. Complete `P32-CRM06 Staff Support Handoff Public Response` through PR `#638`, merge commit `0183b354f0ab25c639888c7eab3da95b6ed0eb0b`, as the bounded support-handoff-specific public-response slice on the existing `/staff/support-handoffs` and `/member/help` surfaces.
188. Complete `P32-DG09 Post-CRM06 Next Slice Selection` as the closeout and next-slice gate that records CRM06 completion, keeps notification implementation, unread state, read receipts, acknowledgements, member replies, conversation threads, and SLA timers deferred, and promotes exactly one next bounded design-review slice.
189. Promote `P32-DG10 Support Handoff Public Response Notification Design Review` as the next bounded P32 design gate to lock member notification and awareness semantics before any public-response notification implementation starts.
190. Complete `P32-DG10 Support Handoff Public Response Notification Design Review` as the implementation-design gate for the first public-response notification slice, approving only existing in-app notification-center delivery and keeping email, push, support-handoff-specific unread counters, read receipts, acknowledgements, SLA timers, member replies, conversation threads, queues, and schema extensions deferred.
191. Promote `P32-CRM07 Support Handoff Public Response In-App Notification` as the bounded implementation slice that creates a tenant-scoped in-app notification for the handoff member after successful assigned-staff public-response version updates while preserving `/member/help`, staff lifecycle semantics, branch-manager read-only behavior, proxy, routes, auth, tenancy, schema, Stripe, Relationship, Matter, TrustSignal, README, AGENTS, and architecture-doc boundaries.
192. Complete `P32-CRM07 Support Handoff Public Response In-App Notification` through PR `#643`, merge commit `8b56bd563d3f48ee7fb81080019aceed0b42f197`, as the bounded in-app notification slice for staff public-response updates.
193. Complete `P32-DG11 Post-CRM07 Next Slice Selection` as the closeout and next-slice gate that records CRM07 completion, keeps generic notification read state distinct from support-handoff acknowledgements, and promotes exactly one next bounded design-review slice.
194. Complete `P32-DG12 Support Handoff Response Acknowledgement Design Review` as the implementation-design gate for explicit member-authored acknowledgement of the current public-response version, rejecting passive notification read receipts, member replies, SLA timers, staff acknowledgement notifications, queue counters, and new routes.
195. Promote `P32-CRM08 Support Handoff Response Acknowledgement` as the bounded implementation slice that adds a member-only acknowledgement action on the existing `/member/help` public-response banner and read-only acknowledgement status on the existing `/staff/support-handoffs` detail panel while preserving proxy, routes, auth layering, tenancy architecture, Stripe, notifications-as-read semantics, member replies, SLA timers, Relationship, Matter, TrustSignal, README, AGENTS, and architecture-doc boundaries.
196. Complete `P32-CRM08 Support Handoff Response Acknowledgement` through PR `#646`, merge commit `ef0ca9628ea9ad9a81e9195c5dbb0530f37e1d50`, plus acknowledgement UX fix PR `#647`, merge commit `88aafa06f9488d94c9a8854911e27d454d1cc8ed`, as the bounded member-authored acknowledgement slice on existing member and staff support-handoff surfaces.
197. Complete `P32-DG13 Post-CRM08 Next Slice Selection` as the closeout and next-slice gate that records CRM08 completion, keeps reply implementation, conversation threads, attachments, SLA timers, unread counters, new routes, and broad CRM abstractions deferred, and promotes exactly one next bounded design-review slice.
198. Promote `P32-DG14 Member Support Handoff Reply Semantics Design Review` as the next bounded P32 design gate to lock member follow-up/reply semantics before any member reply implementation starts.
199. Complete `P32-DG14 Member Support Handoff Reply Semantics Design Review` as the implementation-design gate that approves one scalar member reply per response cycle after current-response acknowledgement, locks schema, validation, typed read-model, CAS fallback, and staff warning requirements, keeps durable reply history, conversation threads, notifications, queue counters, SLA timers, new routes, and broad CRM abstractions deferred, and preserves proxy, canonical route, auth, tenancy, Stripe, Relationship, Matter, TrustSignal, README, AGENTS, and architecture-doc boundaries.
200. Promote `P32-CRM09 Member Support Handoff Reply` as the bounded implementation slice that adds the member reply domain action, nullable scalar persistence fields, member reply form on the existing `/member/help` surface, read-only staff detail visibility, focused unit/E2E proof, and supported-locale i18n while preserving the DG14 non-goals.
201. Complete `P32-CRM09 Member Support Handoff Reply` through PR `#650` as the bounded member reply implementation slice on the existing member-help and staff support-handoff surfaces while preserving the DG14 non-goals.
202. Complete `P33-DG01 Production Maturity 10/10 Roadmap` as the docs-only roadmap gate that records the updated review status, keeps completed `P32-CRM09` closed, and promotes only the optional smallest post-CRM09 hardening candidate, `P33-SEC01 RLS Connection Role Startup Assertion`, while deferring broader 10/10 categories into later bounded gates.
203. Complete `P33-SEC01 RLS Connection Role Startup Assertion` through PR `#664`, merge commit `b3b02ca5ab56b22d28d078a46a568ea890cf375d`, as the bounded RLS connection-role hardening slice that fails closed when the tenant-scoped database connection or optional `DB_RLS_ROLE` target can bypass row-level security or cannot be verified.
204. Complete `P33-DG02 CSP Nonce Phase 0 Selection` as the post-SEC01 maturity selection gate that promotes exactly one next bounded implementation slice, `P33-SEC02 CSP Nonce Phase 0 Report-Only`, while keeping CSP enforcement, style nonce enforcement, durable CSP violation storage, build guards, storage RLS, supply-chain, operational drill, data lifecycle, performance, repo-hygiene, proxy, route, auth, tenancy, schema, Stripe, product UX, README, AGENTS, and architecture-doc changes out of this gate.
205. Promote `P33-SEC02 CSP Nonce Phase 0 Report-Only` as the bounded implementation slice that adds feature-flagged report-only nonce CSP observation through the existing security-header/proxy-logic path while preserving the current enforced CSP by default.
206. Complete `P33-SEC02 CSP Nonce Phase 0 Report-Only` through PR `#676`, merge commit `7c7f193f`, as the bounded report-only CSP nonce observation slice that preserves default/off enforced CSP behavior and exposes first-party `script-src` report evidence.
207. Complete `P33-DG03 Post-SEC02 Next Slice Selection` as the docs-only closeout gate that rejects Phase 1 enforcement until first-party script reports are resolved, appends the hypothesis-driven DG04 protocol to the CSP migration doc, and promotes exactly one next design gate.
208. Promote `P33-DG04 CSP First-Party Script Nonce Coverage Design Review` as the next bounded P33 design gate to confirm or eliminate the SEC02 first-party script report hypotheses and define the smallest safe fix before any CSP enforcement rollout.
209. Complete `P33-DG04 CSP First-Party Script Nonce Coverage Design Review` as the docs-only hypothesis gate that confirms H1 first-party Next framework/static script nonce coverage is the blocker, keeps CSP Phase 1 enforcement blocked, records the violation table, and promotes exactly one bounded implementation slice.
210. Promote `P33-SEC03 First-Party Script Nonce Coverage` as the next bounded implementation slice to make first-party framework/runtime/static scripts carry the active nonce in report mode while preserving enforced CSP behavior and the Phase 0 `enforce` guard.
211. Complete `P33-DG05 withTenantContext Build Guard Design Review` as the parallel tenancy guard design gate that defines the bounded DB access posture classifier, v1-to-v2 baseline schema bump, posture report, contributor playbook, correctness fixtures, runtime budget, release-gate review, and out-of-scope guards.
212. Promote `P33-SEC04A DB Access Posture Baseline` as the bounded implementation slice that extends the existing DB access guard with v2 tenant-posture classification and reporting while preserving product runtime behavior, proxy, canonical routes, auth, tenancy architecture, schema, Stripe, CSP, README, AGENTS, and architecture-doc boundaries.
213. Promote `P33-SEC04B DB Access Posture Burn-Down` as the follow-up tenancy slice required to reduce existing `unclassified` DB access entries from the SEC04A baseline count of `262` to `<= 80` or return to design review with evidence that the remaining entries need a different migration strategy.
214. Complete `P33-SEC04B DB Access Posture Burn-Down` as the bounded tenancy guard slice that reduces existing `unclassified` DB access posture entries from `262` to `80`, records the remaining hard-case evidence set, and preserves product behavior, proxy, canonical routes, auth, tenancy architecture, schema, Stripe, CSP, README, AGENTS, and architecture-doc boundaries.
215. Complete `P33-DG06 Next CSP Nonce Propagation Blocker Triage` as the design gate that keeps CSP Phase 1 blocked and promotes exactly one evidence slice, `P33-SEC05 Next CSP Nonce Propagation Triage`, to classify the Next framework/static nonce blocker before any renewed SEC03 implementation attempt.
216. Complete `P33-SEC05 Next CSP Nonce Propagation Triage` as the evidence slice that classifies the SEC03 blocker as a report-only architecture mismatch caused by the current non-nonce enforced CSP plus nonce Report-Only header model, keeps CSP Phase 1 blocked, and promotes `P33-DG07 CSP Nonce Migration Architecture Rebaseline` as the next bounded design gate.
217. Complete `P33-DG07 CSP Nonce Migration Architecture Rebaseline` as the post-SEC05 decision gate that records `P33-SEC03` as `blocked-by-architecture`, keeps CSP Phase 1 enforcement blocked, sets Phase 0 production posture to `CSP_NONCE_MODE=off` by default with report mode available only for bounded diagnostics, records unlock conditions and six-month/Next-version review cadence in `docs/security/csp-nonce-migration.md`, rejects passive waiting and CSP workaround paths, and promotes exactly one next bounded P33 design gate: `P33-DG08 Storage RLS Backstop Design Review`.
218. Complete `P33-DG08 Storage RLS Backstop Design Review` as the storage-isolation design gate that records the bucket, policy, tenant-prefix path, service-role callsite, and signed URL risk baseline, preserves proxy, canonical route, auth layering, tenancy architecture, product UX, Stripe, README, AGENTS, and architecture-doc boundaries, and promotes exactly one bounded implementation slice: `P33-SEC06 Storage Tenant-Prefix Backstop`.
219. Complete `P33-SEC06 Storage Tenant-Prefix Backstop` as the bounded implementation slice that adds tenant-prefix Storage RLS, centralizes service-role Supabase Storage calls behind path-asserting wrappers, wires a guard against new direct service-role Storage use, records signed URL residual risk, and preserves proxy, canonical routes, auth provider/session shape, non-Storage tenancy architecture, product UX, Stripe, README, AGENTS, and architecture-doc boundaries.
220. Complete `P33-DG09 Post-SEC06 Security Slice Selection` as the docs-only decision gate that confirms the DG07 CSP blocker is unchanged on resolved Next `16.2.4`, keeps SEC03/SEC03R and CSP Phase 1 enforcement non-promotable, records controlled production / pilot GO as acceptable only with green gates, rejects a full hardened-production or `9+/10` claim, and promotes exactly one bounded implementation slice: `P33-SEC07 Signed URL Exposure Hardening`.
221. Complete `P33-SEC07 Signed URL Exposure Hardening` through PR `#695`, merge commit `6d2f030f521525929a63f9789d68cba080f0044a`, as the bounded implementation slice that centralizes signed URL no-store/no-referrer response behavior, preserves signed download TTL defaults, keeps the voice-note preview TTL exception bounded and documented, redacts signed URL bearer tokens from logs/errors, wires the signed URL exposure guard into `pnpm security:guard`, and preserves proxy, canonical routes, auth, tenancy architecture, schema, Storage redesign, Stripe, README, AGENTS, and architecture-doc boundaries while leaving CSP/XSS residual risk blocked by DG07.
222. Complete `P33-DG10 Post-SEC07 Security Slice Selection` as the docs-only decision gate that records SEC07 closeout, confirms the DG07 CSP blocker remains unchanged on resolved Next `16.2.4`, keeps SEC03/SEC03R and CSP Phase 1 enforcement non-promotable, preserves controlled production / pilot GO only with green gates, rejects a full hardened-production or `9+/10` claim, and promotes exactly one bounded implementation slice: `P33-SEC08 Vercel TypeScript Build Suppression Cleanup`.
223. Complete `P33-SEC08 Vercel TypeScript Build Suppression Cleanup` through PR `#698`, merge commit `366741dda756501220ba3cbd07506dff68465a49`, as the bounded implementation slice that removes Vercel-only TypeScript build suppression, wires the Next TypeScript build integrity guard into `pnpm security:guard`, proves local `VERCEL=1` builds fail on synthetic TypeScript errors after suppression removal, and preserves proxy, canonical routes, auth, tenancy architecture, schema, Storage redesign, Stripe, README, AGENTS, and architecture-doc boundaries.
224. Complete `P33-DG11 Post-SEC08 Security Slice Selection` as the docs-only decision gate that records SEC08 closeout, confirms the DG07 CSP blocker remains unchanged on resolved Next `16.2.4`, keeps SEC03/SEC03R and CSP Phase 1 enforcement non-promotable, rejects direct credential implementation before design review, and promotes exactly one bounded design-gate slice: `P33-DG12 CI/Release Seed Credential Hardening Design Review`.
225. Complete `P33-DG12 CI/Release Seed Credential Hardening Design Review` as the docs-only design gate that inventories CI/release/seed/E2E credential surfaces, preserves local deterministic DB fallbacks and CD secret-backed credentials, classifies literal workflow seed passwords and E2E API placeholders as unsafe shared workflow values, and promotes exactly one bounded implementation slice: `P33-SEC09 CI/Release Seed Credential Hardening`.
226. Complete `P33-SEC09 CI/Release Seed Credential Hardening` through PR `#701` as the bounded implementation slice that replaces shared workflow seed credentials with generated masked per-run values, keeps local zero-config E2E seed credentials deterministic, preserves CD staging/production release-gate secrets, wires a workflow seed credential guard into `pnpm security:guard`, and preserves proxy, canonical routes, auth/session shape, tenancy architecture, schema, Storage redesign, Stripe, README, AGENTS, and architecture-doc boundaries.
227. Complete `P33-DG13 Post-SEC09 Security Slice Selection` through PR `#702` as the docs-only decision gate that records SEC09 closeout, confirms the DG07 CSP blocker remains unchanged, keeps SEC03/SEC03R and CSP Phase 1 enforcement non-promotable, rejects direct DB posture implementation because SEC04B intentionally left the remaining `80` unclassified entries as hard cases, and promotes exactly one bounded design-gate slice: `P33-DG14 DB Access Posture Hard-Case Design Review`.
228. Complete `P33-DG14 DB Access Posture Hard-Case Design Review` through PR `#703` as the docs-only design gate that inventories all seven SEC04B residual clusters, rejects mass-stamping or a full `80`-entry burn-down, selects the coherent `14`-entry billing webhook/provider-event tenant-resolution cluster, and promotes exactly one bounded implementation slice: `P33-SEC10 Billing Webhook Tenant Resolution Hardening`.
229. Complete `P33-SEC10 Billing Webhook Tenant Resolution Hardening` through PR `#704`, merge commit `8c4a4ea0b75527db1485b7ee280ad6b262edbc46`, as the bounded Paddle webhook tenant-resolution hardening slice that prefers canonical subscription/user state over provider `customData`, fails closed before unsafe tenant-scoped billing writes, preserves duplicate and invalid-signature audit behavior, fixes the app route resolver ordering, and reduces the DB access posture baseline to `67` unclassified entries.
230. Complete `P33-DG15 Post-SEC10 Security Slice Selection` as the docs-only closeout gate that records SEC10 completion, ranks the named Paddle `transaction.completed` lead conversion residual above the remaining DB access posture clusters, and promotes exactly one bounded implementation slice: `P33-SEC11 Paddle Lead Conversion Tenant Guard Hardening`.
231. Complete `P33-SEC11 Paddle Lead Conversion Tenant Guard Hardening` through PR `#706`, merge commit `e8b9904bb8700ea48e5dde9ee027354044696afe`, as the bounded Paddle lead conversion guard slice that removes the `tenantId || 'unknown'` fallback, validates provider `customData.leadId`, reconciles lead ownership against canonical repo state for the resolved tenant before conversion, preserves webhook audit/dedupe behavior, keeps SEC10 tenant resolution precedence intact, and preserves the post-SEC11 DB access posture baseline at `67` unclassified entries.
232. Complete `P33-DG16 Post-SEC11 Security Slice Selection` as the docs-only closeout gate that records SEC11 completion, ranks the five-entry commercial action idempotency cluster above the remaining DB posture residuals, rejects broad mass-stamping and schema-first approaches, and promotes exactly one bounded implementation slice: `P33-SEC12 Commercial Action Idempotency Tenant Scope Hardening`.
233. Promote `P33-SEC12 Commercial Action Idempotency Tenant Scope Hardening` as the bounded implementation slice that makes idempotency scope explicit, requires canonical tenant scope for tenant-bound commercial actions before reservation writes or cached response release, allows public idempotency only for explicit public flows, fails closed on cross-tenant key conflicts, targets reduction of the commercial idempotency unclassified cluster from `67` to `62` where safe, and avoids proxy, route, auth, tenancy architecture, broad schema/migration, Storage, Stripe, README, AGENTS, and architecture-doc changes.
234. Complete `P33-SEC12 Commercial Action Idempotency Tenant Scope Hardening` through PR `#708`, merge commit `87dbead35e2c436ae695518109e09994511dac75`, as the bounded commercial idempotency tenant-scope hardening slice that reduces the DB access posture baseline to `62` unclassified entries.
235. Queue `P34 Domain CRM Production Roadmap` as the post-`P33-SEC12` CRM product line.
236. Complete `P34-CRM-DG01 Domain CRM Contract` as the first post-SEC12 CRM design gate by creating the `packages/domain-crm` skeleton and freezing actor context, aggregate boundaries, support-handoff state transitions, notification port injection, repository interfaces, timeline read-model invariants, dependency direction, and pure helper tests before CRM10 product work starts.
237. Complete `P34-CRM10 Staff Follow-Up After Member Reply` as the first implementation slice after DG01 by routing support-handoff reply-cycle state through `domain-crm`, surfacing needs-follow-up attention on the existing staff queue, clearing that attention after staff follow-up, preserving the terminal bounded one-cycle member-reply model, and proving authorization failure side effects stay suppressed.
238. Complete `P34-CRM11 Unified CRM Timeline Read Model` as the read-normalization slice by adding a domain-owned derived/read-only timeline model for lead activity and support-handoff response, acknowledgement, member-reply, and staff-follow-up events, while preserving workflow-by-workflow writer migration for later bounded slices and proving negative read authorization for tenant and actor scope.
239. Complete `P34-CRM12 Agent Follow-Up Workflow` as the first agent CRM follow-up slice after the support-handoff and timeline contracts prove the domain pattern, using `domain-crm` for lead follow-up state, explicit agent/tenant/branch authorization, existing agent CRM dashboard/detail surfacing, and same-slice negative authorization proof.
240. Complete `P34-DG02 CRM E2E Gate Coverage Selection` as the post-CRM12 readiness gate that records existing support-handoff gate coverage, identifies agent CRM follow-up gate coverage as the smallest remaining P34 production-readiness gap, and promotes exactly one bounded implementation slice: `P34-CRM13 CRM E2E Gate Coverage`.
241. Complete `P34-CRM13 CRM E2E Gate Coverage` by adding focused blocking-gate Playwright coverage for the existing agent CRM follow-up schedule, due-list, open, complete, and clear workflow; by proving future and other-tenant follow-ups stay out of the current agent due queue; and by wiring default gate specs into `gate-mk-contract` without changing product behavior, proxy, canonical routes, auth, tenancy architecture, schema, Stripe, or broad CRM scope.
242. Complete `P34-DG03 Post-CRM13 CRM Production Readiness Selection` by promoting `P34-CRM14 Agent Lead Mutation Domain Boundary` as the bounded slice that moves create-lead, update-lead-stage, and record-lead-activity writes behind `domain-crm`, keeps the existing `.core.ts` route-facing wrappers, enforces explicit actor, tenant, role, branch, and agent ownership authorization, and documents remaining direct-DB read-model and `memberLeads` exceptions without changing product behavior, proxy, routes, auth, tenancy, schema, Stripe, or broad CRM scope.
243. Complete `P34-CRM14 Agent Lead Mutation Domain Boundary` by routing create-lead, update-lead-stage, and record-lead-activity writes through `domain-crm`, preserving thin existing `.core.ts` wrappers, enforcing explicit actor context plus tenant, role, branch, and agent ownership checks, proving failed-authorization side-effect suppression, and leaving proxy, routes, auth, tenancy, schema, Stripe, and broad CRM architecture unchanged.
244. Complete `P34-DG04 Post-CRM14 CRM Production Readiness Closeout` as the docs-only closeout gate that records PR `#715` merge proof, verifies the remaining CRM direct-DB exceptions are fully classified, closes P34 with no new P34 implementation slice, and intentionally opens no P35 designation.
245. Open `P35 DB Access Posture Residual Hardening` as the post-P34 security/posture tranche, using the current DB access posture residual set rather than continuing CRM by default.
246. Complete `P35-DG01 Post-P34 Next Tranche Selection` as the docs-only selection gate that records P34 closeout, current Notion sync, recent PR history, `pnpm check:db-access` evidence, and the `unclassified=62` DB posture baseline, then promotes exactly one bounded implementation slice: `P35-SEC01 Commission Tenant Proof Contract`.
247. Implement `P35-SEC01 Commission Tenant Proof Contract` by removing the non-Paddle commission fallback tenant lookup, requiring explicit non-empty tenant scope before commission idempotency checks or writes, preserving tenant-scoped webhook and renewal commission paths, deriving tenant scope in the authenticated app wrapper, and updating focused tests plus DB posture proof without proxy, route, auth/tenancy architecture, schema/migration, Storage, Stripe, README, AGENTS, architecture-doc, CRM, or unrelated DB posture cluster changes.
248. Complete `P35-DG02 Agent Dashboard Tenant Read Contract` as the post-SEC01 selection gate that records PR `#719` closeout, preserves the P35 no-broad-burn-down boundary, and promotes exactly one bounded implementation slice: `P35-SEC02 Agent Dashboard Tenant Read Contract`.
249. Implement `P35-SEC02 Agent Dashboard Tenant Read Contract` by requiring session-derived tenant scope before staff/admin dashboard claim reads, adding direct `claims.tenantId` predicates to the five legacy agent-dashboard DB calls, preserving existing agent empty-state behavior, and updating focused tests plus DB posture proof without proxy, route, auth/tenancy architecture, schema/migration, Storage, Stripe, README, AGENTS, architecture-doc, CRM, or unrelated DB posture cluster changes.
250. Open `P36 CRM Data-Model Hardening` after completed `P35` as the CRM data-model correctness tranche prompted by the CRM re-review.
251. Complete `P36-DG01 CRM Data-Model Hardening Re-Review` as the design gate that records the durable branch ownership gap in `crm_leads`, rejects UI/task/automation/analytics work before the model is corrected, and promotes exactly one bounded implementation slice: `P36-CRM-DM01 Durable CRM Lead Branch Ownership`.
252. Implement `P36-CRM-DM01 Durable CRM Lead Branch Ownership` by adding nullable `crm_leads.branch_id`, backfilling existing leads with audit evidence, persisting branch id on new CRM lead creation, reading branch ownership from the CRM lead row for mutation and follow-up adapters, removing current-agent branch reconstruction, failing closed on missing branch custody, and proving branch movement does not retroactively change existing CRM lead authorization.
253. Complete `P36-DG02 Post-DM01 Closeout And Next CRM Data-Model Slice` as the docs-only closeout gate that records PR `#722` merge proof, removes stale DM01 PR-pending state from the live program/tracker, preserves the P36 data-model boundary, and promotes exactly one next bounded implementation slice: `P36-CRM-DM02 CRM Lead Lifecycle History And Stage Discipline`.
254. Implement `P36-CRM-DM02 CRM Lead Lifecycle History And Stage Discipline` by adding `crm_lead_stage_history`, making authorized stage transitions append exactly one history row, proving failed authorization appends no history row, adding database discipline for `crm_leads.stage`, and adding terminal `won_at` / `lost_at` timestamps only if they remain tightly tied to terminal transitions.
255. Complete `P36-DG03 Post-DM02 Closeout And Next CRM Data-Model Slice` as the docs-only closeout gate that records PR `#724` merge proof, removes stale DM02 branch-implemented state from the live program/tracker, preserves the P36 data-model boundary, and promotes exactly one next bounded implementation slice: `P36-CRM-DM03 CRM Lead Ownership History And Transfer Discipline`.
256. Implement `P36-CRM-DM03 CRM Lead Ownership History And Transfer Discipline` by adding `crm_lead_ownership_history`, writing an initial ownership row on CRM lead creation, introducing one domain operation for agent/branch ownership transfer, closing the prior open ownership row and opening a new row in the same transaction as successful transfers, backfilling existing leads from durable DM01 ownership fields, and proving failed authorization/cross-tenant/out-of-branch attempts do not mutate lead ownership or history.
257. Complete `P36-DG04 Post-DM03 Closeout And Next CRM Data-Model Slice` as the closeout gate that records PR `#726` merge proof, removes stale DM03 branch-implemented state from the live program/tracker, preserves the P36 data-model boundary, and promotes exactly one next bounded implementation slice: `P36-CRM-DM04 CRM Activity Discipline And Dashboard Query Readiness`.
258. Implement `P36-CRM-DM04 CRM Activity Discipline And Dashboard Query Readiness` by adding current CRM dashboard and timeline indexes to `crm_activities`, adding DB-level activity-type discipline, snapshotting activity `branch_id` at write time with ownership-history backfill for existing rows, adding a bounded future-write tenant/lead invariant, and proving supported manual activity types still work while unsupported manual types are rejected.
259. Complete `P36-DG05 Post-DM04 Closeout And Next CRM Slice Selection` as the docs-only closeout gate that records PR `#727` merge proof, removes stale DM04 in-progress state from the live program/tracker, preserves the P36 data-model boundary, and promotes exactly one next bounded implementation slice: `P36-CRM-DM05 CRM Dashboard Read-Side Domain Port`.
260. Implement `P36-CRM-DM05 CRM Dashboard Read-Side Domain Port` by moving existing agent CRM dashboard reads behind `domain-crm`, enforcing tenant, agent, and branch scope through `CrmActorContext`, preserving existing dashboard UI/output behavior, proving cross-tenant/wrong-agent/wrong-branch reads are rejected or excluded, and reducing or correctly classifying remaining CRM dashboard direct-DB guard entries.
261. Complete `P36-DG06 Post-DM05 Closeout And Next CRM Slice Selection` as the docs-only closeout gate that records PR `#733` merge proof, removes stale DM05 in-progress state from the live program/tracker, preserves the P36 data-model boundary, and promotes exactly one next bounded implementation slice: `P36-CRM-DM06 CRM Lead Detail Read-Side Domain Port`.
262. Implement `P36-CRM-DM06 CRM Lead Detail Read-Side Domain Port` by moving existing agent lead-detail lead/deal reads behind `domain-crm`, enforcing tenant, agent, and branch scope through `CrmActorContext`, preserving existing lead-detail UI/output behavior, proving cross-tenant/wrong-agent/wrong-branch reads are rejected or excluded before deal data is returned, and reducing or correctly classifying remaining CRM lead-detail direct-DB guard entries.
263. Complete `P36-CRM-DM07 CRM Lead Activity Timeline Read-Side Domain Port` by moving existing agent lead-detail activity-feed reads behind `domain-crm`, enforcing tenant, agent, and branch scope through `CrmActorContext`, preserving existing activity-feed and follow-up next-action output, proving cross-tenant/wrong-agent/wrong-branch activity reads are rejected or excluded, and reducing or correctly classifying remaining CRM lead-activity direct-DB guard entries.
264. Complete `P36-DG08 Post-DM07 Closeout And AI Production Readiness Selection` as the docs-only closeout gate that records PR `#737` merge proof, removes stale DM07 in-progress state from the live program/tracker, closes P36 after the final promoted CRM read-side port, and promotes exactly one next bounded design-gate slice: `P37-AI-DG01 AI Production Readiness Gate And Eval Contract`.
265. Complete `P37-AI-DG01 AI Production Readiness Gate And Eval Contract` before any new AI runtime work by inventorying AI-facing surfaces, classifying data boundaries and model/prompt/schema contracts, requiring eval/golden/adversarial fixtures, placing guardrails, defining observability/cost/reliability gates, and promoting at most one bounded implementation slice with measurable acceptance criteria.
266. Keep `P37-AI01 Policy Extract Strict Output Contract And Eval Gate` complete by preserving strict `policyExtractSchema` policy extraction output, keeping the policy eval-only normalization shim removed, retaining sparse/adversarial no-hallucination fixtures, treating `pnpm ai:eval` as required local proof for AI behavior changes, and preserving the existing queued policy upload/background workflow.
267. Complete `P37-DG02 Post-AI01 Closeout And Next AI Slice Selection` as the docs-only closeout gate that records PR `#740` merge proof, removes stale AI01 in-progress state from the live program/tracker, ranks remaining AI-readiness candidates from repo evidence, and promotes exactly one next bounded implementation slice: `P37-AI02 Formal AI Eval CI And Release Gate`.
268. Keep `P37-AI02 Formal AI Eval CI And Release Gate` complete by preserving the blocking CI AI-eval lane for AI-surface PRs and mainline validation, retaining release-candidate `pnpm ai:eval` proof, keeping the changed-file classifier scoped to current AI and gate-contract surfaces, preserving deterministic fixture-only execution with no network model calls, and avoiding runtime AI behavior, prompt, model-call, auth, tenancy, routing, proxy, Stripe, CRM, automation, campaign, cron/NPS, task aggregate, or `member_leads` changes.
269. Keep `P37 AI Production Readiness` closed for the purpose of opening Dashboard Professionalism, with `P37-AI02` as the last completed marker and any remaining `P37-*` items deferred into separately tracked work.
270. Keep `P40 CRM Product Depth` complete after `P40-CRM33`, preserving the CRM task queue foundation, lifecycle, due-date, cancellation, completed recovery, and priority-control sequence without promoting additional CRM runtime behavior by default.
271. Open `P41 Product Surface UX Professionalism` as a docs-and-evidence product UI/UX audit tranche after P40, starting with `P41-UX01 Product Surface UX Audit And Slice Ranking` and preserving Phase C route, proxy, auth, tenancy, schema, Stripe, README, AGENTS, and architecture-doc boundaries.
272. Keep `P41-UX01 Product Surface UX Audit And Slice Ranking` complete as the docs-and-evidence audit that ranks `/agent/crm` task-row action density and mobile ergonomics as the next design target, recommending `P41-DG02 Agent CRM Task Queue Action Density And Mobile Ergonomics Design Gate` before any runtime UI implementation.
273. Complete `P41-DG02 Agent CRM Task Queue Action Density And Mobile Ergonomics Design Gate` as the approved design authority that promotes exactly one presentation-only implementation slice, `P41-UX02 Agent CRM Task Queue Action Density And Mobile Ergonomics`, preserving existing CRM26 actions, CRM task behavior, completed recovery, lead links, queue markers, legacy due-follow-up behavior, canonical routes, proxy, auth, tenancy, schemas, Stripe, README, AGENTS, and architecture-doc boundaries.
274. Keep `P41-UX02 Agent CRM Task Queue Action Density And Mobile Ergonomics` complete through PR `#856`, preserving the existing CRM task behavior while reducing `/agent/crm` row action density through presentation-only secondary actions.
275. Complete `P41-DG03 Admin/Ops Filter Wrapping Hardening Design Gate` as the approved design authority that promotes exactly one presentation-only implementation slice, `P41-UX03 Admin/Ops Filter Wrapping Hardening`, scoped to shared `OpsFiltersBar` and `/admin/claims` filter wrapping/accessibility hardening while preserving query semantics, routes, proxy, auth, tenancy, data reads, schemas, Stripe, README, AGENTS, and architecture-doc boundaries.
276. Keep `P41-UX03 Admin/Ops Filter Wrapping Hardening` complete through PR `#858`, preserving shared ops/admin filter semantics while adding presentation-only wrapping, grouping, accessible labels, and structural proof.
277. Complete `P41-DG04 Member Dashboard Visual Hierarchy Hardening Design Gate` as the approved design authority that promotes exactly one presentation-only implementation slice, `P41-UX04 Member Dashboard Visual Hierarchy Hardening`, scoped to the canonical `/member` dashboard task-first hierarchy and secondary-service quieting while preserving routes, proxy, auth, tenancy, data reads, runtime behavior, schemas, Stripe, README, AGENTS, and architecture-doc boundaries.
278. Keep `P41-UX04 Member Dashboard Visual Hierarchy Hardening` complete through PR `#861`, preserving canonical `/member` dashboard behavior while making the first visible dashboard region task-first, quieting secondary dashboard content, neutralizing false service-card interactivity, and proving localized authenticated viewport behavior.
279. Complete `IDA-DG02 Auth Portal Visual Refresh Design Gate` as the approved design authority that promotes exactly one presentation-only implementation slice, `IDA-AUTH01 Portal Sign-In Visual Refresh v0`, scoped to the existing localized login route and preserving proxy, canonical routes, auth/session behavior, tenant resolution, safe redirects, billing, schema, backend, Stripe, README, AGENTS, and architecture-doc boundaries.
280. Keep `IDA-AUTH01 Portal Sign-In Visual Refresh v0` complete through PR `#867`, preserving existing localized login auth/session/tenant behavior while adding the route-local portal shell, neutral `en`/`sq`/`mk`/`sr` copy, query continuity, and mobile `360`, `390`, and `430` proof without hard no-scroll behavior, forbidden claims, route changes, proxy edits, or backend changes.
281. Complete `IDA-DG03 Member-Action Hero Render Design Gate` as the approved design authority that promotes exactly one proof-first implementation slice, `IDA-MH02 Member-Action Hero Render Contract`, scoped to dashboard render proof for existing `missing_documents` and `authorization_needed` member-home hero states while preserving proxy, canonical routes, auth/session behavior, tenancy, billing, schema, backend, Stripe, README, AGENTS, and architecture-doc boundaries.
282. Keep `IDA-MH02 Member-Action Hero Render Contract` pending until implemented from a clean synced main branch with focused dashboard/resolver tests, authorization matching-breadth proof, interactive primary CTA role proof, active-member anti-conversion proof, i18n and forbidden-claim proof, and no broad member dashboard redesign or backend changes.

## Status Command

```bash
pnpm plan:status
```

## Proof Command

```bash
pnpm plan:proof
```

## Completed Convergence Priorities

1. `V01` Close the unsafe alternate registration path.
   Exit criteria: `/api/simple-register` is deleted or hard-restricted; caller-controlled role creation is removed; the non-canonical public write path is not reachable.
2. `V02` Canonicalize member write-path generation and orchestration.
   Exit criteria: active registration and conversion flows use the shared member-number generator and no local duplicate generator remains in the write path.
3. `V03` Repair and instrument member-number integrity.
   Exit criteria: malformed existing member numbers are audited and remediated; self-heal activity is observable; fallback removal is not attempted without evidence.
4. `V04` Enforce backend guardrails where they buy leverage immediately.
   Exit criteria: local retry copies are removed, the current strict entrypoint violation is fixed, and strict entrypoint boundary checking is added to verification.
5. `V05` Apply a narrow Next.js production-hardening slice.
   Exit criteria: broad dynamic rendering and root hydration are reduced where safe, and high-value navigation/font hygiene issues are resolved or explicitly documented.

## Next Committed Priority

`P-1` Infrastructure Debt Closure is now complete as the committed post-AI tranche.

`P1C` Commercial Contract Foundations is now complete as the committed post-infrastructure tranche. `C05`, `C01`, `C04`, `C02`, `C03`, and `C06` are complete.

`P1T` Free Start And Trust UX is now complete as the committed post-`P1C` tranche. `T01`, `T02`, `T03`, `T04`, `T05`, and `T06` are complete.

`P2` Billing And Terms Hardening is now complete as the post-`P1T` tranche.

`P3` Commercial Actions And Contract Enforcement is now complete as the post-`P2` tranche. `M01`, `M02`, `M03`, `M04`, `M05`, and `M06` are complete as the commercial mutation-path audit, typed Server Action migration, commercial idempotency, matter-consumption guard, audit-safe escalation decision, and superseded write-path retirement slices.

`P4` Staff Operations And Acceptance Control remains active as the post-`P3` tranche. `S01` is complete as the canonical staff claim queue and filter work slice, `S02` is complete as the canonical manual claim assignment slice, `S03` is now complete as the claim stage history and member-visible tracker slice, `S04` is now complete as the SLA-state visibility slice, `S05` is now complete as the internal-notes isolation slice, `S07` is now complete as the matter-ledger and allowance-visibility slice, intentionally promoted ahead of `S06`, `S06` is now complete as the next promoted slice after `S07`, `S08` is now complete as the explicit recovery-decision gate slice after `S06`, `S09` is now complete as the accepted-case agreement and collection prerequisite slice after `S08`, and `S10` is now complete as the guidance-only and referral-only commercial-enforcement slice after `S09`, keeping the actionable staff queue shipped with explicit staff-side assignment, public claim-history continuity, incomplete-vs-running SLA clarity, internal-note isolation, annual allowance visibility, canonical secure messaging, a typed accept-or-decline recovery gate, accepted-case commercial prerequisite enforcement, and a staff-only launch-scope block before any later auto-assignment work is committed.

`GA03` Add sponsored-seat activation and self-upgrade path is now complete after completed `GA02`, and `GA04` privacy and consent negative tests are now complete after completed `GA03` so group admins still cannot see claim facts, notes, or documents without explicit member consent.

`P6` v1.0.0 RC Gate is now complete after completed `P4` and `P4G`. `G07` Validate commercial promise surfaces is now complete, `G08` Validate Free Start and group privacy boundaries is now complete, `G09` Validate matter and SLA enforcement is now complete, and `G10` Validate escalation agreement and collection fallback in staging is now complete, proving release-candidate validation now covers the published commercial contract surfaces, Free Start and aggregate-only privacy boundaries, matter-and-SLA enforcement, and accepted-case commercial enforcement.

`P7` Pilot Readiness And Release Evidence is now complete after completed `P6`. `R01` Canonicalize the pilot-ready artifact set, `R02` Unify readiness commands and outputs, `R03` Add deterministic pilot evidence capture, `R04` Add explicit continue-pause-hotfix-stop decision proof, `R05` Make pilot-ready tag discipline repo-verifiable, `R06` Establish a modern readiness cadence, `R07` Tighten observability and incident evidence, and `R08` Publish one ranked operator flow for pilot entry are now complete, so pilot-entry command custody stays canonical across branch-manager oversight, staff processing, and admin decision flow before any broader UX redesign is promoted.

`P8` Pilot Reset Gate And Seven-Day Rehearsal is now complete after completed `P7`. `P8R` is complete through `RG01` through `RG05`, proving the reset gate against `pilot-ks-7d-rehearsal-2026-03-16`, and `P8P` is complete through `SP01` through `SP07`, concluding the rehearsal with a bounded `pause` decision and `repeat_with_fixes` recommendation.

`P9` Regional Wedge Foundation And Diaspora Service Line is now complete after completed `P8`. `E1` is complete as the regional-wedge foundation and tenant-proof line, and `E4` is complete as the diaspora / Green Card service-line line, keeping those post-pilot product surfaces inspectable without reopening proxy, auth, or tenancy architecture.

`P10` Membership Funnel And Verification Hardening is now complete after completed `P9`. `E2` is complete as the end-to-end membership acquisition and onboarding line, and `H01` is complete as the post-`E2` verification, QA, and public-entry hardening slice that keeps the funnel-closeout verification path inspectable on canonical repo surfaces.

`P11` Bounded Production Operations And Expansion Proof is now complete after completed `P10`. It promoted Program A into the repo-canonical line as a narrow production-proof tranche. `A01` is complete as the fresh production release gate on merged `main`, `A02` is complete as the fresh bounded operating line on a new pilot or cohort id, `A03` is complete as the repo-backed KPI and progression proof plus corrected-baseline privacy / RBAC rerun line, and `A04` is complete as the explicit executive decision artifact line for `pause`, `repeat_with_fixes`, `expand`, or `stop`. Supporting pilot and roadmap documents constrain this tranche, but they do not convert it into broader portal cleanup or modernization work. The `A02` line used `pilot-ks-expand-readiness-2026-04-15` as the canonical fresh id, bounded the operating window to `2026-04-16` through `2026-04-22` in Europe/Berlin, and was operationally entered on `2026-04-15` with the starter-pack scaffold, copied evidence index, pointer row, rollback tag `pilot-ready-20260415`, first live-day sheet, first daily SQL scaffold, and same-day evidence, observability, and decision rows all recorded in repo-backed custody with no post-hoc repair expectation. Because the first live operating day did not begin until `2026-04-16`, the first live CSV remained explicitly blocked pending same-day capture on `2026-04-16`; `A03` is now complete following same-day capture, KPI rollup generation, and PD05B rerun, and `A04` is now complete with the recorded executive decision to expand.

`P12` Program C Business-Model Integrity Hardening is now complete after completed `P11`. `PC01` through `PC06` closed the canonical membership fulfillment, ownership and attribution, commission and referral finance, branch cash truth, membership lifecycle reporting, and enterprise-safe controls lines without widening into routing, auth, tenancy, portal redesign, or modernization work.

`P13` v1.0.0 Funnel Completion is now complete after completed `P12`. `P13-FC01` is complete as the ClaimPack domain generator slice, `P13-FC02` is complete as the public Free Start integration slice, and `P13-N01` is complete as the critical lifecycle notification wiring slice. The tranche closed the minimum viable v1.0.0 gap by turning the existing Free Start shell into a real generated pack and keeping users informed through existing notification infrastructure, without changing proxy, auth, tenancy, canonical routes, schema, Stripe posture, or request-path AI boundaries.

`P14` Member Experience Guidance is now complete after completed `P13`. `P14-MD01` is complete as the member-dashboard next-action guidance slice, adding a localized member guidance panel for no-claim, active-claim, document, membership, and support states using existing dashboard data and without changing proxy, auth, tenancy, canonical routes, schema, billing, or the broader design system.

`P15` Production Professionalism Hardening is now complete after `P14`. `P15-G01` is complete as the `/api/register` authorization guard slice: the route only allows an explicit agent session and preserves the agent branch when dispatching to the assisted-registration core. `P15-G02` is complete as the protected-route fail-closed proxy slice: unknown, throttled, or errored session introspection no longer passes protected canonical routes through. `P15-S01` is complete as the production runtime and bearer-token hardening slice: debug/test surfaces, share-pack signing, billing test activation, production RLS fallback, rate-limit production posture, and E2E diagnostics now fail closed or require explicit local E2E enablement. `P15-S02` is complete as the claim-upload and activity-read correctness slice: member/admin upload confirmation is bound to a server-issued upload intent plus storage object metadata verification, and member activity reads now enforce tenant plus role or assignment scope independent of RLS. `P15-QA01` landed in PR `#510` as the final committed professionalism-proof slice for E2E-contract, docs-drift, and release-proof gaps from the 2026-04-24 senior-staff review.

`P18` Pilot-Learned Trust And Activation UX is complete after `P17`. `P18-DG01` is complete as the promotion design gate that turned the post-`P7` experience-era recommendation into the live post-`P17` queue without reopening proxy, routing, auth, tenancy, Stripe, or broad portal-redesign scope. `P18-UX01` is complete as the first bounded execution slice: `docs/plans/2026-04-26-p18-ux01-evidence-ranking.md` ranks repo-custodied operational progression and trust/SLA evidence above partial member-return and activation data, records the absence of repo-custodied agent-demo feedback, and promoted exactly one next implementation recommendation, `P18-UX02 Member Claim Progress Clarity`. `P18-UX02` is complete in PR `#534`; the existing canonical member claim detail surface now shows a localized progress summary using the current member-scoped read model and public timeline evidence. `P18-DG02` is complete in PR `#536` as the next design gate and promotes `P18-UX03 Member Trust And SLA Clarity` as the next bounded implementation slice from existing SLA/status and member-visible support evidence. `P18-UX03` is complete in PR `#538`; the canonical member claim detail now derives and renders localized member trust/SLA assurance, latest public update timing, and support handoff from existing member-scoped claim state without changing proxy, routes, auth, tenancy, schema, Stripe, portal structure, broad dashboards, analytics, or agent-demo workflows. CRM and agent-workspace redesign remain unpromoted and require a later design gate before implementation.

`P19` Agent CRM Readiness And Boundary Hardening is complete after `P18`. `P19-DG01` is complete as the promotion design gate that selected `P19-CRM01 Agent CRM Tenant-Scoped Read Contracts` as the bounded implementation slice from existing `/agent/crm`, CRM lead-detail, and tenant-bearing CRM table evidence. `P19-CRM01` is complete in PR `#542`; existing agent CRM and lead-detail reads now resolve tenant identity at the boundary and enforce tenant plus agent scoping in the read cores while leaving broad CRM redesign, broad agent-workspace redesign, product analytics, proxy, route, auth, tenancy, schema, and Stripe changes out of scope. `P19-DG02` is complete as the next CRM-readiness design gate and promoted `P19-CRM02 Agent Dashboard CRM KPI Tenant Read Contracts` as the next bounded implementation slice for existing canonical `/agent` dashboard V2 CRM KPI reads. `P19-CRM02` is complete in PR `#545`; the canonical `/agent` dashboard V2 now resolves tenant identity before CRM KPI reads, passes tenant identity into the stats core, and tenant-scopes CRM lead, deal, commission, subscription, and branch lookup reads without proxy, route, auth, tenancy, schema, Stripe, product analytics, broad CRM redesign, or broad agent-workspace redesign changes. `P19-DG03` is complete in PR `#547` as the next CRM-readiness design gate and promoted `P19-CRM03 Agent CRM Mutation Tenant-Boundary Contracts` as the next bounded implementation slice for existing legacy agent CRM status and activity mutations. `P19-CRM03` is complete in PR `#549`; legacy agent CRM status and activity writes now reject missing tenant identity at the server-action boundary and scope CRM mutation cores by tenant plus agent ownership. P19 is closed for v1.0.0, with broader CRM product work deferred until a later design gate.

`P20` v1.0.0 Release Declaration And Version Authority is complete after `P19`. `P20-DG01` is complete through PR `#551` as the promotion design gate that recorded completed readiness and hardening evidence while identifying the remaining release-governance gap: the live program and package manifests still carried `v0.1.0` / `0.1.0` authority. `P20-RG01` is complete through PR `#553` as the bounded implementation slice that declares `v1.0.0` release authority, reconciles workspace release-version manifests to `1.0.0`, and records the evidence basis without product behavior, proxy, route, auth, tenancy, schema, Stripe, analytics, broad CRM, or agent-workspace redesign scope.

`P21` v1.0.0 Live Surface Revalidation is complete after `P20`. `P21-QA01` uses `docs/plans/2026-04-27-p21-qa01-v1-live-surface-revalidation.md` as the scenario authority and validates the serving behavior of launch-critical public and dashboard surfaces, host-to-tenant resolution, anonymous protected-route redirects, unified agent/member context switching, and production Paddle warning suppression before broader live operation or post-v1 product work proceeds.

`P22` Production Go-Live Operations is complete after `P21`. `P22-GO01` used `docs/plans/2026-04-27-p22-go01-production-go-live-readiness.md` as the operational go/no-go authority and required production deployment identity, environment and secret posture, DNS and tenant-host resolution, Paddle production posture, database backup and rollback proof, observability and alert proof, launch-critical production smoke evidence, and an explicit go/no-go decision before live traffic was opened. `P22-GO02` is complete through PR `#561` as the narrow `/register` G07 blocker fix. `P22-GO03` is complete through PR `#568` as the narrow member-claim SLA status copy fix; the fresh production release-gate rerun on `https://www.interdomestik.com` passed all scripted P0/P1/G07/G08/G09/G10 checks on deployment `dpl_AtRyCJu5RV4sHmw9w9DtXQEZAgiw`. `P22-GO04` records the final non-scripted launch operations acceptance against production deployment `dpl_DSrbEwyPNhuUUAy4QrQvEmkAVfXx` and commit `ad9dd21d0eba8f58b8a18fe5d3ee747b7cbc4e73`, including backup/recovery point, rollback owner and procedure, alert/on-call owner, incident decision owner, launch window, and final `GO` authority.

## Do Not Reopen The Convergence Boundary

- broad domain extraction campaigns
- routing, auth, or tenancy architecture refactors
- repo-wide RLS rollout
- synchronous request-path AI inference or autonomous customer-facing decisions
- enterprise hardening items that do not reduce the `v0.1.0` failure surface

## Inputs, Not Active Plans

These documents can recommend or constrain work, but they do not define the live program:

- `docs/plans/2026-03-07-gpt-5-4-phase-1-implementation-plan.md`
- `docs/plans/2026-03-06-v0-1-0-production-background-plan.md`
- `docs/MATURITY_ASSESSMENT_2026.md`
- `docs/EXECUTIVE_MATURITY_ASSESSMENT.md`
- `docs/plans/2026-02-22-v1-bulletproof-tracker.md`
- `docs/plans/2026-03-03-program-charter-canonical.md`
- `docs/plans/2026-03-03-advisory-foundation-addendum.md`
- `docs/plans/2026-03-05-pg3-advisory-evidence-refresh.md`
- `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md`
- `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md`
- `docs/plans/2026-03-15-pilot-readiness-blueprint-v1.md`
- `docs/plans/2026-03-15-pilot-readiness-roadmap-diff-proposal.md`
- `docs/plans/2026-03-16-p8-pilot-redesign-blueprint-v1.md`
- `docs/plans/2026-03-16-p8-pilot-redesign-roadmap-diff-proposal.md`
- `docs/pilot/V1_0_0_NEXT_STEPS.md`
- `docs/pilot/V1_0_0_BOUNDED_CONTINUATION_PLAN.md`
- `docs/pilot/V1_0_0_EXECUTIVE_DECISION_GATE.md`
- `docs/superpowers/plans/2026-03-28-track-b-expand-readiness.md`

## Promotion Note

- `AI01` was copied into the live program and tracker from `docs/plans/2026-03-07-gpt-5-4-phase-1-implementation-plan.md` and is now complete.
- `AI02` was copied into the live program and tracker from `docs/plans/2026-03-07-gpt-5-4-phase-1-implementation-plan.md` and is now complete as the provenance slice.
- `AI03` was copied into the live program and tracker from `docs/plans/2026-03-07-gpt-5-4-phase-1-implementation-plan.md` and is now complete as the workflow slice.
- `AI04` has now been copied into the live program and tracker from `docs/plans/2026-03-07-gpt-5-4-phase-1-implementation-plan.md` and is complete as the status-and-review slice.
- `AI05` has now been copied into the live program and tracker from `docs/plans/2026-03-07-gpt-5-4-phase-1-implementation-plan.md` and is complete as the claim-and-legal workflow slice.
- `AI06` has now been copied into the live program and tracker from `docs/plans/2026-03-07-gpt-5-4-phase-1-implementation-plan.md` and is complete as the evals, telemetry, and CI wiring slice.
- `P-1` Infrastructure Debt Closure has now been copied into the live program and tracker from `docs/EXECUTIVE_MATURITY_ASSESSMENT.md` and `docs/MATURITY_ASSESSMENT_2026.md`.
- `D01` has now been copied into the live program and tracker from `docs/EXECUTIVE_MATURITY_ASSESSMENT.md` and `docs/MATURITY_ASSESSMENT_2026.md` and is complete as the full PR `e2e:gate` CI slice.
- `D02` has now been copied into the live program and tracker from `docs/EXECUTIVE_MATURITY_ASSESSMENT.md` and `docs/MATURITY_ASSESSMENT_2026.md` and is complete as the deterministic cross-agent messaging isolation slice.
- `D03` has now been copied into the live program and tracker from `docs/EXECUTIVE_MATURITY_ASSESSMENT.md` and `docs/MATURITY_ASSESSMENT_2026.md` and is complete as the blocking 60% repository coverage floor slice.
- `D04` has now been copied into the live program and tracker from `docs/EXECUTIVE_MATURITY_ASSESSMENT.md` and `docs/MATURITY_ASSESSMENT_2026.md` and is complete as the canonical blocking `gitleaks` coverage slice.
- `D05` has now been copied into the live program and tracker from `docs/EXECUTIVE_MATURITY_ASSESSMENT.md` and `docs/MATURITY_ASSESSMENT_2026.md` and is complete as the staging Supabase plus preview-environment separation slice.
- `D06` has now been copied into the live program and tracker from `docs/EXECUTIVE_MATURITY_ASSESSMENT.md` and `docs/MATURITY_ASSESSMENT_2026.md` and is complete as the canonical incident-response playbook slice.
- `D07` has now been copied into the live program and tracker from `docs/EXECUTIVE_MATURITY_ASSESSMENT.md` and `docs/MATURITY_ASSESSMENT_2026.md` and is complete as the Sentry burn-rate alert wiring slice.
- `D08` has now been copied into the live program and tracker from `docs/EXECUTIVE_MATURITY_ASSESSMENT.md` and `docs/MATURITY_ASSESSMENT_2026.md` and is complete as the narrow critical-table RLS proof slice.
- `P11` has now been copied into the live program and tracker from `docs/pilot/V1_0_0_NEXT_STEPS.md`, `docs/pilot/V1_0_0_BOUNDED_CONTINUATION_PLAN.md`, `docs/pilot/V1_0_0_EXECUTIVE_DECISION_GATE.md`, and `docs/superpowers/plans/2026-03-28-track-b-expand-readiness.md` as the canonical Program A tranche after completed `P10`.
- `A01` through `A04` have now been copied into the live program and tracker from those same supporting pilot and roadmap inputs as the bounded production-operations and expansion-proof queue.
- `A01` is now complete as of 2026-04-15. Fresh local verification in the recovered session confirmed `node -v` = `v20.20.0` plus green `pnpm -s plan:status`, `pnpm -s plan:audit`, `pnpm -s track:audit`, `pnpm pr:verify`, `pnpm security:guard`, and `pnpm e2e:gate`; the first `e2e:gate` stop was a sandbox-only Docker socket denial, not a repo regression.
- `A02` operational entry is now complete as of 2026-04-15 around the fresh pilot id `pilot-ks-expand-readiness-2026-04-15`, with one bounded KS operating window from 2026-04-16 through 2026-04-22 in Europe/Berlin, a hard stop at the earliest stop criterion in `docs/pilot/V1_0_0_BOUNDED_CONTINUATION_PLAN.md`, and day-0 custody limited to the starter-pack scaffold, copied evidence index, pointer row, rollback tag, daily live sheets, daily SQL or CSV exports, and same-day evidence, observability, and decision rows. The 2026-04-15 entry baseline is recorded in repo-backed custody, the first live-day sheet and SQL scaffold are present for 2026-04-16, and the first live CSV is explicitly blocked until same-day capture on 2026-04-16 rather than repaired later. `A03` KPI and `PD05B` proof are now complete with the 2-day progression rate derived from the week-1 rollout. `A04` executive decision work is now complete with the canonical artifact formalizing the expansion sequence recorded on 2026-04-16.
- `P12` has now been copied into the live program and tracker as the canonical post-`P11` Program C tranche following the already-recorded code-grounded recommendation to promote business-model integrity work before Program B workflow completion or Program D modernization, and is now complete.
- `PC01` through `PC06` have now been copied into the live program and tracker as the completed Program C hardening queue.
- `PC01` is complete as the membership-fulfillment canonicalization slice.
- `PC02` is complete as the ownership-and-attribution unification slice across PRs `#479`, `#481`, and `#486`.
- `PC03` is complete as the commission-and-referral finance hardening slice in PR `#488`.
- `PC04` is complete as the branch operations and cash-truth slice in PR `#491`.
- `PC05` is complete as the membership lifecycle and reporting-truth slice in PR `#492`.
- `PC06` is complete as the enterprise-safe controls and lifecycle edge-case hardening slice in PR `#493`.
- `P13` has now completed the minimum viable Program B plus critical notification path in the live program and tracker as the post-`P12` v1.0.0 Funnel Completion tranche, while broader Program D, E, and F recommendations remain planning input until a later promotion or design-gate decision explicitly makes them the live queue.
- `P14` has now completed the first narrow Program D member-experience slice as the post-`P13` Member Experience Guidance tranche, limited to localized member-dashboard next-action guidance and not a broad dashboard redesign.
- `P15` is now complete from the 2026-04-24 senior-staff production-professionalism review as the blocker-removal tranche before pilot launch can be treated as a professional Go.
- `P15-G01` is now complete as the registration authorization guard slice, closing the review finding that allowed non-agent sessions to reach the assisted member-registration path.
- `P15-G02` is now complete as the protected-route fail-closed proxy slice, closing the review finding that allowed protected canonical routes to pass through when session introspection returned unknown.
- `P15-S01` is now complete as the production runtime and bearer-token hardening slice, closing the predictable share-pack signing fallback and production-runtime test-affordance findings while preserving explicit local E2E diagnostics.
- `P15-QA01` is now complete in PR `#510`, closing the E2E-contract, docs-drift, release-proof, Sonar, Copilot, finalizer, and pilot-gate evidence gaps.
- `P16` is now complete as the post-`P15` production-professionalism re-review tranche.
- `P16-PR01` is now complete in PR `#512` as the repo-backed professionalism re-review and pilot Go/No-Go evidence slice.
- `P17` is now complete as the post-`P16` professionalism-maturity tranche.
- `P17-PR01` is now complete in PR `#514` as the legacy claim-wizard upload hardening slice.
- `P17-PR02` is now complete in PR `#517` as the tenant-boundary API contract hardening slice.
- `P17-PR03` is now complete in PR `#519` as the push subscription collision contract hardening slice.
- `P17-PR04` is now complete in PR `#521` as the sensitive route ownership map slice.
- `P17-PR05` is now complete in PR `#523` as the verification API boundary contract proof slice.
- `P17-PR06` is now complete in PR `#525` as the share-pack API boundary contract proof slice.
- `P18` has now been copied into the live program and tracker from `docs/plans/2026-03-15-pilot-readiness-blueprint-v1.md` as the post-`P17` Pilot-Learned Trust And Activation UX tranche, using `P18` rather than the blueprint's historical `P8` label to avoid colliding with the completed Pilot Reset Gate And Seven-Day Rehearsal tranche.
- `P18-DG01` is now complete as the design-gate slice that promotes the P18 queue without product-code changes, `P18-UX01` is complete as the first bounded evidence-ranking slice before implementation, `P18-UX02` is complete as the recommended member claim progress clarity implementation slice, and `P18-DG02` is complete as the design gate that promotes `P18-UX03 Member Trust And SLA Clarity` as the next bounded implementation slice.
- `P19` has now been copied into the live program and tracker as the post-`P18` Agent CRM Readiness And Boundary Hardening tranche, `P19-DG01` is complete as the design-gate slice that promoted `P19-CRM01 Agent CRM Tenant-Scoped Read Contracts`, `P19-CRM01` is complete through PR `#542`, `P19-DG02` is complete as the design-gate slice that promoted `P19-CRM02 Agent Dashboard CRM KPI Tenant Read Contracts`, `P19-CRM02` is complete through PR `#545`, `P19-DG03` is complete through PR `#547` as the design-gate slice that promoted `P19-CRM03 Agent CRM Mutation Tenant-Boundary Contracts`, and `P19-CRM03` is complete through PR `#549`, closing the bounded CRM readiness and tenant-boundary hardening line before any broad CRM or agent-workspace redesign is authorized.
- `P20` has now been copied into the live program and tracker as the post-`P19` v1.0.0 Release Declaration And Version Authority tranche, `P20-DG01` is complete through PR `#551` as the design-gate slice that promoted `P20-RG01 v1.0.0 Release Declaration And Version Authority`, and `P20-RG01` is complete through PR `#553` as the release declaration and manifest-version authority reconciliation slice.
- `P21` has now been copied into the live program and tracker as the post-`P20` v1.0.0 Live Surface Revalidation tranche, with `P21-QA01` complete as the live-entry confidence slice that had to pass before product/backlog expansion was promoted.
- `P22` has now been copied into the live program and tracker as the post-`P21` Production Go-Live Operations tranche, with `P22-GO01` complete as the operational go/no-go slice, `P22-GO02` complete through PR `#561` as the register G07 code-blocker fix, `P22-GO03` complete through PR `#568` as the member-claim SLA copy fix that cleared the final production G09 release-gate blocker, and `P22-GO04` complete as the non-scripted launch operations acceptance record.
- `P1C` has now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` and is now the active post-infrastructure tranche.
- `C01` through `C06` have now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the committed commercial-contract publication queue.
- `C01` is complete as the coverage-matrix publication slice.
- `C03` is complete as the Free Start and hotline disclaimer slice.
- `C05` is complete as the claims-first scope-tree and referral-boundary slice.
- `C06` is complete as the commercial-funnel instrumentation slice.
- `P1T` has now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` and is now the active post-`P1C` tranche.
- `T01` through `T06` have now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the committed Free Start and Trust UX queue.
- `T01` is complete as the hero, trust-strip, and footer-safety-net slice.
- `T02` is complete as the Free Start claim-pack generator shell slice.
- `T03` is complete as the confidence-score and recommended-next-step slice.
- `T04` is complete as the evidence prompts, privacy badge, and SLA microcopy slice.
- `T05` is complete as the `/services` alignment slice for the coverage matrix and referral boundaries.
- `T06` is complete as the i18n and component-test coverage slice for the new trust surfaces.
- `P2` has now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` and is now the committed post-`P1T` tranche.
- `B01` through `B06` have now been restored into the live program and tracker as the existing in-place Phase 2 billing hardening queue that the March 9 blueprint diff explicitly preserved.
- `B07` through `B10` have now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the commercial terms enforcement extension of `P2`.
- `P2` is now complete as of 2026-03-12. `B01` is complete as the Stripe residue cleanup and billing-surface contract reconciliation slice, and `B02` through `B10` are complete as the verified billing hardening queue.
- `P3` has now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` and is now the committed post-`P2` tranche.
- `M01` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the first committed `P3` queue item.
- `M01` is now complete as of 2026-03-12. The audit confirms that agreement acceptance and cancellation already have canonical typed server actions, while Free Start submission and escalation request are still telemetry-led, matter consumption lacks a dedicated allowance mutation, and roster import remains a client-only placeholder.
- `M02` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P3` queue item after `M01`.
- `M02` is now complete as of 2026-03-12. Free Start completion now routes through a typed server action, claim submission now returns server-validated escalation-request metadata, and agreement acceptance plus cancellation remain on canonical `_core.ts` action paths.
- `M03` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P3` queue item after `M02`.
- `M03` is now complete as of 2026-03-13. Explicit idempotency keys now guard Free Start, claim escalation, agreement acceptance, and cancellation mutations on the canonical typed action paths, and deterministic reseed cleanup now accounts for persisted commercial idempotency records.
- `M04` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P3` queue item after `M03`.
- `M04` is now complete as of 2026-03-13. Canonical staff-led recovery transitions now consume annual matter allowance once per claim, block exhausted allowance by default, and record a staff-only explicit override reason when recovery begins without available allowance.
- `M05` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P3` queue item after `M04`.
- `M05` is now complete as of 2026-03-13. Escalation acceptance now persists decision actor, decision time, reason, and resulting next state in the durable agreement record, while decline decisions on the canonical staff action path now require a reason and record the same decision metadata in audit history.
- `M06` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P3` queue item after `M05`.
- `M06` is now complete as of 2026-03-14. Migrated Free Start, claim submission, staff commercial action, and membership cancellation callers now import the canonical `.core.ts` server actions directly, compatibility-only commercial wrapper modules were removed, and `apps/web/src/actions/claims.ts` no longer re-exports `submitClaim`.
- `P4` has now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` and is now the committed post-`P3` tranche.
- `S01` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the first committed `P4` queue item.
- `S01` is now complete as of 2026-03-14. The staff claims queue now keeps actionable-only scope while adding branch-aware assignment, status, and search filters with unit and browser proof on the canonical `/staff/claims` surface.
- `S02` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4` queue item after `S01`.
- `S02` is now complete as of 2026-03-14. Staff can now assign claims manually from the canonical `/staff/claims/[id]` detail surface within existing tenant and branch guardrails, while load-based auto-assignment remains deferred.
- `S03` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4` queue item after `S02`.
- `S03` is now complete as of 2026-03-14. Claim submission now persists the initial public `submitted` stage-history event, member claim detail keeps a public tracker even when legacy public history rows are absent, and staff-published claim status changes revalidate the canonical member tracker routes.
- `S04` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4` queue item after `S03`.
- `S04` is now complete as of 2026-03-14. Staff and member claim detail surfaces now distinguish SLA states that are waiting on missing member information from SLA states that are actively running on the canonical claim-detail paths.
- `S05` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as a security-coupled isolation slice ahead of the remaining `P4` queue.
- `S05` implementation landed on 2026-03-14. Non-staff claim-message reads now hard-filter internal notes, member claim-tracking timeline reads stay tenant-scoped and public-only, and a dedicated gate-path negative E2E spec now inserts a staff-only history note and proves that the canonical member claim detail never renders it. That deterministic browser proof now passes in both `gate-ks-sq` and `gate-mk-mk`.
- `S07` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4` queue item after `S05`, intentionally promoted ahead of `S06`.
- `S07` is now complete as of 2026-03-14. Canonical staff and member claim detail surfaces now show annual matter usage and remaining allowance from the shared `M04` allowance semantics, and the deterministic gate-path browser proof now passes in both `gate-ks-sq` and `gate-mk-mk`.
- `S06` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4` queue item after the intentional `S07`-before-`S06` exception.
- `S06` is now complete as of 2026-03-14. The routed member claim detail and routed staff claim detail now both expose the canonical messaging panel on tenant-scoped claim reads, staff can send public messages and internal notes from those live surfaces, members never receive internal-note controls, and read-receipt polish remains optional.
- `S08` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4` queue item after `S06`.
- `S08` is now complete as of 2026-03-14. Staff must explicitly accept or decline recovery matters before `negotiation` or `court` can begin, decline decisions now require a typed taxonomy on the canonical staff action path, the member claim detail shows only the safe decision summary, and the deterministic gate-path browser proof now confirms that staff keep the internal explanation while members see only the member-safe accepted state.
- `S09` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4` queue item after `S08`.
- `S09` is now complete as of 2026-03-14. Accepted recovery matters on the canonical staff mutation path now require both the full agreement snapshot and a usable success-fee collection path before `negotiation` or `court` can begin, accepted legacy states with missing collection data render as blocked instead of silently ready, and the deterministic gate-path browser proof now confirms the missing-collection prerequisite state on the routed staff claim-detail surface.
- `S10` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4` queue item after `S09`.
- `S10` is now complete as of 2026-03-15. Guidance-only and referral-only matters now reuse the canonical launch-scope category semantics on the staff mutation path, cannot save escalation agreement or success-fee collection data, cannot move into `negotiation` or `court`, and surface a staff-only launch-scope restriction on the canonical claim-detail action panel without exposing staff-only operational text to members.
- `GA01` has now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed item after the completed `P4` tranche so the client-only roster import placeholder can be replaced by the canonical group roster import path.
- `GA01` is now complete as of 2026-03-15. Office-tier `/agent/import` now parses canonical CSV rows on the client, routes valid rows through the typed agent import action, creates individual sponsored memberships on the canonical write path, and returns deterministic import summaries with unit coverage and verification proof.
- `GA02` has now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4G` queue item after completed `GA01`.
- `GA02` is now complete as of 2026-03-15. Office-tier `/agent/import` now exposes an aggregate-only group dashboard with activated-member counts, usage-rate rollups, open-case counts, and SLA-safe portfolio metrics while continuing to hide claim facts, notes, and documents.
- `GA03` has now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4G` queue item after completed `GA02`.
- `GA03` is now complete as of 2026-03-15. Group roster imports now create paused sponsored seats with explicit sponsor metadata, member `/membership` operations now expose sponsored activation and family self-upgrade paths on the canonical surface, and the first sponsored activation action now turns only paused sponsored subscriptions active without widening dashboard privacy boundaries.
- `GA04` has now been copied into the live program and tracker from `docs/plans/2026-03-09-interdomestik-business-model-blueprint-v1.md` and `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P4G` queue item after completed `GA03`.
- `GA04` is now complete as of 2026-03-15. Office-tier group access now has explicit privacy-and-consent boundary copy plus negative coverage that proves aggregate dashboards still hide member names, claim facts, staff-only notes, and claim documents without explicit member consent.
- `P6` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed tranche after completed `P4` and `P4G`.
- `G07` is now complete as of 2026-03-15. The scripted release-gate runner now checks `/pricing`, `/register`, `/services`, and `/member/membership` for the published commercial contract sections, records that proof in the canonical RC report, and fails GO or NO-GO status if the coverage matrix, success-fee calculator, disclaimers, or billing terms disappear.
- `G08` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P6` queue item after completed `G07`.
- `G08` is now complete as of 2026-03-15. The scripted release-gate runner now checks the public Free Start surface for the informational-only and routing-only boundary copy, checks office-tier `/agent/import` for the aggregate-only privacy boundary, and fails GO or NO-GO status if seeded member-identifying text appears on the group dashboard.
- `G09` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P6` queue item after completed `G08`.
- `G09` is now complete as of 2026-03-15. The scripted release-gate runner now checks deterministic member and staff claim-detail routes for seeded annual matter allowance counts plus seeded SLA states in both running and waiting-on-member claim statuses, and fails GO or NO-GO status if those enforcement surfaces drift from the canonical claim policy.
- `G10` has now been copied into the live program and tracker from `docs/plans/2026-03-09-blueprint-roadmap-diff-proposal.md` as the next committed `P6` queue item after completed `G09`.
- `G10` is now complete as of 2026-03-15. The scripted release-gate runner now checks deterministic accepted-case staff claim-detail routes for unsigned-agreement blocking, deduction readiness, stored-payment-method fallback, and invoice fallback, and fails GO or NO-GO status if agreement or collection enforcement drifts from the canonical accepted-recovery policy.
- `P7` has now been copied into the live program and tracker from `docs/plans/2026-03-15-pilot-readiness-blueprint-v1.md` and `docs/plans/2026-03-15-pilot-readiness-roadmap-diff-proposal.md` as the next committed tranche after completed `P6`.
- `R01` through `R08` have now been copied into the live program and tracker from `docs/plans/2026-03-15-pilot-readiness-blueprint-v1.md` and `docs/plans/2026-03-15-pilot-readiness-roadmap-diff-proposal.md` as the committed pilot-readiness and release-evidence queue.
- `R01` is now complete as of 2026-03-15. The canonical release-gate runner accepts `--pilotId`, writes the release report plus copied per-pilot evidence index and canonical `docs/pilot-evidence/index.csv` pointer row together, preserves stable `docs/...` references for the pilot artifact set, and documents that contract in the pilot runbook.
- `R02` is now complete as of 2026-03-15. `pnpm pilot:check` now resolves to the shell-backed local pre-launch verification pack, `pnpm release:gate:prod` remains the canonical production full-suite release proof command, `pnpm release:gate:prod -- --pilotId <pilot-id>` remains the canonical pilot-entry artifact command, and the pilot runbook plus go/no-go docs now rank those authorities without overlap.
- `P8` has now been copied into the live program and tracker from `docs/plans/2026-03-16-p8-pilot-redesign-blueprint-v1.md` and `docs/plans/2026-03-16-p8-pilot-redesign-roadmap-diff-proposal.md` as the next committed tranche after completed `P7`.
- `P8R` is now complete as of 2026-03-16. `RG01` through `RG05` now prove the reset gate against the new pilot id `pilot-ks-7d-rehearsal-2026-03-16`, including memory top-hit confirmation, fresh `pilot:check`, corrected Vercel log command guidance, deterministic observability-to-decision sequencing, and a fresh GO release report with copied evidence artifacts.
- `P8P` is now complete as of 2026-03-17. `SP01` is complete as the green `PD01` release-and-artifact baseline on `pilot-ks-7d-rehearsal-2026-03-16`, `SP02` is complete as the green `PD02` rollback-and-resume baseline on the same pilot id, `SP03` is complete as the green `PD03` closed-loop role-flow slice, `SP04` is complete as the bounded-amber `PD04` SLA, matter, and branch-pressure slice, `SP05` is complete as the green `PD05` privacy, RBAC, and multi-tenant stress slice, `SP06` is complete as the green `PD06` communications-and-incident slice, and `SP07` is complete as the bounded-amber `PD07` executive-review slice that concludes the rehearsal with a `repeat_with_fixes` recommendation and a canonical `pause` decision. The checked-in week-1 KPI or SLA rollup now narrows the remaining blocker to claim-based weekly threshold proof rather than missing closeout custody.
- `P9` has now been copied into the live program and tracker from repo-verified `main` history plus the supporting code, test, and pilot-evidence artifacts for the post-`P8` regional-wedge execution line.
- `E1` is now complete as of 2026-04-01. The membership-first acquisition restart, deferred tenant-classification runtime hygiene, and deterministic admin tenant-classification gate-proof slices landed through PRs `#410`, `#414`, and `#418`, keeping the default-public-tenant acquisition model intact while making tenant-classification review and proof inspectable on canonical member and admin surfaces.
- `E4` is now complete as of 2026-04-05. The member diaspora quickstart, preserved claim handoff context, persisted diaspora provenance, staff/admin origin visibility, and staff/admin origin filtering slices landed through PRs `#419`, `#420`, `#421`, `#427`, and `#429`, making diaspora / Green Card support and downstream claim operations inspectable without reopening proxy, auth, or tenancy architecture.
- `P10` has now been copied into the live program and tracker from repo-verified `main` history plus the supporting funnel, verification, and QA artifacts for the post-`P9` execution line.
- `E2` is now complete as of 2026-04-15. The membership conversion surface, assisted-business path, success onboarding, checkout-reconciliation contract, checkout-user reconciliation write path, frictionless OTP onboarding, public CTA alignment, claim-number/Paddle alignment, member follow-up, subscription-lifecycle hardening, and salvage/browser-proof follow-up slices landed through PRs `#430`, `#431`, `#432`, `#437`, `#439`, `#442`, `#446`, `#447`, `#452`, `#453`, `#460`, and `#462`.
- `H01` is now complete as of 2026-04-15. The Codex review workflow, shell-guard follow-up, QA Phase C tool-surface work, Sonar-safe cleanup, member-number salvage, and post-`E2` public-entry coverage slices landed through PRs `#448`, `#451`, `#454`, `#455`, `#461`, and `#462`, keeping the post-funnel verification path inspectable on canonical repo surfaces.

## Historical Foundation, Not Current Sequencing

- `PG1`, `PG2`, and `PG3` remain accepted foundation work.
- `PG4` and `PG5` are no longer the active gating mechanism for `v0.1.0`.
- Advisory memory and boundary artifacts remain inspectable input until they are explicitly copied back into the current program and tracker.

Historical evidence-custody findings remain recorded in `docs/plans/2026-03-05-a22-evidence-reconciliation.md`.

## Review Rule

Any new roadmap, assessment, or strategy memo must end in one of two states:

- copied into `current-program.md` and `current-tracker.md`
- left as non-committed input

There is no third state.

## Proof Chain

Committed work is only considered inspectable when the tracker can answer this chain:

1. source refs
2. execution mode and run identity
3. `sonar` / `docker` / `sentry` status
4. learning status and evidence refs
