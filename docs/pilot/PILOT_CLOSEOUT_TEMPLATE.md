# Pilot Closeout Template

Copy this file to `docs/pilot/PILOT_CLOSEOUT_<pilot-id>.md`.

Use this document only after the pilot line has a canonical evidence index, a week-level rollup, and an executive review for the same pilot id.

## Header

- Pilot ID:
- Closeout date (`YYYY-MM-DD`):
- Status (`closed`/`closed_with_follow_up`):
- Expansion ready (`yes`/`no`):
- Decision posture (`pause`/`repeat_with_fixes`/`expand`/`stop`):

## Outcome

- What the pilot objective was:
- What the pilot proved:
- What it did not prove:
- Whether evidence custody stayed clean:

## Expand Readiness Status

| Requirement                                                          | Status (`met`/`not met`/`unproven`) | Evidence |
| -------------------------------------------------------------------- | ----------------------------------- | -------- |
| Fresh pilot id has complete canonical evidence trail                 |                                     |          |
| No Sev1 exists                                                       |                                     |          |
| No unresolved Sev2 older than one operating day exists               |                                     |          |
| Privacy / RBAC rerun is green                                        |                                     |          |
| Triage SLA stayed within threshold                                   |                                     |          |
| Public update SLA stayed within threshold                            |                                     |          |
| `2 Operating-Day Progression Rate` is no longer the known weak point |                                     |          |
| Executive review explicitly justifies `expand`                       |                                     |          |

## Decision

- Final recommendation (`pause`/`repeat_with_fixes`/`expand`/`stop`):
- Why that recommendation is correct now:
- If the answer is not `expand`, what exact stop date or end condition applied:

## Canonical References

- Copied evidence index:
- Week-1 KPI / SLA rollup:
- Executive review:
- Release report:
- Rollback target:
- Daily exports:

## Required Follow-Up

| Owner | Deadline | Action |
| ----- | -------- | ------ |
|       |          |        |
