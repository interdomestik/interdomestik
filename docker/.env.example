# Docker Environment Overrides
# Copy this to docker/.env

# Redis
REDIS_URL=redis://redis:6379

# Mailpit (SMTP)
SMTP_HOST=mailpit
SMTP_PORT=1025

# MinIO (S3)
S3_ENDPOINT=http://minio:9000
S3_PUBLIC_URL=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET_NAME=claim-evidence

# App URL (Internal Docker Network)
NEXT_PUBLIC_SITE_URL=http://web:3000
BETTER_AUTH_URL=http://web:3000

# Database
# If using host database, use host.docker.internal or your IP
# DATABASE_URL=postgresql://user:pass@host.docker.internal:5432/db
