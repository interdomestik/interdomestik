'use server';
import {
  cancelClaim,
  createClaim,
  submitClaim,
  updateClaimStatus,
  updateDraftClaim,
} from './claims.core';

export { cancelClaim, createClaim, submitClaim, updateClaimStatus, updateDraftClaim };
