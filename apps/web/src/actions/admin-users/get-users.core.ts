// Thin wrapper to keep import path stable while implementation lives in the domain package.
export type { GetUsersFilters } from '@interdomestik/domain-users/admin/get-users';
export * from '@interdomestik/domain-users/admin/get-users';
